# PRODUCTION PLAN — v1.3.0「资产精修 · 可玩装置」

> 本文档在动手写任何实现代码之前提交（阶段 0 产物），定义本轮制作要达到的
> 画质 / 材质 / 音效 / 配乐 / 交互 / 讲解 六项标准、分厅优先级与验收方式。
> 对应工作流见 `WORKFLOW.md`，时间记录见 `WORKLOG.md`，门禁见 `QUALITY_GATES.md`（19–24）。

---

## 0. 本轮用户意图（校正后）

1. **留白继续保持**，但补充「基于公开事实的一行短释」或「风格化的空间提示」；
   **禁止复述电影/剧集叙事情节**（不做剧情还原式旁白与展签）。讲解量回到 v1.0 克制水平。
2. **主目标：资产精细化**。去掉「统一 roundedBox 家具套件」观感——每件关键道具要有
   自己的部件层级与比例；材质达到文档定义的 **PS4 级风格化实时画质**（见 §2 定义）。
3. **可交互性大幅提升**：每厅显著增加可操作物，让展馆像一件可玩的数字艺术装置。

---

## 1. 画质标准（PS4-tier stylized realtime 定义）

### 1.1 定义（可勾选，不空喊）
「PS4 级可游玩风格化实时画质」在本项目落地为下列全部成立：

- [ ] **G1 分辨率策略**：渲染像素比按画质档钳制（高=min(DPR,1.5)，低=1.0）；
      后处理链分辨率随之缩放；UI 始终原生 DPR。
- [ ] **G2 法线细节**：关键材质（木/砖/金属/织物/沥青/水泥）拥有从高度图程序推导的
      **真实法线贴图**（Sobel 近似），而不是只靠 bumpMap 单通道；斜射光下有可见的浮雕感。
- [ ] **G3 PBR 分层**：每类关键材质同时具备 map + normalMap + roughnessMap 三通道以上；
      粗糙度必须随贴图空间变化（禁止单值塑料大色块）。
- [ ] **G4 各向异性/织物**：拉丝金属使用 `MeshPhysicalMaterial.anisotropy`（three r185 支持）
      呈现方向性高光；天鹅绒/织物使用 sheen + 织纹法线；皮革使用 clearcoat + 皱纹法线。
- [ ] **G5 磨损叙事**：地面/台面/把手等高接触区域有程序化磨损（污渍、划痕、包浆、
      边缘露底色），磨损位置符合使用逻辑（门边、台面前沿、楼梯心）。
- [ ] **G6 湿润与反射**：至少两处「湿地面/静水」采用低粗糙度 + 高 envMapIntensity +
      清漆层的镜面处理（双峰跌水潭 / 夜街雨后沥青 / 石阵水面）。
- [ ] **G7 光照语言**：每厅有 1 个主光源叙事（体积光锥/窗光/炉光），2 种以上动态光行为
      （闪烁/呼吸/开关），暗部不死黑（环境光 + envMap 补底）。
- [ ] **G8 后处理逐厅调色**：`meta.look` 逐厅设定饱和/色调/雾/曝光/泛光；胶片颗粒、
      暗角、色差全局统一，不过曝、不塑料。
- [ ] **G9 降级档保帧**：低画质档 = 关 Bloom + 像素比 1.0 + 颗粒/扫描线/色差减半，
      一键切换（Q）+ 低帧自动降档（5s@<32fps），切换不重建场景。

### 1.2 非目标（明示不承诺）
- 不承诺光线追踪、不承诺 4K60、不承诺主机独占级 GI；
- CI 环境为软件渲染，仅验证复杂度预算与视觉截屏，帧率结论以复杂度预算推定（预算见 §6）。

---

## 2. 材质标准

### 2.1 纹理尺寸表（程序化 Canvas，上限硬约束）

| 用途 | 反照率 | 法线 | 粗糙度 | 备注 |
|------|--------|------|--------|------|
| 地面主材（木/折线/沥青/水泥） | 1024² | 512² | 512² | 每厅 ≤2 张 1024² |
| 墙面（砖/木板/帷幕底噪） | 512² | 512² | 256² | repeat 平铺 |
| 家具/道具主材 | 512² | 256² | 256² | |
| 小物件/铭牌 | 256² | — | — | |
| 文字展签/灯牌 | ≤1024² | — | — | 唯一允许 1024 的非地面用途 |

- anisotropy（贴图采样各向异性过滤）统一 8；`SRGBColorSpace` 只用于反照率。

### 2.2 材质工厂（kit 提供，禁止在展厅内手拼裸材质）
`woodMat / brassMat / chromeMat / ironMat / fabricMat / leatherMat / velvetMat /
concreteMat / asphaltMat(wet) / brickMat / glassMat / waterMat` ——
每个工厂输出完整 PBR 通道组合；同一厅内同类材质共享实例（控内存与编译）。

### 2.3 关键资产独有材质
以下资产禁止套用通用材质，必须有专属贴图绘制逻辑：
大厅折线地板（拼板缝+磨损）、档案廊红毯（绒毛方向）、橡皮头锅炉（铆钉带+锈迹）、
蓝丝绒吧台台面（酒渍杯印）、双峰 diner 台面（五十年代 boomerang 纹）、
夜街湿沥青（车辙反光）、林奇房间桌面（颜料渍+烟灼痕）。

---

## 3. 模型标准（去「大方块」）

- 每件关键道具 ≥4 个有命名意义的部件层级（如台灯 = 底座/立杆/关节/灯罩/灯泡/拉链开关）；
- 轮廓禁则：视平线高度的道具不允许出现未打断的 >1m 直边（加线脚/包边/倒角/分段）；
- 车削件（LatheGeometry）用于一切旋转体（灯罩/瓶/杯/柱础/壶）；
- 合并策略：静态同材质部件合并为单 mesh（`mergedMesh`），动态/可交互部件独立；
- 重做清单（每厅至少 2 件「重做而非调参」的关键模型）：

| 厅 | 必重做 | 说明 |
|----|--------|------|
| lobby | 黄铜多臂吊灯、纪念碑石 | 吊灯=中轴+6 曲臂+烛杯+灯罩；碑石=削角+铭文+基座线脚 |
| archive | 荧光灯具、卡片目录柜 | 灯具=吊杆+反光罩+双管；目录柜=抽屉阵列+铜拉手 |
| eraserhead | 大机器、锅炉炉门 | 机器=机体/齿轮箱/飞轮/连杆/铭牌/管束；炉门=铰链+观火窗 |
| bluevelvet | 舞台台口、点唱机 | 台口=拱形檐+侧柱+脚灯槽；点唱机=拱顶+格栅+氖管 |
| twinpeaks | 老轿车、diner 柜台 | 车=翼子板/舱窗/镀铬杠/大灯；柜台=台面+裙板+糖罐纸巾组 |
| mulholland | 剧场排椅、电话亭 | 排椅=翻座+扶手+排灯；电话亭=折门+话机+顶灯 |
| studio | 工作桌、收音机 | 桌=抽屉+桌腿旋木+桌面器物组；收音机=格栅+旋钮+表盘 |

艺术二遍/三遍追加重做（阶段 7 实际完成，超出上表）：蓝丝绒桌灯与酒瓶墙（三剖面×三色玻璃）、
档案长椅与 16mm 放映机展台、阀杆铸座与漏汽接头、垃圾箱与瓦楞铁皮巷墙、diner 靠窗卡座与踏脚铜杆、
锅炉鞍座/钢箍/灰坑/铆接蒙皮、房间圆毯与拼板木顶明椽、立式烟灰缸、装饰派俱乐部椅（红房间×2+读书角）、
剧场台口拱架、瀑布三段活水、吧台壁挂电话、大厅顶冠洗光。

---

## 4. 音效标准

- **交互音覆盖率 100%**：hotspot 激活统一有 click 基音（系统层保证），且每类新交互
  另配专属合成音（拨杆/齿轮/倒酒/收银机/点唱机/风扇/百叶/打字机/断路器/水花……）；
- **空间感**：新增 `sfxAt(name, x, z)` 位置化通道（实现定稿：StereoPanner 声像 +
  幂率距离衰减 `spatialParams`，比 HRTF PannerNode 更省且立体感足够；纯几何部分可单测）；
  听者位姿经 `setListener` 每次播放时取自相机 yawObject；
- **脚步系统（阶段 7 增补落地）**：7 种程序化地面音色（wood/concrete/tile/carpet/
  asphalt/dirt/metal），按步幅 0.82m 触发、左右交替微声像、每步频率抖动、传送保护；
  逐厅 `meta.floorSfx` + 混合厅 `surfaceAt(x,z)` 分区映射（夜街沥青→diner 瓷砖、
  剧场地毯、锅炉步道钢格栅、圆毯绒面……走过材质边界脚下即换声）；
- **空间混响（阶段 7 增补落地）**：交互音发送总线接程序化 IR 卷积（指数衰减噪声 +
  单极点高频阻尼，左右去相关，零采样），四种空间预设（hall 高挑长尾 / room 绒面短促 /
  tiled 瓷砖亮尾 / outdoor 近干声），逐厅 `meta.space` + 混合厅 `spaceAt(x,z)` 分区
  （走进 diner/剧场/红房间，尾音立刻收拢）；预设参数表 `SPACES` 可单测；
- **闪避**：`duck()` 保留——旁白/惊吓前抽真空；
- **零采样版权策略**：一切声音由振荡器+噪声+滤波实时合成（合规扫描单测已有，保持全绿）。

## 5. 配乐标准

- 爵士层（`jazz.js`）保持纯程序化合成：刷镲摇摆 + 行走贝斯 + 和声钢琴；
- 输出总线限幅 ≤ -12dB 相对底噪，不得盖过留白静默（duck 时爵士同样被压）；
- 可开关：V 键旁白模式循环含爵士档；**新增大厅留声机交互**可独立点播/停止爵士层；
- 底噪（drones）分层保留逐厅配方；新厅内交互（点唱机）触发的短乐句 ≤20s 自动结束。

## 6. 性能预算（硬门禁，不放宽；阶段 6 收紧定稿）

- 每厅 meshes ≤ **220**、tris ≤ **200,000**、动态光源 ≤ **40**（冒烟硬断言，超标非零退出）；
  实测最重厅：大厅 195 mesh / 123k tris / 25 lights，其余各厅均在 150/85k/25 以内；
- Canvas 纹理尺寸遵守 §2.1 上限；像素比 ≤1.5；
- 七厅懒加载分包保留；切厅 `disposeGroup` 全量释放；
- **自动降档保帧**：高画质下持续 5s 低于 32fps → 自动切低档并提示；
  玩家手动按 Q 后自动降档退位（尊重玩家选择）。

## 7. 交互标准

- **每厅非导航可交互物 ≥8**（门/传送门不计入），**双峰 ≥10、林奇的房间 ≥10**；
- 交互类型至少覆盖：开关（灯/机器）、拉动（杆/帘/抽屉）、开启（炉门/柜）、
  播放（点唱机/留声机/收音机/投影）、坐下触发、可检视物件、**连锁反应 ≥1 条/厅**；
- 每次交互必须有 ≥2 通道反馈（声 + 光/动画/字幕短句）；
- 冒烟钩子：`__SV__.countInteractives()` 返回非导航交互数、`__SV__.activateAll()` 全量激活，
  运行时冒烟逐厅断言阈值（门禁 20）。

## 8. 讲解标准（v1.0 克制量级）

- **旁白配额**：全馆 ≤8 条（welcome+7 厅），每条 ≤16 字，全馆总字数 ≤110；
  只在首访、延迟出现一次；措辞为风格化空间提示（光/质感/空间），非剧情叙述；
- **展签配额**：每厅林奇原话展签 ≤1 块；事实铭牌（片名/年份一行）≤2 块；
  禁止策展长文、二手标签、精神分析词汇（单测已断言，保持）；
- **字幕短句**：交互字幕 ≤22 字，只描述眼前物性/感受，禁止叙事还原
  （禁词单测：角色名、原对白、"后来/然后他/接着他"式叙事连接词）；
- 原叙事剧透清零：删除现存的直接对白引用（如粉笔字 "he's the one"）。

## 9. 分厅优先级

1. **twinpeaks（最高）**：分区最多、道具最密（车/柜台/红房间/瀑布），交互配额最高；
2. **studio**：个人世界，交互配额最高之二，器物最密（桌面组/唱机/打字机）；
3. **bluevelvet / mulholland**：舞台与剧场是画质名片（帷幕/霓虹/湿地）；
4. **eraserhead**：机器重做收益大（单色厅靠几何与法线细节撑）；
5. **lobby**：第一印象，吊灯+地板反射优先；
6. **archive**：以密度换气氛（目录柜/灯具），改动量最小。

## 10. 验收标准（对应门禁 19–24）

| # | 验收 | 方式 |
|---|------|------|
| 19 | 讲解 v1.0 配额 + 零叙事剧透 | `tests/narration.test.js` 扩展（配额+禁词），npm test 全绿 |
| 20 | 每厅非导航交互 ≥8（双峰/房间 ≥10） | `--smoke` 逐厅断言 `countInteractives().nonNav` |
| 21 | PS4-tier 材质/模型清单 G1–G9 全勾 + 重做清单完成 | 截屏七厅复核 + 源码审读 + 本文件勾选 |
| 22 | 性能预算硬断言保持 | `--smoke` meshes/tris 逐厅输出且退出码 0 |
| 23 | 音效/配乐抛光 | 新 sfx 单测参数域 + sfxAt 存在性 + 冒烟不抛错 |
| 24 | release 1.3.0 双 exe + SHA256 | `release/` 产物 + PE 校验 + SHA256SUMS.txt |
