# TESTING — 测试说明

## 自动化测试

```bash
npm test        # vitest 单元测试（24 用例）
npm run smoke   # 构建冒烟：生产构建 + 产物结构 + Electron 语法 + 打包配置 + 素材合规扫描
```

覆盖范围：

| 文件 | 覆盖 |
|------|------|
| `tests/filmography.test.js` | 年表数据完整性、字段合法性、id 唯一、展厅关联、排序 |
| `tests/guestbook.test.js` | 留言墙发帖/点赞/回复闭环、持久化、输入清洗、损坏数据恢复、XSS 转义 |
| `tests/audio.test.js` | 每厅底噪配方存在、振荡器/噪声/滤波参数安全范围、低频成分存在、事件音间隔 |
| `tests/compliance.test.js` | 合规声明关键词、仓库零媒体素材文件、原创文案非空、README 免责声明 |

## 运行时冒烟（Electron，Linux 下用 xvfb）

```bash
npm run build
xvfb-run -a npx electron . --no-sandbox   # 观察窗口创建无异常、控制台无未捕获错误
```

## 手动测试清单

### 启动与大厅
- [ ] 启动幕布显示标题、免责声明、加载进度；按钮变为「掀开帷幕」后可点击
- [ ] 进入后播放欢迎旁白（有 TTS 则朗读，无 TTS 显示字幕），底部字幕出现
- [ ] 大厅可见：黑白折线地板、红帷幕环廊、旋转霓虹标题、五扇门、吊灯闪烁、烟雾与光尘

### 漫游与交互
- [ ] 点击画面锁定指针；WASD/方向键移动，Shift 加速，鼠标环视；Esc 解锁
- [ ] 准星对准发光热点时放大变粉、提示条出现；按 E 或点击触发
- [ ] 中央碑石 → 生平面板；展签 → 策展文章面板；门 → 转场（黑幕+呼啸声）进入对应展厅
- [ ] 边界约束生效：无法穿墙/出界

### 六个展厅逐检
- [ ] 档案长廊：13 块年代灯牌均可点开档案；荧光灯随机熄灭；尽头纪念墙+烛火
- [ ] 橡皮头：近单色画面；拉动阀门 → 金属撞击+蒸汽喷发+字幕；暖气炉/声音设计展签可读
- [ ] 蓝丝绒：舞台聚光+话筒；点桌灯 → 全场熄灯变蓝，再点恢复；霓虹 THE BLUE ROOM 闪烁
- [ ] 双峰：松林+地表雾+萤火+掠空猫头鹰剪影；咖啡杯互动；红帷幕之门返回大厅
- [ ] 穆赫兰道：夜路+一盏坏路灯；剧场内点蓝色立方体 → 全屏反色渐变恢复；话筒台词字幕
- [ ] 每厅环境底噪不同；进厅首次播放该厅旁白

### UI 与社交
- [ ] T 年表：13 条目按年代排序，「查看」打开档案，可跳转相关展厅；策展文集 6 篇可读
- [ ] G 留言墙：发帖（空内容拒绝）、点赞切换、回复；刷新/重启后数据保留（localStorage）
- [ ] 输入 `<script>alert(1)</script>` 作为留言 → 仅显示为纯文本，不执行
- [ ] C 合规页完整显示；M 静音立即生效；V 关闭旁白后不再朗读；Q 切画质；F 显示 FPS ≥ 30

### 响应式
- [ ] DevTools 模拟 375×667：底部按钮栏换行可点，虚拟摇杆移动、右半屏拖动环视、「互动」按钮可触发热点

### Windows 产物
- [ ] `release/SmokeVelvet-LynchArchive-Portable-1.0.0.exe` 双击直接运行（免安装）
- [ ] `release/SmokeVelvet-LynchArchive-Setup-1.0.0.exe` 一键安装后可启动
- [ ] 窗口标题含「Unofficial Fan Tribute」；音频、旁白（Windows 自带中文语音）、留言墙持久化正常
