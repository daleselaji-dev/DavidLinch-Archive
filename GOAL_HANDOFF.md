# GOAL_HANDOFF — 持续演进 Goal 交接（第 6 轮 → 第 7 轮）

> 本文件由每轮子代理更新：本轮完成了什么 / 下一轮优先做什么 /
> 当前分支与阻塞项。父代理不改代码，只据此派生下一轮 Task。

---

## 本轮（第 6 轮，v1.18.0）完成了什么

**分支**：`cursor/glb-finale-round6-v1180-a993`（基线 v1.17.0 tip
`68591c5`，origin/cursor/velvet-round5-v1170-a993，非 main）。
门禁 86–90 全绿，单测 589→**607**，四连测试（test/smoke/electron/
blender:check）全过，**已打包 1.18.0 exe**。

### 1) Blender loop 第 7 件·卡片柜抽屉阵 + 落厅第五批收官件（门禁 86）✅
- 红线内两空位二选一定档 **archive 卡片柜**（212 余量最大 >
  studio 227）。`gen_card_catalog.py` 三档五拍精修（block 19+1
  格阵立规——4 列 5 排与运行时 cardCatalog 逐位对齐、HOLE (3,1)
  留给程序化可拉抽屉 → mid 修木色/镂空标签框/断茬座盘 → fine
  起线门芯+滑出卡+逐屉顶点色+黑衬板）。第 4 拍 **box(w,h,d)
  参数序病灶**勘破实录（黑衬板 h/d 写反读成外探搁板——斜杠黑影
  横贯卡死抽屉，定点机位样张定位）。定稿 **7 mesh / 5136 tris /
  GLB 146KB**；`blender:check` 扩到**七件**。
- **落厅**：收官件落 archive **第一座**目录柜（第二座连同「没关严
  的抽屉」彩蛋原件不动）。「仅换网格保留程序化动画」**第四次
  执行**：可拉抽屉程序化原件动画不动（GLB 洞位预留）、三张静态
  阵网格原位退场（几何释放/共享材质留任/磨亮光晕贴花留任）、
  材质运行时整套重设；**换接缝抹平**——洞位脸板/拉手换 GLB 阵
  同参材质。`cardArrayReady` 承诺 + glb-landed **第六处**
  （relief/governor/bottlewall/pine/wraith/**catalog**）。
- **陈年病灶修正**：v1.4 卡沓停在 drawer 局部 z=0 探出柜面 9cm
  （GLB 换新后白格显形，场景包围盒探针勘破）——退进屉体 -0.15，
  拉开才看得见「卡片都空着」。

### 2) 新交互「卡死的抽屉」+ 音色 98（门禁 87）✅
- 顶排斜探 14mm 的那只：E → **挣一下又咬死**（drawerstuck 即时，
  **第 98 种音色**——三轮刹车后第一次涨，涨在收官件配套交互上，
  不入 REPLY_DYAD）；2.2s 后**西墙那头** drawerfar 错拍应答
  （v1.10「墙后没有房间」的旧账第二次使用——呼应成对，到此为止）。
- 克制口径：游戏时钟/零字幕/可重复/re-entry 守卫；INTERACTIVE_MIN
  archive 35→**36**（普查 **195**——刹车只为收官件开一格，
  其余六厅相等语义保持）。

### 3) 回声窗可发现性 + 访谈 34→38（门禁 88）✅
- **一句不点破的暗示**：留言簿第四条种子「回头客」（访客口吻
  传闻，不指名件/不讲机制/零 UI——三条红线单测钉死）。**暗示
  预算已用尽**：全馆至多一句。
- 访谈 +4 条**四主题齐涨**（10/10/9/9 = 38）：点子说了算/在电话上
  看电影/生活的抽象/五十年代的小城；防撞第四轮实战——三句候选
  撞 QUOTES 弃用。

### 4) GLB 轴收官转维护（门禁 89）✅
- 六件 GLB 落六厅、**studio 刻意留空**；收官红线入单测（六处
  名单钉死/studio 零导入/七件全 ≤300KB/贴顶禁入不回退）。
  **GLB 轴自此转维护：后续只修不添**。

### 5) Release 1.18.0（门禁 90）✅
- bump 1.18.0（package.json + `__SV__.version`，版本钉移交
  v118-eggs.test；v117 计数钉改「不回退」语义）；
  CHANGELOG/WORKLOG/TESTING/README/STYLE_AUDIT §9 执行账入册；
  四连全绿（**607** / smoke 5·5 / electron EXIT=0 / blender:check
  七件）；`dist:win` **完整一次成型** EXIT=0 双 exe + SHA256SUMS +
  DOWNLOAD 直链 + 发布产物冒烟复跑。

## 下一轮（第 7 轮）优先做什么

1. **GLB 轴已收官——不要产新资产**。七件资产/六处落厅是终态；
   本轴转维护：只许修复病灶或替换劣化件。若发现哪件读不出细节
   再回炉，不为「每轮一件」立新项（STYLE_AUDIT §9 口径）。
2. **彩蛋第六批（若继续）**：回声窗模板化苗头仍在（七件全 ≈6s）
   ——若深化，用「窗长随各件应答时长走」的方案（落定慢的窗短、
   落定快的窗长），**别加新机制**。若恢复加件，优先 bv（24）/
   lobby（22）低密度厅，且先自问「不加行不行」（195 的一格是
   收官特批，不是刹车解除）。
3. **「墙那头」封口**：drawerfar 的西墙叙事已用两次（v1.10 稀发 +
   卡死抽屉应答）——呼应成对，**第三次就是模板**；新交互别再把
   答案推给这面墙。
4. **访谈层**：38 条四主题 10/10/9/9，「点子」「电影」已到 10——
   若再扩建议**封顶 40** 并转质量维护（替换弱条目而非追加），
   或干脆持平；QUOTES 防撞四轮实战口径照旧。
5. **性能预算警戒**：twinpeaks 244、mulholland 244（250 预算）
   贴顶禁入照旧；archive 本轮 212→**216**（GLB 阵净账 +4）/
   era 228 / studio 227 / bv 232 / lobby 207——全馆余量最大的
   是 lobby；两贴顶厅加任何东西前先合并。
6. **音色 98**：收官特批后恢复刹车——新音色要有「不得不涨」的
   理由（新机制配声优先复用旧账）。

## 当前分支与阻塞项

- **分支**：`cursor/glb-finale-round6-v1180-a993`（已推送，PR 开向
  cursor/velvet-round5-v1170-a993）。
- **阻塞项**：无硬阻塞。环境注意（新 VM 必读）：
  - Blender 不随仓库分发——按 `scripts/blender/README.md` 重装
    （NLUUG 镜像 ~30s）；`blender:check` 缺 Blender 时跳过不红。
  - electron `--smoke` 断言跑在 **dist 构建产物**上——改厅代码后必须
    先 `npm run build` 再跑（先确认 build 成功再读冒烟数）。
  - studio 已有 v1.4 节拍器（`metro`）、v1.16 白瓷小碟（`saucer`/
    `chinaState`）、v1.17 摆针加法覆写（`metroNudge`）——新增道具
    前先 rg 同名变量防撞名；archive 新增 `cardArrayReady`/
    `stuckState`/`stuckParts` 同理。
  - GLB 截屏取证两坑：SwiftShader 陈旧帧（`SV_SHOT_DELAY=12000`；
    带 `SV_SHOT_PRE` 探针脚本时给足 150000）+ 双峰岔路机位别踩
    groveEgg 触发圈（机位用 6.7,6.2）。
  - `SV_SHOT_PRE` 探针脚本会在**每个厅**执行——探针里先
    `S.hall()` 守卫目标厅，否则在 lobby 就炸。
  - Blender 生成脚本里 `box()` 参数序是 **(w,h,d)**——本轮黑衬板
    h/d 写反读成外探搁板的病灶已实录入 gen_card_catalog.py 注释。
  - 打包 winCodeSign/NSIS 缓存 VM 轮换后会丢，dist:win 时现场下载
    （连通性已验证可达）；**必须完整 dist:win 一次成型**，单独重跑
    nsis 会在被签名污染的 win-unpacked 上重压 app-64.7z 超限。

## Goal 终态对照（父 Goal 四目标——未终结，持续中）

- **Blender 管线成熟**：**收官**——七件资产五拍 loop + GLB 落六厅
  （tp 松/mull 魅影/lobby 浮雕/era 调速器/bv 酒瓶墙/archive 卡片柜）
  + 宽横幅机位 + 体积纪律 + 落厅红线单测化 + studio 刻意留空；
  管线闭环，轴转维护。
- **林奇风**：STYLE_AUDIT 九节病灶→纪律→执行账六轮闭环；本轮
  「不深化即最好的深化」（回声窗模板不续苗）+ 暗示预算一句用尽。
- **丰富彩蛋互动**：195 件交互（+1 收官特批）；七厅彩蛋五批 +
  七件二层化 + 卡死的抽屉；下一轮警惕「墙那头」第三次使用与
  回声窗模板化（STYLE_AUDIT §9）。
- **合规访谈**：38 条 + 四主题 10/10/9/9 + 低语朗读，防撞纪律四轮
  实战生效。
