# GOAL_HANDOFF — 持续演进 Goal 交接（第 5 轮 → 第 6 轮）

> 本文件由每轮子代理更新：本轮完成了什么 / 下一轮优先做什么 /
> 当前分支与阻塞项。父代理不改代码，只据此派生下一轮 Task。

---

## 本轮（第 5 轮，v1.17.0）完成了什么

**分支**：`cursor/velvet-round5-v1170-a993`（基线 v1.16.0 tip
`7f32c82`，origin/cursor/smoke-velvet-round4-v1160-a993，非 main）。
门禁 81–85 全绿，单测 552→**589**，四连测试（test/smoke/electron/
blender:check）全过，**已打包 1.17.0 exe**。

### 1) Blender loop 第 6 件·酒瓶墙精修件 + 落厅第四批（门禁 81）✅
- `gen_bottle_wall.py` 三档五拍精修（block 比例架两排 12 位+躺瓶
  槽位立规 → mid 修奶瓷玻璃/K3 车削棱/药房货架阵形 → fine 捻口环+
  堆唇+跟珠+逐瓶明暗顶点色）。**首件宽>高横幅资产**——
  `render_views.py` 新增 **wide 机位**（5.6H/30mm，四机位裁横边
  病灶）。体积纪律两步收编 **469→371→297KB**（16→14→12 段+捻口环
  两点法）。定稿 **4 mesh / 11676 tris / GLB 297KB**。
- `blender:check` 扩到**六件**资产比例架。
- **落厅红线执行**：第四批只许 bv/archive/studio（era 每厅 ≤1 件
  已有调速器、tp/mull 244 贴顶禁入——三条全入单测）；首件落
  **bv 吧台背柜**，替换程序化酒瓶墙 v2。
- **仅换网格保留程序化动画（第三次执行）**：程序化三件即兜底、
  GLB 就位原位退场（净账 +1 mesh：bv 231→**232** 冒烟实证）；
  「电压不稳」闪烁改驱 `bottleGlassMats` **活登记表**（换网格
  不换电压线）；材质由运行时整套重设（GLB 只带几何/命名/COLOR_0）；
  `bottleWallReady` 承诺 + glb-landed 第五处
  （**冒烟现在共五处 glb-landed**：relief/governor/bottlewall/pine/wraith）。

### 2) 彩蛋第五批·问第二遍（门禁 82）✅
- **即时轴执行**（等待感堆积清账）：零新增等待——给 v1.16 七件
  各加第二层：应答落定后的**回声窗**（echo，游戏时钟自复位 ≈6s）
  内再问，**同拍即答**、答在意想不到的通道：
  - lobby 烛剪→火苗不动，**三对帷幕流苏**齐晃；
  - archive 上弦钥匙→钥匙不响秒针不动，**停摆钟分针**同拍挣；
  - era 结霜支管→冰不裂，**大机器转速**沉半口（调速器塌臂）；
  - bv 空话筒→脚灯不亮，**酒瓶墙玻璃**泛一口光（GLB 与兜底同答）；
  - tp 保温座→汽不旺，**旋转派柜**悄悄转过一格；
  - mull 路灯铁杆→**光先声无**（第一遍声先光迟，第二遍因果换位）；
  - studio 白瓷小碟→瓷不嗒，**节拍器摆针**无声点头（走时不插话）。
- **克制账**：零新增交互（**INTERACTIVE_MIN 逐厅相等入单测**——
  五轮连涨惯性刹车，21/35/29/23/26/24/29 持平）/ 零新增网格/光源/
  字幕 / **零新增音色**（97 持平）；即答分支源码级审计**零
  setTimeout / 零 sfx / 零 caption**；mull 即时重放不续窗
  （replay 标记，七件同口径：答一次即消耗）。

### 3) 时间错位轴共用件盯防（门禁 83）✅
- **勘破口径修正**：停摆钟挣扎的是**分针**（clockState 唯一写者）、
  上弦钥匙挣的是**秒针**（windState 唯一写者）——两针各只有一个
  写者，单写者纪律入单测；问第二遍只点火 clockState 不直驱任何针；
  windState `k≥3.4` 全量归位后才开回声窗（自复位兜底钉死）。
- **并发抽查实录**（冒烟探针，clockMinHand/clockSecHand 取证句柄）：
  同帧双触发（停摆钟+上弦钥匙）→ 两针各自挣扎各自归位；回声窗
  二拧 → 分针同拍挣、**秒针纹丝不动**、全部归位（六项断言全 true）。

### 4) 访谈 32→34 条·补「此生」主题（门禁 84）✅
- +2 条：**过去上的色**（纪录片访谈 2016——过去不是固定的，记忆
  一直在给它上色）/ **不退休**（公开播报 2024——「我心里满是幸福，
  而且我永远不会退休」）。「此生」6→**8**（四主题 9/9/8/8 = 34）。
- **防撞纪律第三轮实战**：'retire' / 'the past' 关键词与 essays.js
  QUOTES 逐句比对零重复；出处类型收敛勘破——「公开声明」不在
  类型白名单（访谈/著作/讲座/播报），改「播报」。

### 5) Release 1.17.0（门禁 85）✅
- bump 1.17.0（package.json + `__SV__.version`，版本钉移交
  v117-eggs.test——旧测试文件不再钉死版本号）；
  CHANGELOG/WORKLOG/TESTING/README/STYLE_AUDIT §8 执行账全部入册；
  四连全绿（**589** / smoke 5·5 / electron EXIT=0 / blender:check
  六件）；`dist:win` **完整一次成型** EXIT=0 双 exe + SHA256SUMS +
  DOWNLOAD 直链 + 发布产物冒烟复跑。

## 下一轮（第 6 轮）优先做什么

1. **Blender loop 第 7 件**：红线内只剩两个空位——**archive
   卡片柜抽屉阵局部**（212，余量最大）或 **studio 台灯座车削件**
   （227）。二选一落厅；两件都落完后 GLB 轴该**收官转维护**，
   不要为「每轮一件」松贴顶红线（STYLE_AUDIT §8 观察点 2）。
   五拍 loop + 体积纪律 ≤300KB 照旧；宽>高资产记得用 wide 机位。
2. **彩蛋第六批（若继续）**：回声窗口径统一（全 ≈6s）是「同一套
   模板」的苗头——若再深化，考虑**窗长随各件应答时长走**，别再
   加一层新机制（STYLE_AUDIT §8 观察点 3）。INTERACTIVE_MIN 刹车
   后若第六批想恢复加件，优先落 bv（24 件）/lobby（22 件）低密度厅。
3. **回声窗可发现性**：问第二遍七件全部零提示——刻意的，但七件
   全藏可能全程无人发现。若有顺手的位置（访谈语境句等），可考虑
   **一句**不点破的暗示，禁 UI 提示（STYLE_AUDIT §8 观察点 1）。
4. **访谈层**：34 条 + 四主题 9/9/8/8 均衡；若再扩四主题齐涨为宜
   （单主题 ≥10 前先扩别的）；新增前先查 essays.js QUOTES 防撞
   （三轮实战教训：doughnut/beneath the surface/出处类型白名单）。
5. **性能预算警戒**：twinpeaks 244、mulholland 244（250 预算）——
   两厅加任何东西前先合并；era 228 / studio 227 / bv **232** /
   archive 212 / lobby 207（本轮普查口径，冒烟日志为准）。
6. **音色 97 三轮未涨**：刹车成立；第 7 件 GLB 若带交互，一件新
   音色合理，不必为守恒而守恒（STYLE_AUDIT §8 观察点 4）。

## 当前分支与阻塞项

- **分支**：`cursor/velvet-round5-v1170-a993`（已推送，PR 开向
  cursor/smoke-velvet-round4-v1160-a993）。
- **阻塞项**：无硬阻塞。环境注意（新 VM 必读）：
  - Blender 不随仓库分发——按 `scripts/blender/README.md` 重装
    （NLUUG 镜像 ~30s）；`blender:check` 缺 Blender 时跳过不红。
  - electron `--smoke` 断言跑在 **dist 构建产物**上——改厅代码后必须
    先 `npm run build` 再跑（先确认 build 成功再读冒烟数）。
  - studio 已有 v1.4 节拍器（`metro` 占用）、v1.16 白瓷小碟
    （`saucer`/`chinaState`）、v1.17 摆针加法覆写（`metroNudge`）
    ——新增道具前先 rg 同名变量防撞名。
  - GLB 截屏取证两坑：SwiftShader 陈旧帧（`SV_SHOT_DELAY=12000`；
    带 `SV_SHOT_PRE` 探针脚本时给足 150000）+ 双峰岔路机位别踩
    groveEgg 触发圈（机位用 6.7,6.2）。
  - `SV_SHOT_PRE` 探针脚本会在**每个厅**执行——探针里先
    `S.hall()` 守卫目标厅，否则在 lobby 就炸（本轮实录）。
  - 打包 winCodeSign/NSIS 缓存 VM 轮换后会丢，dist:win 时现场下载
    （连通性已验证可达）；**必须完整 dist:win 一次成型**，单独重跑
    nsis 会在被签名污染的 win-unpacked 上重压 app-64.7z 超限。

## Goal 终态对照（父 Goal 四目标——未终结，持续中）

- **Blender 管线成熟**：六件资产五拍 loop + GLB 落厅五件（tp 松/
  mull 魅影/lobby 浮雕/era 调速器/bv 酒瓶墙）+ 宽横幅机位 + 体积
  纪律 + 落厅红线单测化——管线闭环，剩两厅（archive/studio）待落。
- **林奇风**：STYLE_AUDIT 八节病灶→纪律→执行账五轮闭环；本轮
  「问第二遍」证明深化可以代替加件（回声窗即时轴 + 零新增交互/
  音色的克制账）。
- **丰富彩蛋互动**：194 件交互持平（**刻意的**——五轮连涨惯性
  刹车）；七厅彩蛋五批 + 七件二层化；下一轮警惕回声窗模板化
  （STYLE_AUDIT §8）。
- **合规访谈**：34 条 + 四主题 9/9/8/8 + 低语朗读，防撞纪律三轮
  实战生效。
