# GOAL_HANDOFF — 持续演进 Goal 交接（第 7 轮 → 第 8 轮）

> 本文件由每轮子代理更新：本轮完成了什么 / 下一轮优先做什么 /
> 当前分支与阻塞项。父代理不改代码，只据此派生下一轮 Task。

---

## 本轮（第 7 轮，v1.19.0）完成了什么

**分支**：`cursor/echo-window-cadence-round7-v1190-a993`（基线
v1.18.0 tip `17546cb`，origin/cursor/glb-finale-round6-v1180-a993，
非 main）。门禁 91–93 全绿，单测 607→**632**，四连测试（test/
smoke/electron/blender:check）全过，**已打包 1.19.0 exe**。

### 1) 彩蛋第七批·回声窗窗长差异化（门禁 91）✅
- **本轮零新增交互**——「不加行不行」自问先行，第七批做成
  **差异化**而非加件（195 普查口径持平）。
- **余温总账 9s 立规**：问出口那刻起余温一共九秒——答落定花掉
  几秒、窗就剩几秒（**落定慢的窗短、落定快的窗长**）：
  tp 7.7（1.3 即答，全馆最长）/ era 7.3（1.7）/ mull·studio 6.0
  （3.0 账落老值不动）/ bv 5.7（3.3）/ archive 5.6（3.4——
  **旧 8s 特批退役**：最慢落定不再配最长窗）/ lobby 5.3
  （3.7 七件最慢，全馆最短）。
- **机制零改动**：echo 倒数/游戏时钟自复位/即答消耗/mull replay
  不续窗全部原样——只改常数 + 每厅留账注释（算式 `9 − 落定` 在源
  可溯源）。模板拆除：窗长从 1 种值变 **6 种值**。
- v119-eggs.test **25 用例**：七件窗长精确钉 + 总账守恒 9.0 +
  ≥5 种窗长 + 落定/窗长逆序单调 + 机制不回退（零新 setTimeout）+
  版本钉 1.19.0；v117 archive 8s 精确钉放宽移交、v118 版本钉移交
  同值语义。
- **运行时探针双厅取证**：archive 边界双证（落定后 4.5 游戏秒
  二拧分针挣/秒针不动 · 7.0 游戏秒三拧开新序列——旧 8s 账在此
  必挣分针，新 5.6s 账正确过期）+ tp 延长端（落定后 7.9 游戏秒
  追问派柜仍转格——旧 6s 账在 7.3 已关）。探针方法论沉淀：
  注入 `engine.updaters` 记游戏秒，摆脱真钟（见下环境注意）。

### 2) GLB 轴维护巡检·维护模式首轮（门禁 92）✅
- **零新资产、零病灶、零回炉**：blender:check 七件全过 + 六
  glb-landed 信号全在 + 定向机位截屏目检五厅（lobby 浮雕/archive
  卡片柜/era 调速器/bv 酒瓶墙/tp 松）+ mull 魅影走**自然触发功能
  验证**（拐角惊吓引爆即装配正确——藏在暗处的件不摆机位硬照）。
  判定：七件全不回炉，不为「每轮一件」立新项。
- 维护轴纪律入单测：gen_*.py 恰七件不增减 / 六 GLB ≤300KB 逐件
  实测 / studio 维持零 GLB 导入。

### 3) 三条纪律入册（门禁 91 内）✅
- **「墙那头」封口**：drawerfar 全库**恰三处**且全在 archive
  卡死抽屉一件的接线里，其余六厅零借用——单测钉死，第三次使用
  从此过不了门禁。
- **访谈 38 持平**：四主题 10/10/9/9 一条未动（封顶 40 语义入测）；
  QUOTES 防撞照旧。
- **暗示预算冻结**：留言簿种子恰 4 条、「回头客」仅一条、店内 UI
  零机制词（echo/秒数字面扫描）。

### 4) Release 1.19.0（门禁 93）✅
- bump 1.19.0（package.json + `__SV__.version`，版本钉移交
  v119-eggs.test）；CHANGELOG/WORKLOG/TESTING/README/STYLE_AUDIT
  §10 执行账入册；四连全绿（**632** / smoke 5·5 / electron EXIT=0 /
  blender:check 七件）；`dist:win` **完整一次成型** EXIT=0 双 exe +
  SHA256SUMS + DOWNLOAD 直链 + 发布产物冒烟复跑。

## 下一轮（第 8 轮）优先做什么

1. **GLB 轴维护模式照旧**——七件资产/六处落厅是终态，studio 刻意
   留空。巡检走轻量口径：blender:check + 六 glb-landed 信号 +
   只在怀疑病灶时摆定向机位；零病灶就零动作。**不产新资产**。
2. **回声窗轴到此收官**：第五批开窗（v1.17）→ 暗示（v1.18）→
   差异化（v1.19）三步走完，这条轴的「再深化」大概率是过度工程
   ——**「问第三遍」就是新模板**，别做。若彩蛋继续，换新轴且先
   自问「不加行不行」；若恢复加件，优先 bv（24）/lobby（22）
   低密度厅。
3. **「墙那头」封口已单测化**——drawerfar 恰三处钉死，新交互把
   答案推给西墙会直接红测。同类要警惕的是**复用其他已成对的
   叙事**（如「另一边」六件远声谱系已满编，别再借）。
4. **访谈层**：38 条四主题 10/10/9/9 持平即可；若动，走**替换弱
   条目**而非追加（封顶 40 语义已入测）；QUOTES 防撞五轮实战口径
   照旧。
5. **性能预算警戒**：twinpeaks 244、mulholland 244（250 预算）
   贴顶禁入照旧；archive 216 / era 228 / studio 227 / bv 232 /
   lobby 207——全馆余量最大仍是 lobby；两贴顶厅加任何东西前先
   合并。
6. **音色 98 刹车照旧**：新音色要有「不得不涨」的理由（新机制
   配声优先复用旧账）。
7. **林奇风整体审视**：STYLE_AUDIT §10 观察点（回声窗窗长是
   终态值别再调 / 维护轴巡检别演变成每轮全量重摄 / 单测钉死的
   纪律别用改测试的方式绕过 / 「收官」「封口」的轴逐渐增多——
   下一轮先盘点还有哪条轴是活的）。

## 当前分支与阻塞项

- **分支**：`cursor/echo-window-cadence-round7-v1190-a993`
  （已推送，PR 开向 cursor/glb-finale-round6-v1180-a993）。
- **阻塞项**：无硬阻塞。环境注意（新 VM 必读）：
  - Blender 不随仓库分发——按 `scripts/blender/README.md` 重装
    （NLUUG 镜像 ~30s）；`blender:check` 缺 Blender 时跳过不红。
  - electron `--smoke` 断言跑在 **dist 构建产物**上——改厅代码后必须
    先 `npm run build` 再跑（先确认 build 成功再读冒烟数）。
  - **游戏时钟≠真钟**：软件渲染下 dt 封顶 0.1s、游戏时钟走
    0.08–0.42 倍速——凡计时敏感的 `SV_SHOT_PRE` 探针**别用
    setTimeout 数真秒**，注入 `S.engine.updaters` 累加 dt 记游戏秒
    （模板见 WORKLOG v1.19 P 行；`SV_WIN_SIZE=360x560` +
    `setQuality('low')` 可把倍速提到 ~0.42，探针等待给足
    `SV_SHOT_DELAY=150000`）。
  - `SV_SHOT_PRE` 探针脚本会在**每个厅**执行——探针里先
    `S.hall()` 守卫目标厅，否则在 lobby 就炸。
  - `SV_SMOKE_QUEUE` 单厅巡检：lobby 作首厅**总会先装载**——
    要 lobby 机位就指着**别的厅**跑（`SV_SMOKE_QUEUE=lobby` 会
    二次导航退出码 5，且 `SV_SHOT_POS` 全局生效会被后厅覆写）。
  - studio 已有 `metro`/`saucer`/`chinaState`/`metroNudge`；
    archive 已有 `cardArrayReady`/`stuckState`/`stuckParts`——
    新增道具前先 rg 同名变量防撞名。
  - Blender 生成脚本里 `box()` 参数序是 **(w,h,d)**（v1.18 黑衬板
    h/d 写反病灶实录在 gen_card_catalog.py 注释）。
  - 打包 winCodeSign/NSIS 缓存 VM 轮换后会丢，dist:win 时现场下载
    （连通性已验证可达）；**必须完整 dist:win 一次成型**，单独重跑
    nsis 会在被签名污染的 win-unpacked 上重压 app-64.7z 超限；
    asar extract-file 校验版本时**输出到 /tmp**，勿覆盖根
    package.json。

## Goal 终态对照（父 Goal 四目标——未终结，持续中）

- **Blender 管线成熟**：**收官转维护第 1 轮平稳**——七件资产六处
  落厅零病灶零回炉；维护纪律（件数/体积/留空）单测化，巡检方法
  （轻量口径 + 功能验证代目检）入册。
- **林奇风**：STYLE_AUDIT 十节病灶→纪律→执行账七轮闭环；本轮
  「差异化代替加件」（回声窗模板拆除、195 持平）+ 三条封口纪律
  从口头约定升级为单测钉死。
- **丰富彩蛋互动**：195 件交互持平；回声窗轴三步收官（开窗→
  暗示→差异化）——每件的余温散热快慢从此不同；下一轮先盘点
  活轴再动手（STYLE_AUDIT §10）。
- **合规访谈**：38 条 + 四主题 10/10/9/9 持平，封顶 40 语义入测；
  防撞纪律五轮实战生效。
