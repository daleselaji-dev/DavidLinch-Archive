# GOAL_HANDOFF — 持续演进 Goal 交接（第 9 轮 → 第 10 轮）

> 本文件由每轮子代理更新：本轮完成了什么 / 下一轮优先做什么 /
> 当前分支与阻塞项。父代理不改代码，只据此派生下一轮 Task。
> **本轮末尾附 Goal 终态评估：四目标俱达、七轴全维护/封口，
> 建议父代理 UpdateGoal complete（证据清单在文末）。**

---

## 本轮（第 9 轮，v1.21.0）完成了什么

**分支**：`cursor/maintenance-finale-round9-v1210-a993`（基线
v1.20.0 tip `144ec0a`，origin/cursor/live-axis-audit-round8-
v1200-d012，非 main）。门禁 97–99 全绿，单测 649→**669**，四连
测试（test/smoke/electron/blender:check）全过，**已打包 1.21.0
exe**。

### 1) 盘点增量化（门禁 99 前半，STYLE_AUDIT §12）✅
- §11 观察点第 4 条兑现：**只盘状态变了的轴**——本轮恰两条
  （studio 精修：活→维护 / 访谈维护：活→维护），五条没变的
  （GLB 维护 / 林奇风巡检 / 回声窗封口 / 三钉封口 / 第三层判死）
  引用 §11 不重盘；§12 篇幅约 §11 一半，盘点没有变成新仪式。
- 盘点结论：**两条活轴双双收进维护——七条演进轴全部处于维护/
  封口态，馆内没有「还长得动」的活轴**。

### 2) studio 转盘病灶修——问诊三件只修一件（门禁 97）✅
- **「先问哪件看着假」**兑现：磁带机（v1.9+v1.12 两轮抛光）与
  收音机 v2 问诊**零病灶不动手**（两件不动件的关键做工入测钉）；
  **转盘挂账四处实病灶**：唱臂静止横在标签正上方悬空 3.7cm /
  唱头壳下无唱头无针杆（「针尖落进沟槽」只有字幕）/ 转盘无
  主轴 / 臂无歇处。
- 修四件：主轴（并进转盘合并件）+ 歇臂柱（托槽对齐静止臂管）+
  唱头/针杆（播放摆入后针尖落到盘面沟槽带）+ 臂管重指向（静止
  落歇臂柱在唱片外、播放摆入沟槽）。**修法是工厂做工**——手作
  语言（弯点/锤痕/歪旋钮）是台灯专利，单测钉死不扩散。
- 只动做工不动账：userData 三挂点 / -0.5 rad 摆臂线 / hint /
  字幕 / 爵士接线一字不动；网格账 7→6（studio 225→**224**
  冒烟实证）；定向双样张目检（静止歇臂 + 播放入沟）成立。
- 三件老抛光件全部过账，**这条轴没有下一件——转维护**。

### 3) 访谈质量维护第二轮（门禁 98）✅
- 止损线 ≤2 内**只换一条即收手**：negativity（与同主题 poison
  领地重叠——两条都讲负面情绪伤害创作者；五词口号语义最薄，
  poison 更具体故留任）退役 → setup（「随时能动手」工作条件句，
  《钓大鱼》可查证原文）心境同格换入。
- 38 条与 10/10/9/9 双持平入测；防撞第七轮零撞车；退役理由
  三要素（领地重叠 + 语义最薄 + 留任对照）留账进数据源注释。
- 换血机制两轮成熟（弱条目判定→同格换入→防撞→留账闭环），
  **轴转维护**：下一轮起无实病灶不换血。

### 4) GLB 维护巡检第 3 轮（门禁 99 中）✅
- 更轻口径照旧：blender:check 七件全过 + 六 glb-landed 冒烟信号
  全在 + mull 拐角/转身双惊吓自然触发功能验证——**零病灶、
  零改动、零回炉**；落厅代码零改动 → 零定向机位（§10/§11/§12
  三轮承诺连续兑现）；gen_*.py 恰七件复钉。

### 5) Release 1.21.0（门禁 99 后半）✅
- bump 1.21.0（package.json + `__SV__.version`，版本钉移交
  v121-eggs.test）；CHANGELOG/WORKLOG/TESTING/README/STYLE_AUDIT
  §12 执行账入册；四连全绿（**669** / smoke 5·5 / electron
  EXIT=0 / blender:check 七件）；`dist:win` **完整一次成型**
  EXIT=0 双 exe + SHA256SUMS + DOWNLOAD 直链 + 发布产物冒烟复跑。

## 下一轮（第 10 轮——若父 Goal 收官则本节为馆史维护备忘）

1. **「维护轮」的第一病灶是发明工作**：七轴全维护后，若巡检
   零病灶就**零改动收轮**——patch 版本号与纯巡检报告是诚实的
   交付；不为「有交付」硬开新轴（§12 观察点第 1 条）。
2. **问诊制不许全量化**：只在有人报「看着假」或改了相邻代码时
   问诊单件——每轮把全馆道具问诊一遍就是全量重盘的转世。
3. **访谈轴维护口径**：无实病灶不换血；若换，止损线 ≤2 +
   退役理由三要素照旧；38 与 10/10/9/9 是钉死的账。
4. **GLB 轴维护口径照旧**：blender:check + 六 glb-landed 信号
   即可；只在改落厅代码或怀疑病灶时摆机位；gen_*.py 恰七件。
5. **封口轴五条零触碰**：回声窗七件窗长终态（v119 精确钉）、
   drawerfar 恰三处、暗示预算一句、BACK IN 5 最后字面、第三层
   反应判死——改测试绕纪律直接红（v119/v120/v121 三层钉）。
6. **性能预算警戒照旧**：twinpeaks 244、mulholland 244（250
   预算）贴顶禁入；studio 224（转盘修后又回收 1）；lobby 207
   仍是全馆余量最大。
7. **三数口径**：交互 195 / 音色 98 / 访谈 38 持平有账；若真出现
   「不得不加」的叙事需要，按老规矩开一格并留账——刹车防惯性
   不防生长。

## 当前分支与阻塞项

- **分支**：`cursor/maintenance-finale-round9-v1210-a993`
  （已推送，基于 cursor/live-axis-audit-round8-v1200-d012）。
- **阻塞项**：无硬阻塞。环境注意（新 VM 必读）：
  - Blender 不随仓库分发——按 `scripts/blender/README.md` 重装
    （NLUUG 镜像 ~100s）；`blender:check` 缺 Blender 时跳过不红。
  - electron `--smoke` 断言跑在 **dist 构建产物**上——改厅代码后必须
    先 `npm run build` 再跑（先确认 build 成功再读冒烟数）。
  - **游戏时钟≠真钟**：软件渲染下 dt 封顶 0.1s、游戏时钟走
    0.08–0.42 倍速——凡计时敏感的 `SV_SHOT_PRE` 探针**别用
    setTimeout 数真秒**，注入 `S.engine.updaters` 累加 dt 记游戏秒
    （模板见 WORKLOG v1.19 P 行）；等待摆臂/动画类取证给足
    `SV_SHOT_DELAY`（本轮转盘播放样张 22000ms 一次过）。
  - `SV_SHOT_PRE` 探针脚本会在**每个厅**执行——探针里先
    `S.hall()` 守卫目标厅，否则在 lobby 就炸。
  - **teleport 会清俯仰**（controls.teleport 内 pitch 归零）——
    俯桌面/低矮件取证时，俯仰必须写在 PRE 里**瞬移之后**：
    `S.teleport(...); S.engine.camera.parent.rotation.x = -0.5`
    （pitchObject 是 camera 的父节点；本轮转盘双样张实证口径）。
  - `SV_SMOKE_QUEUE` 单厅巡检：lobby 作首厅**总会先装载**——
    要 lobby 机位就指着**别的厅**跑（`SV_SMOKE_QUEUE=lobby` 会
    二次导航退出码 5，且 `SV_SHOT_POS` 全局生效会被后厅覆写）。
  - studio 已有 `metro`/`saucer`/`chinaState`/`metroNudge`；
    archive 已有 `cardArrayReady`/`stuckState`/`stuckParts`——
    新增道具前先 rg 同名变量防撞名。
  - Blender 生成脚本里 `box()` 参数序是 **(w,h,d)**（v1.18 黑衬板
    h/d 写反病灶实录在 gen_card_catalog.py 注释）。
  - 打包 winCodeSign/NSIS 缓存 VM 轮换后会丢，dist:win 时现场下载
    （连通性已验证可达）；**必须完整 dist:win 一次成型**，单独重跑
    nsis 会在被签名污染的 win-unpacked 上重压 app-64.7z 超限；
    asar extract-file 校验版本时**先 cd /tmp 再跑**（extract-file
    没有 --output 参数、总是写进 CWD——在仓库根跑必覆盖根
    package.json，v1.19/v1.21 两轮实踩；覆盖后 git checkout
    package.json 恢复）。
  - lathe 几何做顶点扰动时**只用整数角频率**（sin(n·a)，n∈ℤ）
    ——接缝两侧 ±π 重复顶点才会算出同一位移，不裂缝。

## Goal 终态评估（第 9 轮末——**建议父代理 UpdateGoal complete**）

父 Goal 四目标逐条对照，**全部达标且已转维护**，馆内无活轴：

1. **Blender 管线成熟 ✅（维护第 3 轮平稳）**：七件资产（浮雕/
   魅影/孪生松/图书梯/调速器/酒瓶墙/卡片柜）六处落厅 + studio
   刻意留空的收官名单三轮零变动；「仅换网格保留程序化动画」
   四次执行零回炉；维护成本逐轮递减（全量截屏→定向机位→纯
   信号巡检），纪律钉三层在岗（v118 名单钉 / v119 维护钉 /
   v121 gen_*.py 恰七件钉）。
2. **林奇风 ✅（十二节审计闭环，方法论可自持）**：STYLE_AUDIT
   §1–§12 病灶→纪律→执行账九轮闭环；「收官≠停滞」（§11 活轴
   盘点）与「维护≠发明工作」（§12 增量盘点）两套方法先后立住；
   克制口径全部单测化（字幕 ≤22 字 / 零字幕件 / 暗示预算 /
   手作语言专利权），谁来接手都改不回去。
3. **丰富彩蛋互动 ✅（195 件四轮持平，深化转向做工）**：普查
   195=22/37/30/24/30/27/25 四轮逐厅相等入测；彩蛋谱系九批
   （问第二遍/回声窗/换轴/远声应答…）全部封口有钉；后两轮的
   深化是**做工**（台灯手作 v3、转盘病灶修）而非机制——世界
   不宣布自己被精修过，但站得近的人看得见。
4. **合规访谈 ✅（38 条封顶、质量维护机制成熟）**：四主题
   10/10/9/9 钉死；换血机制两轮实战（absurdity→detectives、
   negativity→setup）闭环成熟；防撞七轮零事故；QUOTES/DOCENT/
   访谈三库互斥、出处只标类型、合理使用尺度单测扫描——合规
   从制度变成了工序。

**证据清单**（第 10 轮或收官复核可直接引用）：
- 测试：`npm test` **669/669** 全绿（26 文件）；`npm run smoke`
  5/5；electron `--smoke` EXIT=0（七厅全过 · 六 glb-landed ·
  普查 195 · mesh 峰值 244/预算 250 · 双惊吓自然触发 · 热点归属
  七厅全过）；`npm run blender:check` 七件全过。
- 产物：release/ 下 1.21.0 双 exe（Portable + Setup，均 <100MiB）
  + SHA256SUMS；DOWNLOAD.md 直链在本分支。
- 文档：CHANGELOG v1.0→v1.21 全程可溯；STYLE_AUDIT §12 盘点
  增量账（七轴全维护/封口的判定表）；QUALITY_GATES 门禁 1–99；
  WORKLOG 逐轮逐段；TESTING 抽查清单 v1.3→v1.21。
- 纪律钉：封口五条（回声窗窗长/drawerfar 三处/暗示一句/BACK IN
  5/第三层判死）+ 三数（195/98/38）+ gen_*.py 七件 + studio 零
  GLB——全部源码级单测，绕纪律改测试直接红。

**收官语义**：complete 不等于封库——若未来有新的叙事需要
（「不得不加」的一格）或有人报实病灶，按第 10 轮备忘的维护
口径开工即可；但作为「持续演进 Goal」，四目标已无未竟事项，
**建议父代理 UpdateGoal complete**。
