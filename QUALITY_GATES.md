# QUALITY GATES — 大卫·林奇数字纪念展览馆

> 本文件为交付前必须全部通过的质量门禁。每一项在交付前对照自检，
> 通过后勾选并注明验证方式。未全绿不得宣称交付。
> v1.1.0 新增门禁 10–14（旁白重做 / 彩蛋 / 林奇的房间 / 文案审计 / 重新打包）。
> v1.2.0 新增门禁 15–18（留白文案 / 多分区可逛地图 / 资产精度 / 性能预算入冒烟）。
> **v1.3.0 新增门禁 19–24（讲解 v1.0 克制+零叙事剧透 / 交互密度 / PS4-tier 材质模型清单 /
> 性能预算保持 / 音景抛光 / 1.3.0 重新打包）。**

## 门禁清单

### 1. 美术 — 统一视觉语言
- [x] 统一色板：深黑 `#0a0608` / 天鹅绒红 `#8f0e1e` / 霓虹蓝 `#3ec5ff` / 荧光粉 `#ff2e88` / 象牙白 `#f2e9dc`（`src/style.css` CSS 变量 + `src/halls/kit.js` PALETTE 常量，全站引用）
- [x] 统一字体语言：衬线展签体（Georgia/宋体，标题与字幕）+ 等宽档案体（Courier，正文/UI），全站一致
- [x] 禁止 AI 默认紫白渐变与模板脸 —— 全部界面为定制暗色档案馆风格
- [x] 至少 3 个可进入的作品展厅 + 1 个总览大厅（**实际：4 作品展厅 + 档案长廊 + 林奇的房间 + 大厅 = 7 个场景**）
- [x] 所有场景为原创程序化生成（Canvas 绘制纹理 + 代码构建几何；`tests/compliance.test.js` 扫描确认仓库零媒体素材文件）

**验证方式**：`SV_SHOT_DIR=… --smoke` 截屏七厅逐一目视复核（红帷幕/折线地板/霓虹/单色工业房/暖木工作室/松林月夜/夜路剧场均符合设定）。

### 2. 画面 — 氛围与响应式
- [x] 每厅 `FogExp2` 雾 + 漂移烟雾粒子层（`kit.smokeLayer`）
- [x] 动态光源每厅 ≥2：大厅吊灯颤动×6、档案廊荧光管×7+壁龛射灯、橡皮头铁笼灯×3+锅炉炉光、蓝丝绒桌灯×6+吧台灯带、双峰帷幕光+镇街路灯闪烁+红房间落地灯、穆赫兰道坏路灯+霓虹跑马灯+巷灯、林奇的房间台灯/顶灯/烛光/窗月光（多数可交互开关）
- [x] 粒子每厅 ≥1 套：光尘/地雾/萤火/蒸汽/烟雾/香烟细烟/瀑布水雾/深水鱼群（`dustField`/`smokeLayer`/冥想序列）
- [x] 全屏后处理链：UnrealBloom + 自研 LynchPass（胶片颗粒/暗角/扫描线/径向色差/亮度颤动/饱和度分级/梦境反色/**惊吓冲击 uShock/闪光帧 uFlash**）
- [x] 桌面与窄屏均可用：375×667 窄屏适配（底部按钮栏换行、字幕/厅名/字母旁白重排）；触屏虚拟摇杆 + 右半屏环视 + 「互动」按钮

**验证方式**：`SV_SHOT_DIR=… xvfb-run npx electron . --smoke` 截屏复核；窄屏 `SV_WIN_SIZE=375x667` 同法验证。

### 3. 性能
- [x] 场景复杂度实测（`--smoke` 输出，v1.2.0）：每厅网格 68–155 个（≈draw call 上限，预算 ≤260）、三角面 1.2 万–11.2 万（预算 ≤40 万），全部在预算内
- [x] **预算为冒烟硬门禁**：`electron --smoke` 每厅统计后断言 `meshes≤260 && tris≤400000`，超标即非零退出（见门禁 18）
- [x] 像素比钳制 ≤1.5，「画质 低」档关闭 Bloom + 像素比 1.0（`Q` 键即时切换）
- [x] 无严重卡死：展厅切换黑幕过场 + 异步 `import()`；运行时冒烟七厅连续切换无异常
- [x] 资源懒加载：7 个展厅独立分包（构建产物 `lobby-*.js` … `studio-*.js` 共 7 chunk，`npm run smoke` 校验 ≥6 且含 studio）

**验证方式**：`npm run smoke`（分包校验）+ `--smoke` 场景统计输出 + 内置 FPS 计数（`F` 键）。
注：本 CI 环境仅有软件渲染无法测 GPU 帧率；以上复杂度在近十年任意独显/核显上 30fps+ 为合理结论，真机清单见 TESTING.md。

### 4. 音效 — 全程序化合成
- [x] 每厅独立底噪配方（`src/audio/drones.js`：振荡器+噪声+滤波+LFO，含新厅 studio 台灯电流暖嗡）；单测校验每厅含 <120Hz 低频成分
- [x] 交互音 >2 类：悬停/点击/呼啸/翻页/蒸汽/金属/猫头鹰/心跳/惊吓噪墙/窃语/呼吸/收音机/火柴/开关灯/布幔/冥想钟/摇篮曲短句 等 27 种，全部 WebAudio 实时合成
- [x] 无任何采样素材：合规测试扫描 src/electron 零音频文件
- [x] 全局静音：`M` 键 + UI 按钮（增益 ramp 平滑切换）；`duck()` 突兀静默/闪避机制

**验证方式**：`npm test`（audio.test.js + jazz.test.js 参数域校验）+ 手动耳机巡检清单（TESTING.md）。

### 5. 语音 / 旁白
- [x] 原创旁白 8 段（欢迎词 + 七厅各一句，`src/data/essays.js` NARRATIONS）——**v1.2 起每段 ≤24 字符**，进厅延迟 2.6 秒后低声出现，仅首访一次
- [x] 三种体验模式 + 关（见门禁 10）；语音档为戏剧化 TTS（分句停顿/变速/耳语拍），不可用环境自动只保留字母显现（无障碍）
- [x] 可切换：`V` 键 + UI 按钮循环四档

**验证方式**：`npm test`（旁白预算断言）+ `--smoke` 校验模式循环与字母显现激活。

### 6. 社交 — 讨论区
- [x] 留言墙完整闭环：发帖（空内容拒绝/超长截断/匿名回落）、列表（倒序）、点赞（可撤销）、回复
- [x] `localStorage` 持久化（重建 store 后数据保留，单测覆盖；损坏 JSON 安全恢复）
- [x] XSS 防护：渲染层全部 `textContent` 注入 + `escapeHtml` 双保险（单测覆盖注入用例）
- [x] 林奇的房间内有 3D 软木留言板，把最新留言渲染为图钉便签（Canvas 程序绘制），点击直达留言墙

**验证方式**：`npm test`（guestbook.test.js 8 用例）+ `--smoke` 运行时发帖/点赞/回复闭环校验。

### 7. 版权合规
- [x] 应用内「版权合规」页（`C` 键）：明示非官方/非授权/独立粉丝纪念展/内容负面清单与正面清单/短引语合理使用说明
- [x] README 顶部免责声明
- [x] 内容自查：无截图/海报/原声采样/影视对白 verbatim/角色肖像/商标 Logo；全部视觉声音程序化生成（自动扫描 + 人工审读 `src/data/*.js` 全部文案）
- [x] 林奇本人语句仅取公开访谈/著作短引语（单测强制单条 ≤200 字符），注明出处类型；彩蛋与致敬全部为原创抽象重构，无精确镜头复刻（双峰各分区均为几何抽象致敬，无角色肖像/原声）

**验证方式**：`npm test`（compliance.test.js + narration.test.js 引语长度审计）+ 人工全文审读。

### 8. 测试
- [x] `npm test` 通过：**42 用例全绿**（数据完整性/留言 store/音频配方/爵士配方/旁白预算/文案审计/合规扫描）
- [x] `npm run smoke` 通过：生产构建 + 产物结构（7 分包）+ Electron 语法 + 打包配置 + 素材扫描
- [x] 运行时冒烟通过：`xvfb-run npx electron . --smoke` — 启动→自动进入→七厅装载→**场景预算断言**→**每厅彩蛋强制触发**→UI 全面板交互→旁白模式循环→留言闭环，全绿退出码 0
- [x] `TESTING.md` 手动清单齐备（含彩蛋复现步骤与新分区巡检）

### 9. Release — Windows 产物
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.2.0.exe`（便携版，双击即运行）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.2.0.exe`（NSIS 一键安装包）
- [x] `release/SHA256SUMS.txt` 校验和（v1.2.0）
- [x] `BUILD.md` 一键构建说明（含 Linux 交叉打包 wine 依赖坑位记录）
- [x] 产物验证：PE 结构校验；同一 dist 由原生 Electron 运行时冒烟全绿

### 10. 旁白体验重做（v1.1 新增）
- [x] **字母显现模式可用且为默认**：旁白逐字打出（打字机音/故障闪现错误字形/句读长停/随机静默拍），无语音也可完整传达（`src/ui/letters.js`）
- [x] **爵士氛围层可用**：程序化「深夜爵士」（63BPM 摇摆刷镲 + 行走贝斯 + 钢琴式和声 + 偶发弱音铜管 + 黑胶炒豆，全 WebAudio 合成，零真实录音；`src/audio/jazz.js`），与各厅林奇低频底噪混音，随模式开关
- [x] **语音档升级为戏剧化 TTS**：分句停顿、变速（0.68–0.92）、随机耳语拍（低音量低语速），拒绝干读
- [x] `V` 键循环 字母显现 → 爵士+字母 → 语音+字母 → 关

**验证方式**：`npm test`（narration.test.js 模式体系 + jazz.test.js 配方）+ `--smoke` 运行时模式循环/爵士层启停/字母激活断言。

### 11. 每厅彩蛋 ≥1，穆赫兰道惊吓可复现（v1.1 新增）
- [x] 大厅「帷幕后的窃语」/ 档案廊「不在年表上的灯牌」（两段式）/ 橡皮头「暖气炉里的小舞台」/ 蓝丝绒「衣柜的暗侧」/ 双峰「环形石阵·空间错位」（v1.2 移至林中岔路尽头）/ 林奇的房间「收音机自己醒来」/ 穆赫兰道「剧场背后的那个东西」
- [x] 全部为**绕过去才触发**的区域触发器（`kit.zoneTrigger`），不在导览图上，无 UI 提示
- [x] **穆赫兰道惊吓彩蛋**：绕到剧场背后空地 → 声音抽真空 → 心跳两记 → 黑影 0.26 秒扑至面前（高频痉挛）+ 合成噪墙 + 后处理冲击（色差暴涨/颗粒风暴/暗角收缩/暗红闪帧）→ 黑幕 → **空间错位**：醒来已被移回巷口。复现步骤已写入 TESTING.md
- [x] 彩蛋触发链纳入运行时冒烟（`window.__SV__.triggerEggs()` 每厅强制引爆，异常即非零退出）

**验证方式**：`--smoke` 逐厅输出 `彩蛋触发 <hall>: <name>`；手动按 TESTING.md 步骤复现。

### 12. 「林奇的个人世界」区域（v1.1 新增，v1.2 精修）
- [x] 独立展厅「HIS ROOM · 林奇的房间」可从大厅进入（第 6 扇门）
- [x] 可操作交互 ≥3（**实际 9 项**）：台灯开关 / 墙上顶灯开关 / 喝咖啡 / 点燃与掐灭香烟 / 收音机天气播报（原创文本 3 则轮播）/ 拉帘（开合决定冥想角可达性）/ 冥想深水序列（画面分级切换 + 大鱼群环游 + 冥想钟）/ 画架自绘 / 软木留言板
- [x] 小叙事链：60 秒内完成 咖啡→香烟→收音机 → 触发特别播报 + 墙上「大鱼」暗画亮起
- [x] v1.2 加可逛深度：读书角（扶手椅+程序化书架）、百叶窗夜光、编织圆毯、两幅程序化抽象画
- [x] 全部原创抽象致敬（咖啡/香烟/冥想/天气播报意象），不使用肖像与商标

**验证方式**：`--smoke` studio 厅装载 + 彩蛋触发；手动清单见 TESTING.md。

### 13. 文案审计（v1.1 新增；v1.2 由门禁 15 收紧取代上限）
- [x] 二手空标签清退：全站不再出现「超现实主义大师」「梦核」等标签堆砌（单测强制断言）
- [x] 林奇原话短引语为唯一「解读」：QUOTES 库 12 条（访谈/著作，中英对照+出处类型），每厅 1 块 3D 引语展签（`kit.quotePlaque`）
- [x] v1.2：策展文章体系整体移除，理论名词（拉康/齐泽克/精神分析）全站清零（单测断言不含）
- [x] 引语合理使用：单条 ≤200 字符 + 非商业粉丝展语境声明（合规页第 4 段）

**验证方式**：`npm test`（narration.test.js 文案审计组）+ 人工审读 `src/data/essays.js`。

### 14. 重新打包 Release
- [x] `npm test` 42 用例全绿
- [x] `npm run smoke` 构建冒烟全绿（7 分包）
- [x] 运行时 `--smoke` 全绿（七厅 + 预算 + 彩蛋 + 旁白模式 + UI + 留言闭环）
- [x] portable + setup 两个 exe 重新打包为 **1.2.0**，`SHA256SUMS.txt` 同步更新

### 15. 留白文案 —— 解释不喧宾夺主（v1.2 新增）
- [x] **策展文章体系移除**：`ESSAYS` 不再导出（单测断言 `undefined`）；档案页仅保留「他自己的话」原话摘录墙（`ui.showQuotes()`，12 条中英对照短引语 + 出处）
- [x] **旁白极简**：每厅一句 ≤24 字符（实测 7–16 字），全馆旁白总字数 ≤160（实测约 100）；仅首访触发、进厅 2.6 秒后才低声出现；禁用说教/总结腔词汇（「告诉我们/象征着/正如」等，单测断言）
- [x] **展签预算**：每厅可见文字展签（quotePlaque）≤2 —— 实际每厅恰好 1 块，只印林奇原话；说明立牌（standPlaque）全馆退场（源码级单测断言）
- [x] **字幕短句化**：厅内交互字幕字面量 ≤26 字符（源码级单测扫描全部 `ui.caption` 调用）
- [x] 合规页保留必要事实陈述，删除说教式结语；帮助/关于页文案精简
- [x] 气氛交给空间、光、声与静默：无强制弹窗式解读，一切面板均为主动打开

**验证方式**：`npm test` narration.test.js「留白预算」「展签预算」两组源码级审计；人工通读 `src/data/essays.js` 与七厅字幕。

### 16. 多分区可逛地图 —— 显著高于「单房间」（v1.2 新增）
- [x] **双峰厅 ≥3 个可走分区（实际 5 个）**，由林间小径连接为一张可逛地图（`zonesBounds` 多边界 union）：
  - ① 林间空地：红帷幕之门 + 树桩咖啡
  - ② 红房间氛围区：环形折线地板 + 红帷幕围合（留缺口接小径）+ 对坐扶手椅 + 落地灯（几何抽象致敬，无角色复制）
  - ③ 小镇夜街：沥青路+车道线、人行道、闪烁路灯、老轿车剪影（无品牌）、**可进入的 DINER**（折线地地板/木墙/柜台/圆凳/玻璃罩樱桃派/咖啡壶）
  - ④ 瀑布眺望台：木栈台+栏杆、程序化流动瀑布、跌水潭水雾、锯木厂烟囱剪影
  - ⑤ 林中岔路：环形石阵彩蛋
- [x] 其余厅同步加可逛分区与装置：档案廊**原话壁龛**、橡皮头**锅炉房**（格栅步道+铆钉锅炉+压力表+炉光）、蓝丝绒**吧台区**（背光酒瓶+黄铜脚踏+高脚凳）、穆赫兰道夜路+暗巷+后场（v1.1 已有，v1.2 加护栏/跑马灯精修）、大厅柱环与穹顶线脚、林奇的房间读书角
- [x] 树木等障碍物避让所有可走区域（`insideWalkable` 采样）；分区边界用 `multiRectBounds`/`zonesBounds` 保证不穿墙不出界

**验证方式**：`--smoke` 七厅装载 + 截屏目视复核各分区；手动按 TESTING.md 巡检路线走通全部分区。

### 17. 资产精度 —— 「PS5-tier stylized realtime」可验证清单（v1.2 新增）
- [x] **几何**：关键装置不再是裸 Box ——
  - 圆角化：`RoundedBoxGeometry`（桌板/舞台/展柜/招牌/家具，`kit.roundedBoxGeo/roundedBoxMesh`）
  - 车削/放样：话筒底座、吊灯灯罩（LatheGeometry）；柱式（柱础/柱身凹槽/柱头，`kit.column`）
  - 多部件组装+合并：门廊=圆柱+黄铜环+檐口+踏步+防滑条；扶手椅=坐垫/靠背/扶手/车削腿（`kit.armchair`）；栏杆=立柱+横杆合并（`kit.railing`）
  - 帘幕三层褶皱（三阶正弦位移 + 底摆外扩 hem）+ 幕帷 valance + 黄铜横杆（`kit.curtainWithValance`）
  - 地面接缝/镶边：大厅金圈镶边、档案廊红毯+护墙线脚、橡皮头伸缩缝、夜街人行道与车道线
  - 有机形体：位移二十面体岩石（`kit.rockMesh`）、异形显示避免尖锐穿插
- [x] **材质**：Canvas 程序纹理细节升级（anisotropy 8）——
  - 木纹（年轮细线+木结+板缝，`woodTexture`）、金属拉丝（定向细划痕，`brushedMetalTexture`）、织物织纹（经纬+起球噪点，`weaveTexture`）、污迹/磨损叠加（`grime`）
  - PBR 分层：MeshPhysicalMaterial 天鹅绒（sheen+粗糙度贴图+凹凸）、玻璃（transmission 感的高光+clearcoat）、皮革坐垫（sheen+clearcoat）；roughness/metalness 随贴图变化，无塑料感大色块
- [x] **光照**：每厅关键光有体积感/边缘光/可控闪烁 —— 体积光锥（大厅穹顶射灯/舞台/剧场）、路灯故障闪烁、荧光管衰减闪烁、烛光/炉光噪声抖动、窗缝月光、吧台灯带
- [x] **后处理**：LynchPass + Bloom 按厅调参（`meta.look`：曝光/泛光/饱和/色调/雾密度逐厅设定），电影感不过曝；`Q` 低画质档关 Bloom 降像素比保帧率
- [x] **性能预算仍守住**：上述升级后实测每厅 68–155 网格 / 1.2–11.2 万三角面，仍在预算内（见门禁 3、18）

**验证方式**：`SV_SHOT_DIR=… --smoke` 七厅截屏目视复核（无裸大方块观感）+ `--smoke` 场景统计在预算内 + 代码审读 `src/halls/kit.js`。

### 18. 性能预算入冒烟门禁（v1.2 新增）
- [x] `electron --smoke` 每厅装载后遍历场景统计 meshes / tris，**超过预算（meshes>260 或 tris>400000）立即非零退出**
- [x] v1.2.0 实测（软件渲染冒烟输出）：

  | 厅 | meshes | tris |
  |----|--------|------|
  | lobby | 155 | 111,805 |
  | archive | 68 | 15,492 |
  | eraserhead | 85 | 21,221 |
  | bluevelvet | 89 | 40,071 |
  | studio | 76 | 12,425 |
  | twinpeaks | 105 | 71,185 |
  | mulholland | 101 | 40,627 |

- [x] 纹理预算：Canvas 纹理单张 ≤1024²（多数 256²/512²），每厅纹理总量受程序生成上限约束；像素比钳制 ≤1.5
- [x] 懒加载保留：七厅独立 chunk，切换时旧厅 `disposeGroup` 全量释放

**验证方式**：`xvfb-run npx electron . --smoke` 输出逐厅统计且退出码 0。

---

### 19. 讲解回退 v1.0 克制 + 零原作叙事剧透（v1.3 新增）
- [ ] 旁白配额：全馆 ≤8 条（welcome+7 厅），每条 ≤16 字，总字数 ≤110；措辞为
      风格化空间提示（光/质感/空间感受）或公开事实，禁止剧情叙述（单测断言）
- [ ] 展签配额：每厅林奇原话展签（quotePlaque）≤1；事实铭牌仅「片名+年份」一行式
- [ ] 交互字幕 ≤22 字（源码级扫描收紧），只写眼前物性/感受
- [ ] 叙事剧透禁词清零（源码级扫描）：原作对白引用（含 "he's the one"/"就是他"）、
      角色名、叙事连接词（"后来/然后他/接着他"）等全站不出现
- [ ] 现存违规文案（穆赫兰道粉笔字对白等）删除或替换为原创风格化文字

**验证方式**：`npm test` narration.test.js「v1.3 讲解配额」「叙事禁词」组全绿 + 人工通读。

### 20. 交互密度 —— 可玩的数字艺术装置（v1.3 新增）
- [ ] 每厅非导航可交互物 ≥8（门/传送门 nav 标记不计入），双峰 ≥10、林奇的房间 ≥10
- [ ] 交互类型覆盖：开关 / 拉动 / 开启 / 播放 / 坐下触发 / 可检视物件 / 连锁反应（每厅 ≥1 条链）
- [ ] 每次交互 ≥2 通道反馈（声 + 光/动画/字幕短句）
- [ ] `__SV__.countInteractives()` 钩子返回 `{total, nonNav}`；运行时冒烟逐厅断言阈值，
      不达标非零退出

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅输出交互统计且全绿；TESTING.md 手动清单。

### 21. PS4-tier 材质/模型可勾选清单（v1.3 新增）
- [ ] PRODUCTION_PLAN §1.1 G1–G9 全部勾选（分辨率策略/法线细节/PBR 分层/各向异性织物/
      磨损叙事/湿润反射/光照语言/逐厅调色/降级档）
- [ ] 法线贴图管线落地：`normalFromHeight()` Sobel 推导，关键材质 map+normalMap+roughnessMap 三通道
- [ ] 拉丝金属使用 `MeshPhysicalMaterial.anisotropy` 方向性高光
- [ ] PRODUCTION_PLAN §3 逐厅「必重做」模型清单完成（每厅 ≥2 件重做而非调参）
- [ ] 纹理尺寸遵守 §2.1 上限表（源码审计）
- [ ] 七厅截屏证据留档并逐张复核（冒烟 SV_SHOT_DIR）

**验证方式**：截屏复核 + 源码审读 kit.js/各厅 + `npm run smoke`。

### 22. 性能预算硬断言保持（v1.3 复核）
- [ ] `--smoke` 逐厅 meshes ≤260 / tris ≤400,000 断言保持且全绿（精修加量后仍在预算内）
- [ ] v1.3.0 实测表更新到本文件
- [ ] 低画质档强化：关 Bloom + 像素比 1.0 + 颗粒/色差减半，Q 键即时切换

**验证方式**：`xvfb-run npx electron . --smoke` 输出逐厅统计且退出码 0。

### 23. 音景 / 配乐抛光（v1.3 新增）
- [ ] 新交互专属合成音 ≥10 种（点唱机/收银机/倒酒/风扇/百叶/打字机/断路器/齿轮/水花/铃…）
- [ ] 位置化音效通道 `sfxAt(name, position)`（PannerNode），听者随相机每帧更新
- [ ] 大厅留声机可独立点播/停止爵士层；配乐音量不盖过留白静默（duck 连动）
- [ ] 零采样策略保持（合规扫描全绿）

**验证方式**：`npm test`（audio 扩展）+ 冒烟交互触发链不抛错 + 人工耳机巡检。

### 24. Release 1.3.0（v1.3 新增）
- [ ] `npm test` / `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
- [ ] `release/SmokeVelvet-LynchArchive-Portable-1.3.0.exe` + `Setup-1.3.0.exe`
- [ ] `SHA256SUMS.txt` 更新；PE 结构校验；文档（README/BUILD/TESTING/本文件）同步 1.3.0

---

## 补充说明：关于「PS5 级别」
本项目将其落地为门禁 17 的**可勾选清单**（几何圆角化/多部件、程序化 PBR 纹理分层、体积光与可控闪烁、逐厅调色后处理、预算内实测），即 **高完成度风格化实时渲染**。以 WebGL 合理复杂度实现电影氛围，不虚假承诺主机独占级光追画质；交互探索性为第一优先。

---

## 最终自检结果（v1.2.0）

| # | 门禁 | 状态 | 备注 |
|---|------|------|------|
| 1 | 美术 | ✅ | 7 场景全程序化，统一色板/字体，截屏复核 |
| 2 | 画面 | ✅ | 雾/动态光/粒子/后处理链（含冲击通道）全配 |
| 3 | 性能 | ✅ | 每厅 ≤155 网格 / ≤11.2 万三角，预算断言入冒烟 |
| 4 | 音效 | ✅ | 7 配方底噪 + 27 种合成音效 + duck，零采样 |
| 5 | 语音旁白 | ✅ | 8 段原创旁白（每段 ≤24 字），四档模式 |
| 6 | 社交 | ✅ | 发帖/点赞/回复 + localStorage + 3D 留言板 |
| 7 | 版权合规 | ✅ | 合规页 + README 免责 + 短引语审计 + 自动扫描 |
| 8 | 测试 | ✅ | 42 单测 + 构建冒烟 + 运行时七厅巡检全绿 |
| 9 | Release | ✅ | 两个 Windows exe（1.2.0）+ 校验和 + BUILD.md |
| 10 | 旁白重做 | ✅ | 字母显现默认 / 爵士层 / 戏剧化 TTS / 四档循环 |
| 11 | 每厅彩蛋 | ✅ | 7 厅 7 彩蛋，穆赫兰道惊吓可复现并入冒烟 |
| 12 | 林奇的房间 | ✅ | 9 项交互 + 叙事链 + 冥想深水序列 + 读书角 |
| 13 | 文案审计 | ✅ | 标签清退 + 12 条原话引语 + 理论名词清零 |
| 14 | 重新打包 | ✅ | 1.2.0 portable + setup + SHA256SUMS |
| 15 | 留白文案 | ✅ | 文章体系移除 / 旁白 ≤24 字 / 每厅展签 1 块 / 字幕 ≤26 字（源码级单测） |
| 16 | 多分区地图 | ✅ | 双峰 5 分区小径互连；六厅均加可逛深度 |
| 17 | 资产精度 | ✅ | 圆角/车削/合并多部件 + 程序化 PBR 纹理 + 体积光 + 截屏复核 |
| 18 | 预算入冒烟 | ✅ | meshes≤260 / tris≤40 万 硬断言，实测最高 155 / 11.2 万 |
