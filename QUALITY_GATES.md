# QUALITY GATES — 大卫·林奇数字纪念展览馆

> 本文件为交付前必须全部通过的质量门禁。每一项在交付前对照自检，
> 通过后勾选并注明验证方式。未全绿不得宣称交付。

## 门禁清单

### 1. 美术 — 统一视觉语言
- [x] 统一色板：深黑 `#0a0608` / 天鹅绒红 `#8f0e1e` / 霓虹蓝 `#3ec5ff` / 荧光粉 `#ff2e88` / 象牙白 `#f2e9dc`（`src/style.css` CSS 变量 + `src/halls/kit.js` PALETTE 常量，全站引用）
- [x] 统一字体语言：衬线展签体（Georgia/宋体，标题与字幕）+ 等宽档案体（Courier，正文/UI），全站一致
- [x] 禁止 AI 默认紫白渐变与模板脸 —— 全部界面为定制暗色档案馆风格；穆赫兰道夜空曾出现紫渐变，已改为洛杉矶式暗橙光污染渐变（commit "visual grading tweaks"）
- [x] 至少 3 个可进入的作品展厅 + 1 个总览大厅（**实际：4 作品展厅 + 档案长廊 + 大厅 = 6 个场景**）
- [x] 所有场景为原创程序化生成（Canvas 绘制纹理 + 代码构建几何；`tests/compliance.test.js` 扫描确认仓库零媒体素材文件）

**验证方式**：`--smoke` 截屏六厅逐一目视复核（红帷幕/折线地板/霓虹/单色工业房/松林月夜/夜路剧场均符合设定）。

### 2. 画面 — 氛围与响应式
- [x] 每厅 `FogExp2` 雾 + 漂移烟雾粒子层（`kit.smokeLayer`）
- [x] 动态光源每厅 ≥2：大厅吊灯颤动×6、档案廊荧光管×7、橡皮头铁笼灯×3+炉光、蓝丝绒桌灯×6+熄灯交互、双峰帷幕光+神秘路灯、穆赫兰道坏路灯+霓虹
- [x] 粒子每厅 ≥1 套：光尘/地雾/萤火/蒸汽/烟雾（`dustField`/`smokeLayer`）
- [x] 全屏后处理链：UnrealBloom + 自研 LynchPass（胶片颗粒/暗角/扫描线/径向色差/亮度颤动/饱和度分级/梦境反色）
- [x] 桌面与窄屏均可用：375×667 窄屏截图验证（底部按钮栏换行、字幕/厅名重排）；触屏虚拟摇杆 + 右半屏环视 + 「互动」按钮

**验证方式**：`SV_SHOT_DIR=… SV_WIN_SIZE=375x667 xvfb-run npx electron . --smoke` 截屏复核。

### 3. 性能
- [x] 场景复杂度实测（`--smoke` 输出）：每厅网格 26–99 个（≈draw call 上限，预算 <120）、三角面 1.5k–33k（预算 <40 万），大幅低于预算
- [x] 像素比钳制 ≤1.5，「画质 低」档关闭 Bloom + 像素比 1.0（`Q` 键即时切换）
- [x] 无严重卡死：展厅切换黑幕过场 + 异步 `import()`；运行时冒烟六厅连续切换无异常
- [x] 资源懒加载：6 个展厅独立分包（构建产物 `lobby-*.js` 等 6 chunk，`npm run smoke` 校验 ≥5）

**验证方式**：`npm run smoke`（分包校验）+ `--smoke` 场景统计输出 + 内置 FPS 计数（`F` 键）。
注：本 CI 环境仅有软件渲染无法测 GPU 帧率；以上复杂度在近十年任意独显/核显上 30fps+ 为合理结论，真机清单见 TESTING.md。

### 4. 音效 — 全程序化合成
- [x] 每厅独立底噪配方（`src/audio/drones.js`：振荡器+噪声+滤波+LFO；橡皮头方波机器嗡鸣、双峰风声、蓝丝绒和声垫+唱片炒豆、穆赫兰道暗涌等）；单测校验每厅含 <120Hz 低频成分
- [x] 交互音 >2 类：悬停嗡音/点击电脉冲/传送门呼啸/UI 翻页/蒸汽/金属撞击/猫头鹰/翻涌/心跳钟声/啜饮 等 14 种，全部 WebAudio 实时合成
- [x] 无任何采样素材：合规测试扫描 src/electron 零音频文件
- [x] 全局静音：`M` 键 + UI 按钮（增益 ramp 平滑切换）

**验证方式**：`npm test`（audio.test.js 参数域校验）+ 手动耳机巡检清单（TESTING.md）。

### 5. 语音 / 旁白
- [x] 原创旁白 7 段（欢迎词 + 六厅导览，`src/data/essays.js` NARRATIONS）
- [x] Web Speech API 朗读（中文语音优先），不可用环境自动降级为字幕；字幕恒显（无障碍）
- [x] 可开关：`V` 键 + UI 按钮，关闭即 cancel

**验证方式**：进厅首访自动旁白；compliance 测试校验旁白文本非空原创。

### 6. 社交 — 讨论区
- [x] 留言墙完整闭环：发帖（空内容拒绝/超长截断/匿名回落）、列表（倒序）、点赞（可撤销）、回复
- [x] `localStorage` 持久化（重建 store 后数据保留，单测覆盖；损坏 JSON 安全恢复）
- [x] XSS 防护：渲染层全部 `textContent` 注入 + `escapeHtml` 双保险（单测覆盖注入用例）

**验证方式**：`npm test`（guestbook.test.js 8 用例）+ `--smoke` 运行时发帖/点赞/回复闭环校验。

### 7. 版权合规
- [x] 应用内「版权合规」页（`C` 键）：明示非官方/非授权/独立粉丝纪念展/内容负面清单与正面清单
- [x] README 顶部免责声明
- [x] 内容自查：无截图/海报/原声采样/对白 verbatim/角色肖像/商标 Logo；全部视觉声音程序化生成（自动扫描 + 人工审读 `src/data/*.js` 全部文案）
- [x] 事实性内容仅公开事实（片名/年份/奖项），评述均为原创中文文案

**验证方式**：`npm test`（compliance.test.js）+ 人工全文审读。

### 8. 测试
- [x] `npm test` 通过：**24 用例全绿**（数据完整性/留言 store/音频配方/合规扫描）
- [x] `npm run smoke` 通过：生产构建 + 产物结构 + Electron 语法 + 打包配置 + 素材扫描
- [x] 运行时冒烟通过：`xvfb-run npx electron . --no-sandbox --smoke` — 启动→自动进入→六厅装载→UI 全面板交互→留言闭环，全绿退出码 0
- [x] `TESTING.md` 手动清单齐备

### 9. Release — Windows 产物
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.0.0.exe`（95.3 MiB，便携版，双击即运行）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.0.0.exe`（95.5 MiB，NSIS 一键安装包）
- [x] `release/SHA256SUMS.txt` 校验和
- [x] `BUILD.md` 一键构建说明（含 Linux 交叉打包 wine32 依赖坑位记录）
- [x] 产物验证：PE 结构校验（NSIS 自解压 + 内部 x86-64 PE）；wine 下自解压与主进程启动成功（GPU 初始化崩溃为 wine 已知限制，同一 dist 已由原生 Electron 运行时冒烟全绿）

## 补充说明：关于「PS5 级别」
本项目将其定义为 **高完成度风格化实时渲染目标**：PBR 材质、全屏后处理链（Bloom/颗粒/色差/暗角）、体积氛围、流畅第一人称探索。以 WebGL 合理复杂度实现电影氛围，不虚假承诺主机独占级光追画质；交互探索性为第一优先。

---

## 最终自检结果

| # | 门禁 | 状态 | 备注 |
|---|------|------|------|
| 1 | 美术 | ✅ | 6 场景全程序化，统一色板/字体，截屏复核 |
| 2 | 画面 | ✅ | 雾/动态光/粒子/后处理链全配，375px 窄屏验证 |
| 3 | 性能 | ✅ | 每厅 ≤99 网格 / ≤3.3 万三角，6 分包懒加载 |
| 4 | 音效 | ✅ | 6 配方底噪 + 14 种合成音效，零采样 |
| 5 | 语音旁白 | ✅ | 7 段原创旁白，TTS+字幕降级，可开关 |
| 6 | 社交 | ✅ | 发帖/点赞/回复 + localStorage，XSS 安全 |
| 7 | 版权合规 | ✅ | 应用内合规页 + README 免责 + 自动扫描 |
| 8 | 测试 | ✅ | 24 单测 + 构建冒烟 + 运行时六厅巡检全绿 |
| 9 | Release | ✅ | 两个 Windows exe + 校验和 + BUILD.md |
