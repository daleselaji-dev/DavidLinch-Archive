# QUALITY GATES — 大卫·林奇数字纪念展览馆

> 本文件为交付前必须全部通过的质量门禁。每一项在交付前对照自检，
> 通过后勾选并注明验证方式。未全绿不得宣称交付。
> v1.1.0 新增门禁 10–14（旁白重做 / 彩蛋 / 林奇的房间 / 文案审计 / 重新打包）。
> v1.2.0 新增门禁 15–18（留白文案 / 多分区可逛地图 / 资产精度 / 性能预算入冒烟）。
> **v1.3.0 新增门禁 19–24（讲解 v1.0 克制+零叙事剧透 / 交互密度 / PS4-tier 材质模型清单 /
> 性能预算保持 / 音景抛光 / 1.3.0 重新打包）。**
> **v1.4.0 新增门禁 25–31（讲解配额保持 / PS5-tier 画质 P1–P10 / 分厅重做清单 /
> 交互 ≥105+连锁 ≥2 / 音景 v1.4 / 预算 240·240k·40 / 1.4.0 重新打包）。**
> **v1.5.0 新增门禁 32–35（非真人配音 / show-don't-tell 立牌 / 大厅焦点+全馆减法 /
> 1.5.0 重新打包）。**
> **v1.6.0 新增门禁 36–38（蓝丝绒氛围收紧 / 穆赫兰道后巷通路修通+惊吓可复现 /
> 1.6.0 重新打包）。**
> **v1.7.0 新增门禁 39–43（大厅中央焦点完整独石 / 穆赫兰道转身惊吓 /
> 讲解员导览+立牌只留名言 / 彩蛋与交互密度 / 1.7.0 重新打包）。未全绿不得交付。**

## 门禁清单

### 1. 美术 — 统一视觉语言
- [x] 统一色板：深黑 `#0a0608` / 天鹅绒红 `#8f0e1e` / 霓虹蓝 `#3ec5ff` / 荧光粉 `#ff2e88` / 象牙白 `#f2e9dc`（`src/style.css` CSS 变量 + `src/halls/kit.js` PALETTE 常量，全站引用）
- [x] 统一字体语言：衬线展签体（Georgia/宋体，标题与字幕）+ 等宽档案体（Courier，正文/UI），全站一致
- [x] 禁止 AI 默认紫白渐变与模板脸 —— 全部界面为定制暗色档案馆风格
- [x] 至少 3 个可进入的作品展厅 + 1 个总览大厅（**实际：4 作品展厅 + 档案长廊 + 林奇的房间 + 大厅 = 7 个场景**）
- [x] 所有场景为原创程序化生成（Canvas 绘制纹理 + 代码构建几何；`tests/compliance.test.js` 扫描确认仓库零媒体素材文件）

**验证方式**：`SV_SHOT_DIR=… --smoke` 截屏七厅逐一目视复核（红帷幕/折线地板/霓虹/单色工业房/暖木工作室/松林月夜/夜路剧场均符合设定）。

### 2. 画面 — 氛围与响应式
- [x] 每厅 `FogExp2` 雾 + 漂移烟雾粒子层（`kit.smokeLayer`）
- [x] 动态光源每厅 ≥2：大厅吊灯颤动×6、档案廊荧光管×7+壁龛射灯、橡皮头铁笼灯×3+锅炉炉光、蓝丝绒桌灯×6+吧台灯带、双峰帷幕光+镇街路灯闪烁+红房间落地灯、穆赫兰道坏路灯+霓虹跑马灯+巷灯、林奇的房间台灯/顶灯/烛光/窗月光（多数可交互开关）
- [x] 粒子每厅 ≥1 套：光尘/地雾/萤火/蒸汽/烟雾/香烟细烟/瀑布水雾/深水鱼群（`dustField`/`smokeLayer`/冥想序列）
- [x] 全屏后处理链：UnrealBloom + 自研 LynchPass（胶片颗粒/暗角/扫描线/径向色差/亮度颤动/饱和度分级/梦境反色/**惊吓冲击 uShock/闪光帧 uFlash**）
- [x] 桌面与窄屏均可用：375×667 窄屏适配（底部按钮栏换行、字幕/厅名/字母旁白重排）；触屏虚拟摇杆 + 右半屏环视 + 「互动」按钮

**验证方式**：`SV_SHOT_DIR=… xvfb-run npx electron . --smoke` 截屏复核；窄屏 `SV_WIN_SIZE=375x667` 同法验证。

### 3. 性能
- [x] 场景复杂度实测（`--smoke` 输出，v1.2.0）：每厅网格 68–155 个（≈draw call 上限，预算 ≤260）、三角面 1.2 万–11.2 万（预算 ≤40 万），全部在预算内
- [x] **预算为冒烟硬门禁**：`electron --smoke` 每厅统计后断言 `meshes≤260 && tris≤400000`，超标即非零退出（见门禁 18）
- [x] 像素比钳制 ≤1.5，「画质 低」档关闭 Bloom + 像素比 1.0（`Q` 键即时切换）
- [x] 无严重卡死：展厅切换黑幕过场 + 异步 `import()`；运行时冒烟七厅连续切换无异常
- [x] 资源懒加载：7 个展厅独立分包（构建产物 `lobby-*.js` … `studio-*.js` 共 7 chunk，`npm run smoke` 校验 ≥6 且含 studio）

**验证方式**：`npm run smoke`（分包校验）+ `--smoke` 场景统计输出 + 内置 FPS 计数（`F` 键）。
注：本 CI 环境仅有软件渲染无法测 GPU 帧率；以上复杂度在近十年任意独显/核显上 30fps+ 为合理结论，真机清单见 TESTING.md。

### 4. 音效 — 全程序化合成
- [x] 每厅独立底噪配方（`src/audio/drones.js`：振荡器+噪声+滤波+LFO，含新厅 studio 台灯电流暖嗡）；单测校验每厅含 <120Hz 低频成分
- [x] 交互音 >2 类：悬停/点击/呼啸/翻页/蒸汽/金属/猫头鹰/心跳/惊吓噪墙/窃语/呼吸/收音机/火柴/开关灯/布幔/冥想钟/摇篮曲短句 等 27 种，全部 WebAudio 实时合成
- [x] 无任何采样素材：合规测试扫描 src/electron 零音频文件
- [x] 全局静音：`M` 键 + UI 按钮（增益 ramp 平滑切换）；`duck()` 突兀静默/闪避机制

**验证方式**：`npm test`（audio.test.js + jazz.test.js 参数域校验）+ 手动耳机巡检清单（TESTING.md）。

### 5. 语音 / 旁白
- [x] 原创旁白 8 段（欢迎词 + 七厅各一句，`src/data/essays.js` NARRATIONS）——**v1.2 起每段 ≤24 字符**，进厅延迟 2.6 秒后低声出现，仅首访一次
- [x] 三种体验模式 + 关（见门禁 10）；语音档为戏剧化 TTS（分句停顿/变速/耳语拍），不可用环境自动只保留字母显现（无障碍）
- [x] 可切换：`V` 键 + UI 按钮循环四档

**验证方式**：`npm test`（旁白预算断言）+ `--smoke` 校验模式循环与字母显现激活。

### 6. 社交 — 讨论区
- [x] 留言墙完整闭环：发帖（空内容拒绝/超长截断/匿名回落）、列表（倒序）、点赞（可撤销）、回复
- [x] `localStorage` 持久化（重建 store 后数据保留，单测覆盖；损坏 JSON 安全恢复）
- [x] XSS 防护：渲染层全部 `textContent` 注入 + `escapeHtml` 双保险（单测覆盖注入用例）
- [x] 林奇的房间内有 3D 软木留言板，把最新留言渲染为图钉便签（Canvas 程序绘制），点击直达留言墙

**验证方式**：`npm test`（guestbook.test.js 8 用例）+ `--smoke` 运行时发帖/点赞/回复闭环校验。

### 7. 版权合规
- [x] 应用内「版权合规」页（`C` 键）：明示非官方/非授权/独立粉丝纪念展/内容负面清单与正面清单/短引语合理使用说明
- [x] README 顶部免责声明
- [x] 内容自查：无截图/海报/原声采样/影视对白 verbatim/角色肖像/商标 Logo；全部视觉声音程序化生成（自动扫描 + 人工审读 `src/data/*.js` 全部文案）
- [x] 林奇本人语句仅取公开访谈/著作短引语（单测强制单条 ≤200 字符），注明出处类型；彩蛋与致敬全部为原创抽象重构，无精确镜头复刻（双峰各分区均为几何抽象致敬，无角色肖像/原声）

**验证方式**：`npm test`（compliance.test.js + narration.test.js 引语长度审计）+ 人工全文审读。

### 8. 测试
- [x] `npm test` 通过：**42 用例全绿**（数据完整性/留言 store/音频配方/爵士配方/旁白预算/文案审计/合规扫描）
- [x] `npm run smoke` 通过：生产构建 + 产物结构（7 分包）+ Electron 语法 + 打包配置 + 素材扫描
- [x] 运行时冒烟通过：`xvfb-run npx electron . --smoke` — 启动→自动进入→七厅装载→**场景预算断言**→**每厅彩蛋强制触发**→UI 全面板交互→旁白模式循环→留言闭环，全绿退出码 0
- [x] `TESTING.md` 手动清单齐备（含彩蛋复现步骤与新分区巡检）

### 9. Release — Windows 产物
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.2.0.exe`（便携版，双击即运行）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.2.0.exe`（NSIS 一键安装包）
- [x] `release/SHA256SUMS.txt` 校验和（v1.2.0）
- [x] `BUILD.md` 一键构建说明（含 Linux 交叉打包 wine 依赖坑位记录）
- [x] 产物验证：PE 结构校验；同一 dist 由原生 Electron 运行时冒烟全绿

### 10. 旁白体验重做（v1.1 新增）
- [x] **字母显现模式可用且为默认**：旁白逐字打出（打字机音/故障闪现错误字形/句读长停/随机静默拍），无语音也可完整传达（`src/ui/letters.js`）
- [x] **爵士氛围层可用**：程序化「深夜爵士」（63BPM 摇摆刷镲 + 行走贝斯 + 钢琴式和声 + 偶发弱音铜管 + 黑胶炒豆，全 WebAudio 合成，零真实录音；`src/audio/jazz.js`），与各厅林奇低频底噪混音，随模式开关
- [x] **语音档升级为戏剧化 TTS**：分句停顿、变速（0.68–0.92）、随机耳语拍（低音量低语速），拒绝干读
- [x] `V` 键循环 字母显现 → 爵士+字母 → 语音+字母 → 关

**验证方式**：`npm test`（narration.test.js 模式体系 + jazz.test.js 配方）+ `--smoke` 运行时模式循环/爵士层启停/字母激活断言。

### 11. 每厅彩蛋 ≥1，穆赫兰道惊吓可复现（v1.1 新增）
- [x] 大厅「帷幕后的窃语」/ 档案廊「不在年表上的灯牌」（两段式）/ 橡皮头「暖气炉里的小舞台」/ 蓝丝绒「衣柜的暗侧」/ 双峰「环形石阵·空间错位」（v1.2 移至林中岔路尽头）/ 林奇的房间「收音机自己醒来」/ 穆赫兰道「剧场背后的那个东西」
- [x] 全部为**绕过去才触发**的区域触发器（`kit.zoneTrigger`），不在导览图上，无 UI 提示
- [x] **穆赫兰道惊吓彩蛋**：绕到剧场背后空地 → 声音抽真空 → 心跳两记 → 黑影 0.26 秒扑至面前（高频痉挛）+ 合成噪墙 + 后处理冲击（色差暴涨/颗粒风暴/暗角收缩/暗红闪帧）→ 黑幕 → **空间错位**：醒来已被移回巷口。复现步骤已写入 TESTING.md
- [x] 彩蛋触发链纳入运行时冒烟（`window.__SV__.triggerEggs()` 每厅强制引爆，异常即非零退出）

**验证方式**：`--smoke` 逐厅输出 `彩蛋触发 <hall>: <name>`；手动按 TESTING.md 步骤复现。

### 12. 「林奇的个人世界」区域（v1.1 新增，v1.2 精修）
- [x] 独立展厅「HIS ROOM · 林奇的房间」可从大厅进入（第 6 扇门）
- [x] 可操作交互 ≥3（**实际 9 项**）：台灯开关 / 墙上顶灯开关 / 喝咖啡 / 点燃与掐灭香烟 / 收音机天气播报（原创文本 3 则轮播）/ 拉帘（开合决定冥想角可达性）/ 冥想深水序列（画面分级切换 + 大鱼群环游 + 冥想钟）/ 画架自绘 / 软木留言板
- [x] 小叙事链：60 秒内完成 咖啡→香烟→收音机 → 触发特别播报 + 墙上「大鱼」暗画亮起
- [x] v1.2 加可逛深度：读书角（扶手椅+程序化书架）、百叶窗夜光、编织圆毯、两幅程序化抽象画
- [x] 全部原创抽象致敬（咖啡/香烟/冥想/天气播报意象），不使用肖像与商标

**验证方式**：`--smoke` studio 厅装载 + 彩蛋触发；手动清单见 TESTING.md。

### 13. 文案审计（v1.1 新增；v1.2 由门禁 15 收紧取代上限）
- [x] 二手空标签清退：全站不再出现「超现实主义大师」「梦核」等标签堆砌（单测强制断言）
- [x] 林奇原话短引语为唯一「解读」：QUOTES 库 12 条（访谈/著作，中英对照+出处类型），每厅 1 块 3D 引语展签（`kit.quotePlaque`）
- [x] v1.2：策展文章体系整体移除，理论名词（拉康/齐泽克/精神分析）全站清零（单测断言不含）
- [x] 引语合理使用：单条 ≤200 字符 + 非商业粉丝展语境声明（合规页第 4 段）

**验证方式**：`npm test`（narration.test.js 文案审计组）+ 人工审读 `src/data/essays.js`。

### 14. 重新打包 Release
- [x] `npm test` 42 用例全绿
- [x] `npm run smoke` 构建冒烟全绿（7 分包）
- [x] 运行时 `--smoke` 全绿（七厅 + 预算 + 彩蛋 + 旁白模式 + UI + 留言闭环）
- [x] portable + setup 两个 exe 重新打包为 **1.2.0**，`SHA256SUMS.txt` 同步更新

### 15. 留白文案 —— 解释不喧宾夺主（v1.2 新增）
- [x] **策展文章体系移除**：`ESSAYS` 不再导出（单测断言 `undefined`）；档案页仅保留「他自己的话」原话摘录墙（`ui.showQuotes()`，12 条中英对照短引语 + 出处）
- [x] **旁白极简**：每厅一句 ≤24 字符（实测 7–16 字），全馆旁白总字数 ≤160（实测约 100）；仅首访触发、进厅 2.6 秒后才低声出现；禁用说教/总结腔词汇（「告诉我们/象征着/正如」等，单测断言）
- [x] **展签预算**：每厅可见文字展签（quotePlaque）≤2 —— 实际每厅恰好 1 块，只印林奇原话；说明立牌（standPlaque）全馆退场（源码级单测断言）
- [x] **字幕短句化**：厅内交互字幕字面量 ≤26 字符（源码级单测扫描全部 `ui.caption` 调用）
- [x] 合规页保留必要事实陈述，删除说教式结语；帮助/关于页文案精简
- [x] 气氛交给空间、光、声与静默：无强制弹窗式解读，一切面板均为主动打开

**验证方式**：`npm test` narration.test.js「留白预算」「展签预算」两组源码级审计；人工通读 `src/data/essays.js` 与七厅字幕。

### 16. 多分区可逛地图 —— 显著高于「单房间」（v1.2 新增）
- [x] **双峰厅 ≥3 个可走分区（实际 5 个）**，由林间小径连接为一张可逛地图（`zonesBounds` 多边界 union）：
  - ① 林间空地：红帷幕之门 + 树桩咖啡
  - ② 红房间氛围区：环形折线地板 + 红帷幕围合（留缺口接小径）+ 对坐扶手椅 + 落地灯（几何抽象致敬，无角色复制）
  - ③ 小镇夜街：沥青路+车道线、人行道、闪烁路灯、老轿车剪影（无品牌）、**可进入的 DINER**（折线地地板/木墙/柜台/圆凳/玻璃罩樱桃派/咖啡壶）
  - ④ 瀑布眺望台：木栈台+栏杆、程序化流动瀑布、跌水潭水雾、锯木厂烟囱剪影
  - ⑤ 林中岔路：环形石阵彩蛋
- [x] 其余厅同步加可逛分区与装置：档案廊**原话壁龛**、橡皮头**锅炉房**（格栅步道+铆钉锅炉+压力表+炉光）、蓝丝绒**吧台区**（背光酒瓶+黄铜脚踏+高脚凳）、穆赫兰道夜路+暗巷+后场（v1.1 已有，v1.2 加护栏/跑马灯精修）、大厅柱环与穹顶线脚、林奇的房间读书角
- [x] 树木等障碍物避让所有可走区域（`insideWalkable` 采样）；分区边界用 `multiRectBounds`/`zonesBounds` 保证不穿墙不出界

**验证方式**：`--smoke` 七厅装载 + 截屏目视复核各分区；手动按 TESTING.md 巡检路线走通全部分区。

### 17. 资产精度 —— 「PS5-tier stylized realtime」可验证清单（v1.2 新增）
- [x] **几何**：关键装置不再是裸 Box ——
  - 圆角化：`RoundedBoxGeometry`（桌板/舞台/展柜/招牌/家具，`kit.roundedBoxGeo/roundedBoxMesh`）
  - 车削/放样：话筒底座、吊灯灯罩（LatheGeometry）；柱式（柱础/柱身凹槽/柱头，`kit.column`）
  - 多部件组装+合并：门廊=圆柱+黄铜环+檐口+踏步+防滑条；扶手椅=坐垫/靠背/扶手/车削腿（`kit.armchair`）；栏杆=立柱+横杆合并（`kit.railing`）
  - 帘幕三层褶皱（三阶正弦位移 + 底摆外扩 hem）+ 幕帷 valance + 黄铜横杆（`kit.curtainWithValance`）
  - 地面接缝/镶边：大厅金圈镶边、档案廊红毯+护墙线脚、橡皮头伸缩缝、夜街人行道与车道线
  - 有机形体：位移二十面体岩石（`kit.rockMesh`）、异形显示避免尖锐穿插
- [x] **材质**：Canvas 程序纹理细节升级（anisotropy 8）——
  - 木纹（年轮细线+木结+板缝，`woodTexture`）、金属拉丝（定向细划痕，`brushedMetalTexture`）、织物织纹（经纬+起球噪点，`weaveTexture`）、污迹/磨损叠加（`grime`）
  - PBR 分层：MeshPhysicalMaterial 天鹅绒（sheen+粗糙度贴图+凹凸）、玻璃（transmission 感的高光+clearcoat）、皮革坐垫（sheen+clearcoat）；roughness/metalness 随贴图变化，无塑料感大色块
- [x] **光照**：每厅关键光有体积感/边缘光/可控闪烁 —— 体积光锥（大厅穹顶射灯/舞台/剧场）、路灯故障闪烁、荧光管衰减闪烁、烛光/炉光噪声抖动、窗缝月光、吧台灯带
- [x] **后处理**：LynchPass + Bloom 按厅调参（`meta.look`：曝光/泛光/饱和/色调/雾密度逐厅设定），电影感不过曝；`Q` 低画质档关 Bloom 降像素比保帧率
- [x] **性能预算仍守住**：上述升级后实测每厅 68–155 网格 / 1.2–11.2 万三角面，仍在预算内（见门禁 3、18）

**验证方式**：`SV_SHOT_DIR=… --smoke` 七厅截屏目视复核（无裸大方块观感）+ `--smoke` 场景统计在预算内 + 代码审读 `src/halls/kit.js`。

### 18. 性能预算入冒烟门禁（v1.2 新增）
- [x] `electron --smoke` 每厅装载后遍历场景统计 meshes / tris，**超过预算（meshes>260 或 tris>400000）立即非零退出**
- [x] v1.2.0 实测（软件渲染冒烟输出）：

  | 厅 | meshes | tris |
  |----|--------|------|
  | lobby | 155 | 111,805 |
  | archive | 68 | 15,492 |
  | eraserhead | 85 | 21,221 |
  | bluevelvet | 89 | 40,071 |
  | studio | 76 | 12,425 |
  | twinpeaks | 105 | 71,185 |
  | mulholland | 101 | 40,627 |

- [x] 纹理预算：Canvas 纹理单张 ≤1024²（多数 256²/512²），每厅纹理总量受程序生成上限约束；像素比钳制 ≤1.5
- [x] 懒加载保留：七厅独立 chunk，切换时旧厅 `disposeGroup` 全量释放

**验证方式**：`xvfb-run npx electron . --smoke` 输出逐厅统计且退出码 0。

---

### 19. 讲解回退 v1.0 克制 + 零原作叙事剧透（v1.3 新增）
- [x] 旁白配额：全馆 ≤8 条（welcome+7 厅），每条 ≤16 字，总字数 ≤110（实测 8 条 / 最长 16 字 / 总 84 字）；措辞为
      风格化空间提示（光/质感/空间感受）或公开事实，禁止剧情叙述（单测断言）
- [x] 展签配额：每厅林奇原话展签（quotePlaque）≤1（实测恰 1 块）；事实铭牌仅「片名+年份」一行式（档案廊年表灯牌）
- [x] 交互字幕 ≤22 字（源码级扫描收紧，narration.test.js），只写眼前物性/感受
- [x] 叙事剧透禁词清零（源码级扫描）：原作对白引用（含 "he's the one"/"就是他"）、
      角色名、叙事连接词（"后来/然后他/接着他"）等全站不出现
- [x] 现存违规文案已清除：穆赫兰道粉笔字对白 → 原创螺旋图形；winkies → backlot；电台长句缩短

**验证方式**：`npm test` narration.test.js「v1.3 讲解配额」「叙事禁词」组全绿 + 人工通读。✅ 126 用例全绿。

### 20. 交互密度 —— 可玩的数字艺术装置（v1.3 新增）
- [x] 每厅非导航可交互物 ≥8（门/传送门 nav 标记不计入），双峰 ≥10、林奇的房间 ≥10
      —— **实测（终版）：大厅 10 / 档案 19 / 橡皮头 12 / 蓝丝绒 11 / 房间 14 / 双峰 13 / 穆赫兰道 11，共 90**
- [x] 交互类型覆盖：开关（调光/闸刀/排灯/放映机/衣镜追逐灯/台灯拉链）/ 拉动（汽笛链/幕绳/吊扇拉链/窗百叶）/
      开启（炉门/电话亭折门）/ 播放（留声机/点唱机/唱机+黑胶底噪）/ 坐下触发（剧场折座/diner 卡座）/
      可检视物件（派罩/胶片/搁着的烟/地漏/红房间没主的咖啡）/ 驻留触发（眺望台守望：站定 8s 水雾应答）/
      连锁反应（迎宾铃→六门齐亮、汽笛→蒸汽+机器猛冲、幕绳→后幕寒颤+脚灯、折座→舞台灯链、咖啡→烟→电台特别播报、
      漏汽接头→安静五秒→整段猛喷、放映机→双盘转+镜头亮+墙上空白画格、
      **点唱机→脚灯洗亮+聚光呼吸+后幕轻颤+吧台吊灯收暗（歌厅暗场）**）
- [x] 每次交互 ≥2 通道反馈（声 + 光/动画/字幕短句）——`activateAll()` 巡检逐厅全量激活验证不抛错
- [x] `__SV__.countInteractives()` 钩子返回非导航计数；运行时冒烟逐厅断言阈值**锁定在终版普查 -1**
      （9/18/11/10/13/12/10），不达标非零退出——防止后续改动无声回退交互密度

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅输出交互统计且全绿；TESTING.md 手动清单。✅ 全绿。

### 21. PS4-tier 材质/模型可勾选清单（v1.3 新增）
- [x] PRODUCTION_PLAN §1.1 G1–G9 全部勾选（分辨率策略/法线细节/PBR 分层/各向异性织物/
      磨损叙事/湿润反射/光照语言/逐厅调色/降级档）
- [x] 法线贴图管线落地：`normalFromHeight()` Sobel 推导，八类三通道纹理组（木/砖/拉丝金属/织物/
      水泥/沥青/折线地板/皮革），seeded RNG 保证 albedo/height/roughness 三图对齐
- [x] 拉丝金属使用 `MeshPhysicalMaterial.anisotropy` 方向性高光（brassMat/chromeMat）
- [x] PRODUCTION_PLAN §3 逐厅「必重做」模型清单完成（预制体库 35 件：吊灯/点唱机/轿车/剧场排椅/
      电话亭/炉门/留声机/纪念碑…全部车削/贝塞尔管/部件层级化）；艺术二遍再重做：蓝丝绒桌灯、
      档案长椅、阀杆铸座、垃圾箱、diner 卡座+踏脚杆、锅炉鞍座+灰坑+铆接蒙皮、房间圆毯、立式烟灰缸；
      **艺术三遍再重做**：装饰派俱乐部椅（部分车削环抱背，红房间×2+读书角）、16mm 放映机展台、
      蓝丝绒酒瓶墙（三剖面×三色玻璃）、剧场台口拱架（条纹壁柱+楣梁+鎏金沿）、瀑布三段活水分层、
      暗巷瓦楞铁皮墙、漏汽接头补丁、大厅顶冠洗光、吧台壁挂电话、房间拼板木顶+明椽、
      diner 街立面（自发光分格亮窗+窗台/滴水檐+百叶暗窗）、橡皮头铸铁地漏+油渍贴花、
      后台衣镜（框边 A/B 追逐灯泡）、栏杆语言分家（overlookRail 栈道木作 / pipeRail 工业管件）、
      大厅大理石镶边、蓝丝绒衣柜家具化（檐/座/缝/芯板/旋钮）、画架工作件（托板/抹布/笔筒）、
      档案阅览桌+银行家台灯（bankersLamp）、房间可调百叶+夜雨玻璃、吧台吊坠灯×3、红房间独脚圆桌+瓷杯
- [x] 纹理尺寸遵守 §2.1 上限表（kit.test.js 源码审计断言）
- [x] 七厅截屏证据留档并逐张复核（冒烟 SV_SHOT_DIR 全厅 + 内景机位：diner/红房间/剧场/锅炉房/
      吧台角/壁龛/烟灰缸/圆毯，共 20+ 机位）

**验证方式**：截屏复核 + 源码审读 kit.js/props.js/各厅 + `npm run smoke`。✅ 全绿。

### 22. 性能预算硬断言保持（v1.3 复核 + 收紧）
- [x] `--smoke` 预算断言**收紧**至 meshes ≤220 / tris ≤200,000 / **动态光 ≤40（新增）**，全绿
- [x] v1.3.0 实测表（软件渲染冒烟输出，2026-08-25 23:1x 终版全巡检，含艺术三遍全部增件）：

  | 厅 | meshes | tris | lights |
  |----|--------|------|--------|
  | lobby | 203 | 124,153 | 26 |
  | archive | 120 | 50,230 | 26 |
  | eraserhead | 122 | 33,328 | 13 |
  | bluevelvet | 138 | 54,599 | 18 |
  | studio | 117 | 22,031 | 9 |
  | twinpeaks | 166 | 83,759 | 12 |
  | mulholland | 139 | 86,447 | 17 |

- [x] 低画质档强化：关 Bloom + 像素比 1.0 + **颗粒/扫描线/色差减半**（引擎记录出厂基线按档回退），Q 键即时切换
- [x] **自动降档保帧**：高档持续 5 秒 <32fps → 自动切低档 + 字幕提示 + 按钮同步；玩家手动按 Q 即锁定
      （冒烟软件渲染下实际触发，截屏可见「画质 低」，即真实工况验证）
- [x] **装载健壮性（二修）**：厅分包动态 import 指数退避重试 ×3 **+ 进馆后空闲预取**（每 900ms
      一个分包，趁 IO 平静入缓存）——第七厅 chunk fetch 曾在长跑内存/IO 压力下连败四次，
      预取后全巡检稳定全绿（见 WORKLOG 21:38 段）

**验证方式**：`xvfb-run npx electron . --smoke` 输出逐厅统计且退出码 0。✅ 全绿。

### 23. 音景 / 配乐抛光（v1.3 新增）
- [x] 新交互专属合成音 ≥10 种 —— **实际 18 种**：bell 迎宾铃 / ratchet 拨号棘轮 / creak 木革吱呀 /
      switch 重型拨杆 / crank 留声机发条 / projector 放映机快门 / vinyl 黑胶底噪 / doorfar 远门闷响 /
      phonering 木盒电话双铃 / gurgle 地漏咕噜 / rain 雨声坡（百叶开着时叠续成雨幕）+
      **脚步 7 种**（wood/concrete/tile/carpet/asphalt/dirt/metal，每步频率抖动，
      按步幅 0.82m 触发、左右交替声像）
- [x] 位置化音效通道 `sfxAt(name, x, z)`（StereoPanner + 纯几何 `spatialParams` 声像/距离衰减，
      可单测），听者位姿 `setListener` 每帧取相机；铃/蒸汽/拨号/信号灯等已位置化
- [x] **空间混响**：ConvolverNode + 程序化立体声 IR（零采样），四预设 SPACES（hall/room/tiled/outdoor），
      逐厅 `meta.space` + `spaceAt(x,z)` 分区动态切换（diner/红房间/剧场=room，外景干声）
- [x] 逐厅地面材质映射：`meta.floorSfx` ×7 + `surfaceAt(x,z)` 分区 ×4（夜街沥青/diner 瓷砖/
      剧场地毯/圆毯绒面/锅炉步道钢格栅…）
- [x] 大厅留声机可独立点播/停止爵士层；配乐音量不盖过留白静默（duck 连动 + 字母旁白静默拍）；
      **爵士层抛光**：四小节回转双音下行钢琴填充 + 22s 低通呼吸 LFO（fillScale 严格下行入单测）
- [x] 底噪事件层逐厅有戏：大厅远门 doorfar / 蓝丝绒幽灵杯盏 sip / 双峰猫头鹰 / 档案荧光管 /
      橡皮头远汽笛 ×2 / 穆赫兰道涌动 swell
- [x] 零采样策略保持（compliance 扫描全绿：仓库零媒体文件）

**验证方式**：`npm test`（audio.test.js 含 spatialParams 几何断言 + 脚步/混响审计）+
冒烟 `activateAll()` 90 交互不抛错 + 人工耳机巡检。✅ 全绿。

### 24. Release 1.3.0（v1.3 新增）
- [x] `npm test`（139 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke`（全厅巡检：
      预算/交互阈值/激活/彩蛋/UI 全绿）三连全绿（2026-08-25 23:15 UTC，版本 bump 后复跑）
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.3.0.exe`（100.0MB，便携版双击即运行）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.3.0.exe`（100.2MB，NSIS 一键安装包）
- [x] `SHA256SUMS.txt` 更新（portable `d4c744f6…` / setup `e9f00f8c…`）；`file` PE32 结构校验
      两 exe 均通过；文档（README/BUILD/TESTING/本文件）同步 1.3.0

---

### 25. 讲解配额保持 v1.0 克制（v1.4 审计）
- [x] 旁白配额保持：全馆 ≤8 条、每条 ≤16 字、总字数 ≤110（实测 84 字）；展签每厅 ≤1；
      交互字幕 ≤22 字（含全部 v1.4 新增交互字幕）；叙事禁词扫描零出现
- [x] v1.4 新增交互字幕逐条人工通读：只写眼前物性/感受，零原作叙事剧透
      （伞架/衣帽架/拨盘/冰桶/投币/报箱/服务铃/鸮/糖罐/盒盖/站钟等全部合规）

**验证方式**：`npm test` narration 组全绿 + 人工通读。

### 26. PS5-tier 画质清单 P1–P10（v1.4 新增，继承 G1–G9）
- [x] P1 五通道 PBR：`aoFromHeight`（两尺度环绕盒模糊）AO + metalnessMap 落地；
      `setFrom` 升级后木/砖/水泥/沥青/折线地板/拉丝金属/大理石全部自动获得
- [x] P2 新材质组 ×3：marbleSet（大厅镶边带/柱础/纪念台+档案纪念墙）/
      boomerangSet（diner 柜台+卡座桌两号纹样）/ rustSet（锅炉底橇+泄压总管）
- [x] P3 微观质感：吊灯水晶垂坠/机器铆钉排+缸盖螺栓/柜台包边/票亭双层顶檐/
      椅号牌/伞架束环/戏报箱黄铜框……倒角+铆钉+磨损+污迹全场 ≥2 种同场
- [x] P4 电影分级：七厅 `meta.look.grade` 全配（暖金大厅/月夜冷双峰/蓝调歌厅/
      梦紫剧场/琥珀房间/冷灰工业/档案荧光）
- [x] P5 Halation：LynchPass 六向亮部采样暖晕（uHalation 0.10–0.17 逐厅），
      低档自动置 0（engine.setQuality）
- [x] P6 近景实用光：桌灯池/炉口红光/票亭内灯/包厢桌灯/霓虹反光池；
      交互连锁改读感 ≥1：脚灯三档拨盘（暖/蓝/熄）整台变脸 + 深蓝时刻 + 调光三档
- [x] P7 双层体积光锥 lightCone2 ×3 处：大厅纪念台（随调光收放）/
      穆赫兰道剧场聚光 / 蓝丝绒舞台聚光
- [x] P8 远近景三层：双峰（近景英雄→中景四分区→ridgeRing 山脊双环+双主峰+雾）；
      穆赫兰道（近景→路肩/暗巷/空地→屋脊环+水塔+棕榈 ×7+群山+地平线光晕）
- [x] P9 画质档：低档=关 Bloom+关 halation+像素比 1.0+颗粒/扫描线/色差减半；
      自动降档（5s@<32fps）+ Q 手动锁定保持（冒烟截屏多次捕获降档 toast 佐证）
- [x] P10 禁塑料：新增可见面全部程序纹理/织物 sheen/清漆/玻璃透明层（截屏复核）

**验证方式**：七厅+内景机位截屏逐张复核 + 源码审读 + PRODUCTION_PLAN §1.1 勾选。

### 27. 分厅重做清单（v1.4 新增）
- [x] PRODUCTION_PLAN §3.1 表内每厅 ≥2 件「重做或显著增件」完成
      （lobby 大理石柱础+吊灯 v3+双层锥 / twinpeaks boomerang+轿车 v2.5+杂物组 v2+远景 /
      bluevelvet 台口 v2+包厢+话筒 v2 / mulholland 排椅 v2+票亭+远景 /
      studio 工作桌 v2+颜料台+窗雨 / eraserhead 机器 v2+锈蚀+蒸汽歧管 /
      archive 目录柜 v2+图书梯+档案盒堆）——全表 21/21 件完成
- [x] 每厅 ≥2 件独有英雄资产（吊灯/纪念台、派柜/轿车、台口/包厢、票亭/排椅、
      工作桌/颜料台、大机器/泄压总管、纪念墙/图书梯——均为厅内独有）
- [x] 装配感：曲柄连杆全链可读/缸盖螺栓/地脚螺栓板/折窗铰链/抽屉滑轨/
      梯钩扣轨/托座搁板——螺栓/合页/榫头至少一种全场可见

**验证方式**：截屏对照 + commit 链 + 源码审读。

### 28. 交互密度 ≥105 + 连锁 ≥2/厅（v1.4 新增）
- [x] 全馆非导航交互 **122 ≥105**（lobby 13 / archive 24 / eraserhead 17 /
      bluevelvet 16 / studio 18 / twinpeaks 18 / mulholland 16）；
      每厅 ≥13（第六遍抛光后普查）
- [x] 每厅连锁 ≥2 条（服务铃→派柜自转、泄压手轮→跨房压力表、投币→亭灯闪、
      乐队→脚灯+吊灯+后幕、调光→吊灯+体积锥+顶光、图书梯→轮转+落定、
      低音提琴→聚光吸气、总闸→全屋灯塌+机器拖慢+自弹回、气送管→远响+回舱、
      打卡钟→压杆+卡片跳+工作灯眨、杯架→晃+双声错拍……）
- [x] 新交互全部 ≥2 通道反馈；activateAll 七厅全量激活无异常（冒烟输出）
- [x] 冒烟阈值锁定在终版普查 -1：INTERACTIVE_MIN lobby 12 / archive 23 /
      eraserhead 16 / bluevelvet 15 / studio 17 / twinpeaks 17 / mulholland 15

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅输出交互统计且全绿。

### 29. 音景/配乐 v1.4 抛光（v1.4 新增）
- [x] 新合成音色 **11 种 ≥6**：coin/springdoor/ladderroll/flutter/stamp/iceclink/
      jostle/thunder/drip/coalrattle/pluck（audio.test.js 源码审计 + 展厅接线普查断言）
- [x] 连锁反馈声分层：主音 + 空间尾音 + 持续层（如 flutter+900ms 远 hoot、
      stamp 自带抬起→压落时间线、thunder 随闪电延迟衰减距离感）
- [x] sfxAt 位置化/程序化混响四预设/七种脚步/爵士层全部保持；零采样扫描全绿

**验证方式**：`npm test`（audio 扩展）+ 冒烟 activateAll 不抛错。

### 30. 性能预算 PS5-tier 上调 + 硬断言（v1.4 新增）
- [x] `--smoke` 预算断言上调至 meshes ≤240 / tris ≤240,000 / lights ≤40，全绿
- [x] v1.4.0 逐厅实测表入册（PRODUCTION_PLAN §6.1，软件渲染冒烟输出）
- [x] 低画质档补关 halation（engine.setQuality 置 uHalation=0）；
      自动降档与 Q 锁定保持（截屏多次捕获降档 toast）
- [x] 纹理尺寸遵守 §2.1 上限表（kit.test.js「所有程序纹理 canvas 尺寸 ≤1024」审计）

**验证方式**：`xvfb-run npx electron . --smoke` 输出逐厅统计且退出码 0。

### 31. Release 1.4.0（v1.4 新增）
- [x] `npm test`（166）/ `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
      （版本 bump 后 08:06–08:08Z 复跑）
- [x] WORKLOG v1.4 段显示本轮 ≥8 小时（00:08Z 开工 → 08:1xZ 发布，UTC 时间戳与 commit 互证）
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.4.0.exe`（100.0MB，PE32 GUI x64）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.4.0.exe`（100.2MB，NSIS oneClick）
- [x] `SHA256SUMS.txt` 更新（8a709a81… / dae05302…）+ `file` PE32 结构校验通过
- [x] 文档（README/BUILD/TESTING/CHANGELOG/本文件）同步 1.4.0

---

### 32. 非真人配音 —— 介于真实与虚假之间（v1.5 新增）
- [x] **清晰真人朗读（Web Speech TTS）全库退场**：`speechSynthesis` / `SpeechSynthesisUtterance`
      在 `src/` 零出现（narration.test.js 源码级扫描断言，门禁写死防回潮）
- [x] **「低语」代替配音**（`src/audio/murmur.js` MurmurVoice）：气声音节（粉噪过两级
      成形带通，F1/F2 落在真实语音共振峰域内游走但绝不成词）+ 无线电静电碎语 +
      句尾一次呼吸；总线电平 ≤0.25 压在氛围之下——听得出「有个声音在说话」，
      永远听不清说什么字
- [x] 模式表更新：`letters 字母显现（默认）→ murmur 低语+字母 → jazz 爵士+字母 → off`；
      字母显现仍为一切开启模式的基座（无障碍字幕）
- [x] murmurPlan 纯函数入单测（murmur.test.js 9 用例）：确定性/时间线单调/句尾必呼吸/
      共振峰域/标点留白/静电占比 <40%/未解锁与静音安全空转
- [x] 旁白内容保持 v1.0 克制配额：全馆 8 条、每条 ≤16 字、总 84 字、首访一次、
      延迟 2.6s（narration.test.js 保持断言）

**验证方式**：`npm test`（murmur.test.js + narration.test.js TTS 退场扫描）+
`--smoke` 模式循环含 murmur 且 speak/stop 无异常。✅ 全绿。

### 33. show, don't tell —— 走近才显影的引语立牌（v1.5 新增）
- [x] **墙挂大字展签（quotePlaque）全馆退场**（源码级扫描断言）；取代者为
      `kit.quoteStand`：黄铜细杆托斜面小板，远看近黑只有一枚微亮引号——
      **字在你走近之前不存在**
- [x] **接近显影**：`quoteStandUpdater` 入厅 updaters，玩家进 3m 半径 → 板上原话
      缓缓显影（透明显影层 + 自发光渐亮），同时 HUD 侧卡浮现
      **原话 + 一句解释（note）+ 一句评述（aside）**；走出（带 0.9m 迟滞）即收回
- [x] 随行文案配额：note/aside 各 ≤36 字、单句、事实级（趣闻/策展短评克制不展开）——
      单测断言长度与单句性；每厅立牌 ≤1、每座必接接近驱动器（源码级审计）
- [x] **元叙述清零**：展陈文案（引语+note/aside+旁白）不谈制作方法
      （「程序化/建模/渲染/多边形/着色器/PS5/引擎/帧率/贴图」禁词扫描）；
      「关于画质」页（ABOUT_RENDER）整页移除——合规页保留必要的版权事实陈述
- [x] 零原作叙事保持：叙事禁词/角色名/对白扫描全绿（门禁 19 断言不变）

**验证方式**：`npm test` narration.test.js 立牌组 + 截屏复核（远景近黑 / 走近显影 /
侧卡浮现）。✅ 全绿。

### 34. 大厅焦点修复 + 全馆减法（v1.5 新增）
- [x] **中央唯一焦点**：出生点 → 纪念碑视线全程无遮挡——伞架（正立在视线上）/
      丝绒长凳对（横在半路「一排一排」）/ 绒绳围栏 / 立式烟灰缸全部退场；
      纪念花圈从碑后肩移到帷幕脚下柱间
- [x] **地面语言收敛**：同心圈从四五道减到一道外金圈 + 大理石镶边带；
      碑体加低位暖洗光（steleWash），进门第一眼读得到名字与年份
- [x] **帷幕整体化**：`curtainRing` 重写为一件连续圆柱幕（褶皱沿整弧不断、
      整圆时波数取整 + 接缝法线焊接，弧径向段数按弧长自适应）——大厅环幕、
      幕头、**红房间围合幕**全部单件成型，不再是多段布片拼出的无来由接缝
- [x] **交互减法（有意的减）**：全馆非导航交互 122 → **117**（lobby 10 / archive 24 /
      eraserhead 17 / bluevelvet 15 / studio 18 / twinpeaks 17 / mulholland 16）；
      去掉的全是清单打卡件：大厅伞架、双峰缺角派（罩下整派已把话说完）、
      蓝丝绒吊杯架（酒瓶背光已说完吧台的话）;真正有意味的连锁全部保留
- [x] 冒烟阈值随普查下调并锁 -1：INTERACTIVE_MIN lobby 9 / archive 23 / eraserhead 16 /
      bluevelvet 14 / twinpeaks 16 / mulholland 15 / studio 17
- [x] 大厅场景瘦身实证：meshes 203→156、tris 12.4万→7.0万、呼吸感回归
      （冒烟统计 + 出生点截屏复核）

**验证方式**：`--smoke` 统计 + 出生点/红房间截屏逐张复核。✅ 全绿。

### 35. Release 1.5.0（v1.5 新增）
- [x] `npm test`（186 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.5.0.exe`（PE32 GUI x64）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.5.0.exe`（NSIS oneClick）
- [x] `SHA256SUMS.txt` 更新 + `file` PE32 结构校验
- [x] 文档（README/BUILD/TESTING/CHANGELOG/本文件）同步 1.5.0

---

### 36. 蓝丝绒厅氛围收紧 —— 深夜酒吧的暗红与钴蓝（v1.6 新增）
- [x] **分级收紧**（`meta.look`）：曝光 0.92→0.86、雾更沉更蓝黑（fogDensity 0.05→0.055、
      fogColor 0x02030a）、红增益抬高 + 绿压扁 + 暗部灌钴蓝（CDL gain [1.07,0.95,1.03] /
      lift 蓝通道 0.026）、halation 0.14→0.18——过亮的红与过静的蓝同框
- [x] **过亮的红**：后幕换深牛血红天鹅绒（0x66101c）+ 专属红洗光 PointLight（0xff2438，
      基础 5.2 / 乐队起 6.6）——红在暗场里也「亮着」，像有人从幕后打光
- [x] **过静的蓝**：舞台聚光转钴蓝（0x9fc0ff）窄锥慢呼吸；**中央空场冷光池**
      （无来由的冷顶光 + 双层体积锥，桌子全部退向墙根让出空地）——房间中央
      没有任何理由地亮着一小片冷
- [x] **全线减亮去「夜店感」**：桌灯/吊坠灯/吧台背光/霓虹全部压低 30–50%，
      桌灯闪烁放慢；吧台背光偶发「压降」（brown-out 半秒全排暗一档再爬回）
- [x] **阴影里的东西（声先于影）**：深蓝时刻的包厢里坐进一个不动的形体
      （darkFigure 坐姿，先闻布料摩擦声后见影，注视 2.2s 或灯光回暖即消失，
      ≥90s 冷却）；**骤静拍**：每 75–110s 全场 duck 1.4s + 一声很远的门
- [x] **底噪失谐**：三度音压低 0.9Hz（微失谐拍频）+ 36.7Hz 次低频常驻 + LFO 放慢
      ——和弦永远差一口气才解决（audio.test.js 频率/增益域断言保持全绿）
- [x] 交互密度/预算不回退：非导航交互 ≥15 保持，meshes/tris/lights 预算内

**验证方式**：`SV_SHOT_DIR=… --smoke` 截屏复核（红幕烧亮/钴蓝窄锥/中央冷光池/
整体暗场）+ `npm test` audio 组 + 冒烟交互阈值断言。✅ 全绿。

### 37. 穆赫兰道后巷通路修通 + 惊吓可复现（v1.6 新增）
- [x] **通路修通（v1.5 的路走不过去）**：v1.5 walkable 只有夜路与暗巷北端一个窄口，
      玩家沿剧场右侧右拐即撞隐形墙（x=4.6）。v1.6 重排 `multiRectBounds` 为七段
      连续 union：路面 ROAD → 右侧便道 WALKWAY（x 4.6–8.4）→ 票亭转角 CORNER →
      暗巷 ALLEY（加宽至 2.6m、南北打通）→ 背后空地 BACKLOT，任意相邻段重叠
      ≥1.5m 走廊宽（单测断言）
- [x] **地面/边界/碰撞可见化**：右侧水泥便道与巷面浇上可见混凝土 pad（`concreteMat`，
      不再是「看着是墙的地方能走/看着是地的地方不能走」）；报箱挪到巷口当面包屑、
      电话亭贴墙让出走廊；`surfaceAt` 同步（便道/巷面=concrete 脚步、空地=dirt）
- [x] **灯光引导**：便道尽头「SALIDA DE ARTISTAS」立牌 + 铁笼罩灯（将熄的黄光，
      闪烁节律与巷内壁灯呼应）——不用 UI 指路，用一盏灯把人往巷子里勾
- [x] **巷中恐惧拍**（前菜）：暗巷中段 zoneTrigger——两盏巷灯同时熄灭 1.2s +
      一声不属于自己的呼吸（左后方位置化），灯再颤回来；40s 冷却
- [x] **惊吓强化**（主菜）：背后空地垃圾箱带 → 声音抽真空 → 两记心跳 →
      **整巷灯灭（lampKill）** → 黑影 0.65s 扑至面前（高频痉挛 + 合成噪墙 +
      thud 闷击 + uShock 后处理冲击 + 暗红闪帧）→ 黑幕 → **空间错位**：
      醒来已被移回巷口（9.7, 9.5）；45s 冷却可再触发
- [x] **可走通性入自动化**（防回潮三层）：
      ① `tests/mulholland-path.test.js` 纯几何单测 ×8（SPAWN/SCARE_ZONE/传送点
      均在 walkable 内、文档路线逐帧钳制走行可达、v1.5 右拐卡死回归守卫、
      北口老路仍通、往返可走、走廊 ≥1.5m、触发区覆盖 >50%）；
      ② `window.__SV__.walkPath(route)` 钩子：真实玩家逐帧钳制行走（每帧 0.06m，
      与 controls 同一 bounds），冒烟中走 路→便道→转角→暗巷→空地 全程，
      断言实际到点（与目标距离 <0.6m）；
      ③ 冒烟继续等待**自然触发**的惊吓把玩家传回巷口（轮询位置突变），
      证明「走过去就会被吓」而非仅 triggerEggs 强制引爆
- [x] TESTING.md 复现步骤只写空间路径（沿建筑右侧绕到后巷），零原作情节剧透

**验证方式**：`npm test`（mulholland-path ×8）+ `xvfb-run npx electron . --smoke`
输出 `后巷走通性 mulholland: {ok:true…}` 与自然触发传送断言。✅ 全绿。

### 38. Release 1.6.0（v1.6 新增）
- [x] `npm test`（194 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.6.0.exe`（PE32 GUI x64）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.6.0.exe`（NSIS oneClick）
- [x] `SHA256SUMS.txt` 更新 + `file` PE32 结构校验
- [x] 文档（README/BUILD/TESTING/CHANGELOG/本文件）同步 1.6.0

---

### 39. 大厅中央焦点完整化 —— 不再是「半截黑块」（v1.7 新增）
- [x] **纪念独石重做（props.memorialStele v3）**：单件完整独石——底座三层收分台阶
      环抱、碑身四棱倒角、**两条侧棱嵌通高光缝**（自发光缝条随呼吸脉动）、
      正面全高刻字面（名字+年份+一句），**背面蚀刻烟形浮纹**——任何角度看
      都是一件完成的雕塑，绕到背后不再是一块没做完的黑板
- [x] **双面洗光**：正面低位暖洗光保留，新增背面第二盏洗光（steleWashB）——
      「显露一半」的病根（背面无光掉进雾里）根治；台座圆环金圈保留唯一地面语言
- [x] **站位有构图理由**：全部外围站件（留声机/胶片柜/引语立牌/讲台/迎宾铃桌/
      衣帽树/花圈画架）退到**柱环半径的外圈、与柱位对齐**——中央只剩独石与
      光锥，出生点视线全程无遮挡，第一眼是完成的数字艺术不是未完成关卡
- [x] **中央可交互**：独石光缝（E→光缝涌亮+石钟低鸣）/ 台座光涌（踏上台阶
      光锥增强）/ 帷幕触摸（波纹爬上绒面+帷头回弹，两成概率带窃语）
- [x] 场景瘦身保持：lobby meshes 158 / tris 69,951 / lights 28（预算内，冒烟统计）

**验证方式**：`SV_SHOT_DIR=… --smoke` 出生点+绕背截屏复核 + 冒烟统计。✅ 全绿。

### 40. 穆赫兰道转身惊吓 —— 回头那一下它已经在扑来（v1.7 新增）
- [x] **踩圈触发退场**：v1.6 的 zoneTrigger 惊吓移除——走到某个圈上不再有任何事
- [x] **转身触发器（kit.turnTrigger）**：滑动窗累计 yaw 角速度——半秒窗内累计
      转角 ≥2.0 rad（甩头式 180° 回望）才触发；慢扫视 180°/2s、小甩 90° 均不触发；
      武装区（暗巷深段+背后空地）内驻留 ≥1s 才上膛，出区归零；50s 冷却
- [x] **实体人影冲脸（kit.veiledFigure）**：LatheGeometry 车削帷形人影（连续
      剖面曲线+高分段光滑法线+裙裾摆动），**零方块零棱角**；触发瞬间它已在你
      转过去的视线上 4.6m 处，0.42s 加速扑至面前（前倾+裙裾展开+连续侧摆）
- [x] **穆赫兰道式节奏**：转身瞬间整巷灯灭 + 声音抽真空 → 冲刺 → 闷击 +
      uShock 后处理冲击 + 暗红闪帧 → 黑幕 → **空间错位**：醒来已被移回巷口
      (9.7, 9.5)，背对来路；字幕「有些东西只在你回头时存在。」（原创，零对白引用）
- [x] **三层自动化防回潮**：① `tests/turnscare.test.js` 纯逻辑仿真 ×11（甩头触发/
      慢转不触发/小甩不触发/未上膛不触发/区外不触发/15fps 鲁棒/单帧突变触发/
      冷却行为/force/参数与展厅导出一致）② `tests/mulholland-path.test.js` 几何
      守卫（站定点在武装区、武装区 walkable 覆盖 >90%、传送点在区外）③ 运行时
      冒烟：walkPath 走到空地站定 → `spinYaw(π)` 模拟猛回头 → 断言**自然触发**
      把玩家传回巷口（软渲染低帧下 40s 预算 + 每 3s 补甩）
- [x] TESTING.md 复现步骤更新为转身触发（空间路径，零剧透）

**验证方式**：`npm test`（turnscare + mulholland-path）+ `xvfb-run npx electron . --smoke`
输出「转身惊吓自然触发 OK」。✅ 全绿。

### 41. 博物馆讲解员 —— 信息型导览替换空洞氛围句（v1.7 新增）
- [x] **进厅旁白重写（NARRATIONS）**：8 段全部改为讲解员口吻——制作背景、
      公开事实、风格语境（如首映年份/拍摄周期/手法特征），每段 24–96 字、
      ≤3 句、全馆 ≤620 字；「适应黑暗」「冷风吹过山」类空洞氛围句全部清退
      （单测列禁句扫描）
- [x] **每段含事实锚点**：年份/片名/工艺名词等可查证信息（单测断言每段命中
      锚点词表）；仍然零叙事剧透——只讲制作与语境，不讲剧情（叙事禁词扫描保持）
- [x] **DOCENT 随行讲解层**：每块引语立牌配一段讲解（林奇对该片/该环节的
      公开访谈想法+出处类型，短引语非大段抄袭）——玩家在立牌旁**驻足端详
      1.6s** 后由字母显现层低声补出（每厅一遍，不打断其他讲解），不塞进立牌弹层
- [x] 低语/字母代配音体系保持（门禁 32 不回退）；无清晰 TTS 干读
- [x] 版权约束保持：短引语 ≤200 字符、出处类型标注、合理使用声明（门禁 7 断言）

**验证方式**：`npm test` narration.test.js「讲解员导览」组（长度/锚点/禁句/
DOCENT 配套）。✅ 全绿。

### 42. 立牌只留名言 + 彩蛋与交互密度（v1.7 新增）
- [x] **立牌减法**：走近/交互立牌只显示**那句名言**（中英+出处）——QUOTES 的
      note/aside 字段删除、overlay 弹层不再渲染补充段（源码级单测断言）；
      背景与访谈想法全部移交 DOCENT 讲解层（见门禁 41）
- [x] **影片彩蛋新增 ≥5（宇宙机器人式点哪都有反馈，不照搬）**：
      大厅**摸帷幕**（波纹+帷头回弹+偶发窃语）/ 大厅**台座光涌**（踏上台阶光
      锥增强）/ 穆赫兰道**台边浓缩咖啡杯**（E→勺子轻碰+一声很轻的嫌弃）/
      穆赫兰道**场记板**（E→合板脆响+聚光眨一下）/ 双峰**绒垫原木**（E→
      木身微滚+一声很近的松涛）——全部为原创抽象致敬，无角色肖像
- [x] **角色彩蛋约束遵守**：唯一「人形」是车削光滑的帷形人影（门禁 40）；
      未做任何方块人/棱角粗糙角色
- [x] **交互密度普查 122（v1.5 终版 117 → +5）**：lobby 12 / archive 24 /
      eraserhead 17 / bluevelvet 15 / studio 18 / twinpeaks 18 / mulholland 18；
      冒烟阈值重锁普查 -1：11/23/16/14/17/17/17
- [x] 每个新交互 ≥2 通道反馈（声+光/动画/字幕）；activateAll 七厅全量激活无异常

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅交互统计全绿 + TESTING.md
交互密度抽查清单。✅ 全绿。

### 43. Release 1.7.0（v1.7 新增）
- [x] `npm test`（211 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.7.0.exe`（PE32 GUI x64）
- [x] `release/SmokeVelvet-LynchArchive-Setup-1.7.0.exe`（NSIS oneClick）
- [x] `SHA256SUMS.txt` 更新 + `file` PE32 结构校验
- [x] 文档（README/BUILD/TESTING/CHANGELOG/本文件）同步 1.7.0

---

### 44. 穆赫兰道拐角惊吓 —— 主触发改拐角 + 多幕节奏（v1.8 新增）
- [x] **主触发改拐角（zoneTrigger 系，贴原作戏剧位置）**：`kit.cornerTrigger`——
      走进巷尾拐角触发区（圆心 9.25,-25.9 r2.3）且视线落进「垃圾箱·后门」
      方向 ±fov/2（74.5°）锥内即引爆；顺巷南行天然入锥、**背向北归穿区不触发**、
      面朝墙进区的转回那个方向的瞬间触发；75s 冷却**可重复**；
      不再以 turnTrigger 猛回头为主
- [x] **v1.7 成果保留**：转身惊吓（turnTrigger + veiledFigure + 武装区/上膛/
      甩头阈值）完整保留为第二重扳机，两重扳机共用状态机互斥；巷中恐惧拍
      保留并让位（主惊吓进行中不抢灯）；后巷通路/讲解员/独石/交互密度全部不动
- [x] **多幕节奏（SCARE_BEATS 实时钟时间线，单测锁定次序）**：
      整巷灯狂闪（新 panic 灯通道）→ 全灭 + 声音抽真空 + 拐角金属长刮擦
      （新音色 `scrape`）+ 心跳 → **留白 ≥0.5s 万籁俱寂** → 剪影光起、黑影从
      拐角后**一顿一顿挪出**（`lurchEase` 阶梯缓动：每步只在前 42% 挪、其余
      死死停住；每顿一声刮擦+落定闷响、心跳渐密）→ **0.5s 加速扑脸**（k² 加速）
      → 闷击 + uShock 冲击 + 暗红闪帧 → 黑幕 → **空间错位**移回巷口；
      字幕「有些拐角，不该拐过去。」（原创 ≤22 字，零对白引用）
- [x] **三层自动化防回潮**：① `tests/cornerscare.test.js` ×27（主路径触发/
      背向不触发/转回瞬间触发且早于正对角/区外不触发/冷却可重复/force/
      yaw 卷绕/15fps 鲁棒/触发区几何守卫/节拍次序与留白/lurchEase 顿挪性质/
      源码级接线与字幕审计）② 几何守卫：错位落点在拐角区与转身武装区之外
      ③ 运行时冒烟：walkPath 走进拐角区断言**自然触发**（多幕走完传回巷口），
      再走空地 spinYaw(π) 断言转身扳机保留——两重断言全绿

**验证方式**：`npm test`（cornerscare + turnscare + mulholland-path）+
`xvfb-run npx electron . --smoke` 输出「拐角惊吓自然触发 OK」与
「转身惊吓自然触发 OK」。✅ 全绿。

### 45. 拐角魅影形体升级 + 场景二级细节（v1.8 新增）
- [x] **新预制体 `kit.cornerWraith`（比 veiledFigure 更细一级，仍抽象无面目）**：
      车削主身（48 段光滑）+ 非对称后处理——肩背**驼峰**向背侧钟形隆起、
      **头前倾埋进兜帽**（无任何五官/面部细节）、下身深布褶；裙裾一圈
      seeded 长短错落的**破布条**（13 条收尖锥合并单 mesh）；**两条过长垂臂**
      （双段微屈+收尖手锥、指尖近地）挂独立枢轴——`setLurch` 顿挪体态
      （侧倾/沉肩/臂拖摆，平台段全身冻住）与 `setRush` 扑近形变（深前倾+
      双臂甩后+裙裾展开）两套身体语言
- [x] **剪影光**：拐角后一盏冷背光（0x9fb7ff）只在现身时渐亮、黑幕即灭——
      黑影只读出轮廓读不出细节（闪光拍里读出体积）
- [x] **场景二级细节**：后墙拐角根**刮痕组**（七道拖行划痕+尽头三道并排
      深抓痕，透明贴花）——新交互 E→很近的刮擦+半拍后黑里一次呼吸+
      「刮痕比你高，还在变多。」；拐角后**地面拖痕与焦黑蹲踞斑**（多层
      半透椭圆叠边+平行擦线，它等的地方）
- [x] **交互密度普查 123（v1.7 122 → +1 墙角刮痕）**：mulholland 18→19；
      INTERACTIVE_MIN 重锁普查-1（mulholland 17→18），单测断言不回退
- [x] 预算复核：mulholland 206 mesh / 98,538 tris / 21 灯（240/240k/40 内，
      冒烟统计）；合规保持——零媒体素材、全程序化、无受保护对白、无面目

**验证方式**：运行时冒烟逐厅统计 + 交互密度全绿；`npm test` 源码级审计。✅ 全绿。

### 46. Release 1.8.0（v1.8 新增）
- [x] `npm test`（240 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke` 三连全绿
- [x] 版本 bump 1.8.0（package.json + `__SV__.version`）；CHANGELOG v1.8.0
- [x] 文档（README/BUILD/TESTING/本文件/WORKLOG）同步 1.8.0
- [ ] `release/*-1.8.0.exe` 双目标重打包 + `SHA256SUMS.txt` 重写（交由发布流程：
      `npm run dist:win`，Linux 下需 wine64+wine32:i386，见 BUILD.md）

---

### 47. 画面会呼吸 —— 雾/尘埃节奏 + 开幕点灯 + enamel（v1.9 新增）
- [x] **B1 雾的呼吸**：逐厅雾密度极缓呼吸（`meta.look.fogPulse` 周期 26–44s、
      深度 ±7–14% 逐厅配置，工业厅急房间浅；纯标量零带宽，低档保留）
- [x] **B2 尘埃节奏**：lobby 光尘+地烟 / mulholland 路雾+尘 / twinpeaks 林地雾
      三厅粒子层透明度随 `engine.breath` 同一相位 ±28–32% 低频调制
- [x] **B3 大厅开幕点灯序列**：黑起 → 六盏吊灯 0.9s 起错拍点亮（各配位置化
      lampon）→ 3.6s 主灯组/光锥 smoothstep 升起 + swell → 6.1s 霓虹醒 + chime →
      7.2s 收口（≈7.2s，模块级只演一次；openGate 乘法闸与颤动/blackout 互不干扰）
- [x] **B4 enamelSet/enamelMat 瓷釉铁皮**落地（kit 四通道：白瓷釉 rough 52 +
      贴边磕碰崩瓷露黑铁 + 26 条釉裂游走细纹 + 茶渍云斑 + clearcoat 0.6）；
      bluevelvet 脚灯槽反光碗 ×9 + eraserhead 压力表珐琅盘系使用
- [x] **B5 电影分级复核**：抛光第 1–9 遍逐厅默认机位截屏巡检 + 微调入 WORKLOG
      （lobby 褶皱篷顶/tp 月亮 v2/bv 壁灯光池/碑身流纹 roughnessMap 等）
- [x] **B6 低档回退**：fogPulse 纯标量、尘埃只改 opacity 零带宽、开幕闸乘法
      不碰档位；自动降档在 swiftshader 冒烟真实触发（截屏「画质 低」为证）

**验证方式**：截屏对比 + 源码审读 + `--smoke` 全绿。✅ 全绿（阶段 1/5 + 抛光 1–9 遍）。

### 48. 逐厅英雄资产二级细节 v1.9 + 交互 ≥126（v1.9 新增）
- [x] PRODUCTION_PLAN §2 清单每厅 ≥2 件新增/重做（2a–2f 七厅两件全落地 +
      抛光 1–9 遍再加码：篷顶/月亮/链条吊钩/立面收顶/挂画轨/暗巷穿线管/
      还书推车/矮柜磁带机/饭盒/触痕层贴花×3/幕后鼓包/夜车扫光；
      指纹污迹类：票亭指印+调光板指痕晕+桌面残环+窗凝雾擦痕 ≥2 种同场）
- [x] 全馆非导航交互实测 **143**（lobby16/arc27/era21/bv17/tp19/mul20/stu23，
      v1.8 123 → +20，要求 ≥126 达标）；新交互全部 ≥2 通道反馈、
      字幕 ≤22 字零剧透；每厅连锁 ≥2 保持（PRODUCTION_PLAN §3.1 表）
- [x] INTERACTIVE_MIN 重锁普查 -1（15/26/20/16/18/19/22）；
      activateAll 七厅全量激活无异常
- [x] 既有彩蛋/通路/两重惊吓不回退（拐角/转身惊吓自然触发断言每遍全绿；
      cornerscare ×27 / turnscare ×11 / mulholland-path 单测不放宽）
- [x] 性能预算保持 240/240k/40 硬断言全绿；逐厅终版实测
      171–221 mesh / 33.1k–100.8k tris / 12–29 灯（PRODUCTION_PLAN §5.1）

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅统计全绿 + 截屏复核 + 普查表。✅ 全绿。

### 49. 音景 v1.9 —— 新合成音色 ≥4 + 零采样（v1.9 新增）
- [x] 新合成音色 **9 种**（要求 ≥4）：flamegut / woodknock / porcelain / sharpen /
      tassel / glasswipe / chainrattle / tapewhirr / carpass——全部与新交互/怪谈
      配套接线、sfxAt 位置化；分层反馈（主音+尾音/持续层）
- [x] audio.test.js 音色审计扩展（NEW_V19 ×9 实现 + 七厅接线扫描）；参数域安全
- [x] 零采样扫描保持全绿（compliance：src/ 与 electron/ 零图像/音频/视频文件）

**验证方式**：`npm test` audio 组 + 冒烟 activateAll 不抛错。✅ 全绿（251 用例）。

### 50. Release 1.9.0（v1.9 新增）
- [x] `npm test`（251 用例）/ `npm run smoke` / `xvfb-run npx electron . --smoke`
      三连全绿（打包构建产物上复跑，两重惊吓自然触发保持）
- [x] WORKLOG v1.9 段显示本轮 ≥8 小时（14:40–23:0x UTC，逐段与 git 提交时间互证）
- [x] 版本 bump 1.9.0（package.json + `__SV__.version` 一致，dist 产物内核对）；
      CHANGELOG v1.9.0
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.9.0.exe` + Setup NSIS 双目标
      （wine64+wine32 交叉打包；`file` PE32 校验通过）+ `SHA256SUMS.txt` 重写；
      旧版 1.7.0 exe 从本分支移除防混淆
- [x] `DOWNLOAD.md` 提供 raw 下载直链并醒目标注「请下本轮 Portable-1.9.0」
- [x] 文档（README/BUILD/TESTING/本文件/WORKLOG/DOWNLOAD）同步 1.9.0

---

### 51. 画质 v1.10 —— 第 0 拍 / 尘埃流 / 呼吸全馆化 / 湿反射（v1.10 新增）
- [x] **C1 开幕点灯序列 v2「第 0 拍」**：黑场里碑前长明灯先独亮（一粒火
      先醒），0.9s 后吊灯错拍跟上；收口「尘埃醒来」（光尘层透明度 0→常值
      缓升）——模块级只演一次、openGate 乘法闸与颤动/blackout 互不干扰保持
      ▸ 阶段 1 落地（commit `1ba29db`）；P24 SV_OPEN_SHOT 连拍取证推进有据
- [x] **C2 体积光锥尘埃流**：lightCone 系加程序化竖向尘埃条纹纹理滚动
      （缓慢下沉 + 随 breath 微呼吸），三处英雄光锥（大厅纪念台/蓝丝绒舞台/
      穆赫兰道剧场聚光）；低档自动退素色锥 ▸ kit.test 尘埃流审计入测
- [x] **C3 尘埃呼吸全馆化**：archive/eraserhead/bluevelvet/studio 四厅粒子层
      接 `engine.breath`（只改 opacity 零带宽），与 v1.9 三厅同一相位体系
      ▸ 七厅光尘/雾/烟全部随呼吸起伏（阶段 1 ③）
- [x] **C4 湿润反射**：mulholland 巷口积水洼（暗玻湿面 + ILLUSIÓN 霓虹倒影
      微漾，顶点级动画）——拐角惊吓区氛围加厚，触发逻辑零改动
      ▸ 三轮调参截屏定稿；P8 在册说明纯场景件不设热点
- [x] **C5 电影分级复核**：逐厅默认机位截屏对比微调，发现回归先修再继续
      ▸ P12/P22 两轮七厅终审 + studio「奶感」分级修正（lift/gamma 回拉）
- [x] **C6 低档回退**：以上新效果低档保留或自动降强度，零新增每像素带宽；
      自动降档（5s@<32fps）与 Q 锁定保持 ▸ P1/P11/P21 三轮低档点验入册

**验证方式**：截屏对比 + 源码审读 + `--smoke` 全绿。

### 52. 逐厅英雄资产再增件 + 交互 ≥146（v1.10 新增）
- [x] PRODUCTION_PLAN §2 清单每厅 ≥2 件新增/重做（绞盘/白花、点唱盒/信箱排、
      歌单/蒙布镜、积水洼/呼叫铃、转椅/行李箱、更衣柜/接水桶、
      阅读器/索引灯箱——终版 14 件入册）▸ P27 逐件近景终审七图成画
- [x] 全馆非导航交互 ≥146（**普查 156**，v1.9 143 → +13）；新交互 13 件全部
      ≥2 通道反馈（P8 v110-interactions.test 45 用例逐件断言）、字幕 ≤22 字
      零剧透（P16 全量扫描门禁）；本轮新连锁 5 条入册
- [x] INTERACTIVE_MIN 重锁终版普查 -1（17/28/22/18/20/20/24）；activateAll
      七厅 156 全量激活无异常
- [x] 既有彩蛋/通路/两重惊吓不回退（P7 四连 + P20 三连专项：拐角惊吓自然
      触发 7/7；cornerscare/turnscare/mulholland-path 单测不放宽）
- [x] 性能预算保持 240/240k/40 硬断言全绿；逐厅终版实测入 PRODUCTION_PLAN
      §5.1（峰值 237/102.3k/29，预算未上调）

**验证方式**：`xvfb-run npx electron . --smoke` 逐厅统计全绿 + 截屏复核 + 普查表。

### 53. 音景 v1.10 —— 新合成音色 ≥3 + 零采样（v1.10 新增）
- [x] 新合成音色 ≥3 种（**终版 10 种**：winch/wallbox/lockerclang/waterlap/
      callbell/latchsnap 六种交互音色 + pipeknock/sirenfar/drawerfar/liftbell
      四种远声）——全部与新交互/稀疏事件配套接线、sfxAt 位置化；
      callbell 两短一长包络与铃锤动画同拍入测
- [x] audio.test.js 音色审计扩展（v1.10 组 +8、P13 远声组 +8、P26 混音纪律
      组 +4：音量天花板 1.0 零越界/far 族必位置化/满格裸调用钉界）
- [x] 零采样扫描保持全绿（compliance：src/ 与 electron/ 零图像/音频/视频文件）

**验证方式**：`npm test` audio 组 + 冒烟 activateAll 不抛错。

### 54. Release 1.10.0（v1.10 新增）
- [x] `npm test` / `npm run smoke` / `xvfb-run npx electron . --smoke`
      三连全绿（打包构建产物上复跑，两重惊吓自然触发保持）
      ▸ 348 单测 / 构建冒烟 5/5 / 运行时冒烟 EXIT=0（与 exe 同源 dist）；
      本轮惊吓自然触发累计 13/13
- [x] WORKLOG v1.10 段显示本轮 ≥8 小时（UTC 逐段与 git 提交时间互证）
      ▸ 22:57–06:2x 逐段入册，合计 ≥8h（P1–P33 共 33 遍抛光）
- [x] 版本 bump 1.10.0（package.json + `__SV__.version` 一致，dist 产物内核对）；
      CHANGELOG v1.10.0（说明取 1.10 而非 2.0 的理由）▸ 三处 1.10.0 grep 核对
- [x] `release/SmokeVelvet-LynchArchive-Portable-1.10.0.exe` + Setup NSIS 双目标
      （wine 交叉打包；`file` PE32 校验双过）+ `SHA256SUMS.txt` 重写
      （87787ee5… / 0080fae8…）；旧版 1.9.0 exe 从本分支移除防混淆
- [x] `DOWNLOAD.md` 提供 raw 下载直链并醒目标注「请下本轮 Portable-1.10.0」
- [x] 文档（README/BUILD/TESTING/本文件/WORKLOG/DOWNLOAD）同步 1.10.0

---

### 55. 拐角惊吓拐角化修正 + 压迫感升级（v1.11 新增）
- [x] **触发时机修正（病灶：v1.8–v1.10 触发区北缘 z=-23.6，离拐角
      z≈-26.7 还有 3 米就引爆——错位置）**：CORNER_SCARE.zone 移到
      拐角本体 (9.3,-26.9) r1.6（北缘 z≈-25.3，距拐角沿 1.4m）；导出
      `CORNER_EDGE`，cornerscare.test +7 几何守卫（≤1.6m/不晚 0.3m/
      老病灶回归钉 z≥-25.0 绝不触发）钉死防再漂（阶段 2，`2ad09da`）
- [x] **压迫感节拍升级（次序/留白单测锁定不变）**：dread 拍加 `dreadswell`
      低频升压（brown 低通 + 27→46Hz 次声）；顺手修掉恐惧拍 lampKill
      吃掉第一幕狂闪的时序病（先还灯再起 panic，入测）；lurch 平台段
      红光 2.4Hz 呼吸搏动 + 越挪越前倾（0.12→0.26）+ 侧倾随 s 递增
      （0.06→0.105）——注记：魅影为整体车削剪影无独立头关节，「头部
      歪向玩家」以全身侧倾实现，剪影层读感一致
- [x] **cornerWraith v2 模型细节**：兜帽内 hoodVoid 纯黑无光空腔；第二层
      错相位破披（36 段 Lathe + seeded 撕口下摆双面）；双手各三指过长
      收尖；抽象无面目、零肖像复刻（compliance 词表复跑全绿）
- [x] **冒烟自然触发稳定**：routeA 停点 -24.6/进区步 -26.9 同步新几何；
      新几何下自然触发本轮累计 ≥19 轮全绿（每轮全冒烟）；转身第二扳机
      同轮保持
- [x] **视觉取证突破（P9，翻案 P2 两轮旧帧）**：SV_SHOT_LOW 低档连拍
      拿到**灯闪拍/留白拍/错位收尾字幕**三拍活帧（留白帧像素级核验
      全帧零红点——旧帧病灶字节级相同为证）；SV_SHOT_PRE 定妆照工具链
      （场景图特征定位 + 摄影补光 + 旁白关）拿到**魅影 v2 直立全身
      正面像**：兜帽无面剪影/驼峰肩/长垂臂/披层布褶全部可读；诚实注记：
      lurch/rush 瞬间本体帧仍被合成器冻结窗吃掉（fogSurge+shock 最重
      叠加段），该两拍以自然触发链 + 源审计 + 定妆照为证

**验证方式**：`npm test`（cornerscare 34 用例）+ `--smoke` 连轮自然触发 + 截屏。

### 56. 风格审计 + 粗糙资产重做——树/梯优先（v1.11 新增）
- [x] **风格审计 A1–A6 过单**：117 条字幕 + 146 条 hint 逐条审读；改 1 条
      字幕（衣柜暗侧降格为物性细节）+ 裁 5 处效果预告式括注；A3–A6 转
      新增内容执行纪律；结论逐条入 WORKLOG（阶段 1，`2b3ea9d`）
- [x] **B1 twinpeaks 松林重做**：`pineGeometryMaterial` v2——冠 8 层枝轮
      开口锥 + 枝尖三谐波参差下垂；针叶双色贴图；杆树皮沟壑贴图 + 根部
      张开/S 形微弯/三断枝残桩；体量退让边距 1.6+0.6s；实测 tris 108.1k
      预算内。**偏差注记**：原案「双 LOD 近/远树」简化为**单细节版**——
      InstancedMesh 单 draw call 不因细节翻倍、tris 预算余 55%，LOD 分档
      收益不抵复杂度（阶段 3，`d5ec6c8`）
- [x] **B2/B3 枯树桩鸮 v2 + 咖啡树桩年轮端面**：树皮沟壑 + 树洞暗斑 +
      两级分枝（鸮栖位保持）；侧皮/偏心年轮/底面三材质单 mesh；近景
      终审两图（P3）
- [x] **B4 archive 图书梯细节遍**：中段五级踏面磨浅 + 每级防滑刻槽 ×2 +
      弦木黄铜螺钉 ×8 + 墙轨支耳铆钉 ×6；近景终审一图（P3）
- [x] **B5/B6 拐角护角铁件 + 全馆粗糙面抽查**：东南砖角 L 形锈蚀护条
      （螺栓/流挂/刮亮斜痕——长刮擦的实体锚点，P2 成画）；抽查经 P1–P4
      十余定向机位逐帧过，未再发现「大色块/光杆/平板」级病灶

**验证方式**：近景截屏对比留档（/tmp/shots-v111-*）+ `--smoke` 预算 + antiplastic/captions 全绿。

### 57. 多厅捏他彩蛋 ≥7 + 新音色 ≥3 + 交互 ≥160（v1.11 新增）
- [x] **eraserhead 重点两件**：缠布之物（锌盆缠布团、呼吸微搏动、E→
      屏息 + 壁龛灯病闪 + `wetstir`、怠速翻身，**零字幕**）+ 焦黑坑洼球
      （座陨噪声位移 + 焦炭裂纹、常态自转、E→裂纹发红 + 房灯压暗 +
      `om` 低鸣，**零字幕**）——两件近景成画（P1）
- [x] **五厅各一**：bluevelvet 耳形凹痕首饰盒（负空间；盖合死→停一拍→
      自弹开一缝 + 耳语）/ twinpeaks 椅臂咖啡不认重力（倾 30° 液面跟杯
      走 + `reversecup`）/ studio 帘后上锁小门（三预置位每次进厅掷点 +
      把手半圈 + `deepdrip`，零字幕）/ archive 心跳灯牌接骨（显形期 E→
      整排 22 灯牌 lub-dub 齐搏与音色 0.42s 对时，零字幕）/ lobby 帷幕
      过影（seeded 90–150s 人形暗带 + 绒面脚步 + 帷头轻颤；12s 内触发
      窃语→贴耳 `breath` 连锁，零字幕）——共 7 件，v111-eggs.test 30 用例
- [x] **抛光遍「第二眼」细节六件（P13–P18、P20）**：小门三处磨痕（门会
      搬家这件事地板自己说）/ 灯牌余温（只有不在年表上的记得，26s 缓退 +
      余温期可再按）/ 首饰盒盖内镜像淡印（两面都记得）/ 壁龛湿痕晕
      （锌盆的年头，UV 重映射合并单 mesh + 包围盒自证成画）/ 椅臂咖啡
      放不回原样（立回方位漂移 3–7° 累积钉界 ±20°，预算冻结厅零新增）/
      镜角布光（0.9/2.2m 暖顶光只罩凳盒、光不指向镜子——克制钉界入测
      强度 ≤1/半径 ≤2.5m，灯 29→30 预算内）
      ——全部零字幕零热点或复用既有热点，源审计逐件入测
- [x] **新合成音色 5 种**（dreadswell/wetstir/reversecup/deepdrip +
      fencewomp 巷侧瓦楞铁皮夜风稀发层——位置沿围栏随机、惊吓进行中
      让位守卫入测）零采样、全部接线、位置化 ref≥2、vol≤1.0 混音纪律
      保持（audio.test 天花板审计自动覆盖新增发声点）
- [x] **交互 162 ≥160**（lobby 18 / archive 30 / era 25 / bv 20 / studio 26 /
      tp 22 / mul 21，冒烟逐厅断言）；新交互全部 ≥2 通道反馈；带字幕两件
      ≤22 字、其余五件零字幕（克制口径入测）；activateAll 162 全量无异常
- [x] 合规红线：全部程序化、无面目、非肖像、零对白引用、零剧情长复述
      （compliance/captions/narration 复跑全绿）

**验证方式**：`--smoke` 逐厅断言 + v111-eggs.test + audio.test + 截屏（P1–P4）。

### 58. Release 1.11.0（v1.11 新增）
- [x] `npm test` / `npm run smoke` / `xvfb-run npx electron . --smoke`
      三连全绿（打包构建产物上复跑，两重惊吓自然触发保持）
      ▸ 387 单测 / 静态冒烟 5/5 / 全冒烟 EXIT=0（/tmp/smoke-release.log）
- [x] WORKLOG v1.11 段显示本轮 ≥8 小时（UTC 逐段与 git 提交时间互证）
      ▸ 06:24 起 28 段合计 ≈8h21m
- [x] 版本 bump 1.11.0（package.json + `__SV__.version` 一致，dist 产物
      内核对——asar 内 package.json 与 dist bundle 双处 1.11.0）；
      CHANGELOG v1.11.0
- [x] `release/*-1.11.0.exe` 双目标（wine 交叉打包 + PE32 校验 ×2）+
      `SHA256SUMS.txt` 重写（98b747cf… / d888ea06…）；旧版 1.10.0 exe
      从本分支移除防混淆
- [x] `DOWNLOAD.md` raw 直链醒目标注「请下本轮 Portable-1.11.0」
- [x] 文档（README/BUILD/TESTING/本文件/WORKLOG/DOWNLOAD）同步 1.11.0

### 59. 拐角惊吓贴角化 + 魅影 v3「长发与眼窝」（v1.12 新增）
- [x] **触发再前移收紧**：CORNER_SCARE.zone (9.3,-27.2) r1.15——北缘
      z≈-26.05 距拐角沿 z≈-26.7 仅 **0.65m ≤0.7m**（v1.11 为 1.4m）；
      几何守卫同步收紧（≤0.7m / 不晚 0.3m / 老病灶回归钉收紧到
      z≥-25.8 绝不触发，cornerscare.test 全组）。
- [x] **黑影从拐角处挪出来**：LURK_PATH 三点贝塞尔——hide (7.55,-26.1)
      在侧墙正后方剧场体内（六个可达采样点视线全被墙截断，几何守卫
      入测）→ corner (8.05,-27.25) 贴墙南端点 → out (8.6,-26.8) 贴拐角
      沿离玩家约 1m；三顿挪与贝塞尔联立验算入测：第一顿墙后只闻其声、
      第二顿探出拐角半身、第三顿全身出角；刮擦/闷响声源钉到 CORNER_EDGE。
- [x] **cornerWraith v3**：发帘局部车削留 76° 前脸开口 + 成绺长发 ×13
      参差垂落（前侧两绺垂过胸口框住开口）+ faceVoid 凹陷纯黑弧面 +
      眼窝空洞 ×2（无光黑芯 + 极暗红环随呼吸/扑近搏动 0.5→2.8）+
      headPivot 顿挪一档一档抬头；**A-P3 发帘惯性层**：顿挪平台段身体
      死停但发帘 t 基慢摆（1.7/1.15 rad·s⁻¹）+ 扑近 13 rad·s⁻¹ 高频扑动；
      **抽象非肖像**：无鼻无嘴无瞳无脸皮，compliance 词表复跑全绿。
- [x] **冒烟自然触发稳定**：routeA 同步新几何（停 -24.6/进区 -27.2）+
      走测起步定点化（截屏机位残留位置解耦）；本轮自然触发连轮 15+
      全绿；**A-P4/D-15/RC 专项稳定性统计九批：连续硬断言链 36/36 双
      惊吓全部自然触发 EXIT=0（单厅四批 6/6+3/3+6/6+6/6 + 发布候选全
      冒烟五批 3/3×5，另有巡查轮 +2 累计 38/38）**；转身第二扳机保持；cornerscare.test
      34→41 用例全绿（vitest 实测复核——此前文档记 37 为漏计 it.each 展开项，审计轮修正）。
- [x] **视觉证据**：魅影 v3 定妆照六拍四轮修正（正脸位「发帘框脸 +
      两粒红眼窝环」清晰读出，/tmp/shots-v112-wraith）+ 低档惊吓连拍
      14 帧 13 帧独立（灯闪/留白/错位收尾成像；如实注记：顿挪扑近窗
      口合成器停更与 v1.11 同源，魅影本体帧以定妆照+源审计为证）。

**验证方式**：`npm test`（cornerscare 扩展）+ `--smoke` 连轮自然触发 + 定妆照。

### 60. 资产精修循环——Blender 工作法 Three.js 适配（v1.12 新增）
- [x] **循环纪律**：逐资产 INSPECT→比例/结构→中尺度→近景→shading/
      合并/预算→对照→SAVE 七步入 WORKLOG（阶段 2-B/2-C/4-D 全系列）；
      巡查判定达标者不动（anti-overwork 逐件注记：档案长凳/双峰猫头
      鹰/era 吊钩链/bv 话筒/衣帽架/吊灯/锅炉/电话亭/两处英雄立面）。
- [x] **B twinpeaks 松林/树桩 v3**：每层锥收短 ×0.8 拉开层间空隙 +
      冠内脊露杆 + 顶点色分层明暗（0.62→1.0 一棵树自己有纵深）+
      下层枝轮下垂加深 + 冠 16/杆 12 段；散布件三根倒木（根盘劈裂缘 +
      断枝残桩）；低档退化点验零损失。
- [x] **C archive 图书梯第二遍 + 各厅梯/栏杆复查**：结构级病灶修正
      （梯面 XY→ZY 平面，双钩同高同距落轨 x=4.34 验算精确）+ 踏杆
      贯穿榫/横楔 + 轮叉轴销；overlookRail（柱帽/宽手板/立缘中板）+
      pipeRail（地法兰/球接头/双横管）中近景两拍复核全读出。
- [x] **D placeholder 巡查十四轮**：D-1 石阵（裸黑方块→风化立石）/
      D-2→D-9 双瀑（三轮定案：冠顶崖齿劈水+干影带+织合——形与位勘破）/
      D-3 观景镜（投币双目镜解剖）/ D-4 棕榈（两段式拱叶+枯叶裙）/
      D-5 报箱（朝向病灶转 180°）/ D-6 studio 开盘机（盘解剖+走带路径
      实体化+VU 铝框，净 −1 mesh）/ D-7 大垃圾箱（惊吓戏剧锚点：工业
      漆面贴图+叉车袋+护角钢零新增）/ D-8 放映机盘缘圈 / D-10 老轿车
      （圆角玻璃+B 柱+白圈胎壁 useGroups 双材质零新增）/ **D-11 吧台
      软包立面**（菱形绗缝+扣钉双画布零网格 + 绗缝前板/臂枕卷/黄铜
      踢脚三件结构分割）/ **D-12 红房间丝罩竖向渐变 + era 暖气炉龛 v2**
      （真壁龛+铸铁排管双列+中央台口缺——彩蛋小人就在缺口前起舞，
      净 −4 mesh）/ **D-13 大厅六门虚空**（纯色发光平板→底缘渗光+
      中缝竖隙+楣影三层次，零网格）/ **D-14 帷幕之门帘头卷**      （顶缘
      断口锯齿收口，并进楣幕守恒）/ **D-15 餐馆台面克制化**（近白+
      重蜡面爆白压档，低档判定拍证建模无病）/ **D-17 衣柜百叶逐门
      分幅**（整条横贯双门的装配矛盾→左右两半守恒+0.2 立梃带，中缝
      旋钮回来了）/ **D-18 锯木厂剪影 v2**（平顶盒黑上加黑→双坡月色
      顶+上料坡道+木屑焚炉+顶点色值夜窗 ×2，单 mesh 守恒）——两件
      结构守卫入测 +2（v111-eggs，397→399）；第十一至十四轮再判定
      不动 14 件（打卡钟/气送管站「金线」语言/空白胶片展柜/节拍器/
      公交站晒褪语言/更衣柜排/信箱排/香槟冰桶/还书推车/卡片柜复核/
      花圈/配电箱/画架「未完成的画」语言/星章）——后段连续两轮
      零病灶，巡查以收敛判据收官（十四轮共修 17 件 / 判定不动 33 件）。
- [x] **光/声克制审视项**（用户反馈 1）：写字台台灯（点光 5→3.4、
      半径 8→5.5、泡 3→1.4 塞进罩腔）/ 红房间丝罩（0.7 平铺→梯度
      0.5）/ 暖气龛辉光（径向渐变+减半）/ 餐馆台面（清漆与底色双降）
      ——四处「光吃掉场景」逐一收回，光池归位到该在的地方。
- [x] 预算不破（终值入册，v1.12 全量回归扫描实测）：lobby 198 /
      archive 198 / era 214 / bv 222 / studio 220 / tp 240（贴顶）/
      mul 239 全 ≤240；tris 峰值 tp 140.5k ≤200k；灯峰值 bv 30 ≤40。
- [x] **低档+窄屏第二批点验**：门虚空/暖气龛/吧台软包低档（后处理
      全关）反而更利落；390×844×低档组合两拍成立（字幕换行/HUD 正常）；
      **第三批**：衣柜分幅/锯木厂值夜窗低档零损失（几何+顶点色驱动）；
      **窄屏第三批**（SV_WIN_SIZE 390×844）：两件竖幅构图成立、HUD
      底栏两行重排、字幕换行正常。

**验证方式**：近景截屏对比留档（/tmp/shots-v112-*）+ `--smoke` 预算 + antiplastic。

### 61. 细节与彩蛋可发现性加强（v1.12 新增）
- [x] 七厅英雄道具再一轮二级细节（每厅 ≥1 件，E1–E7 逐件入 WORKLOG）：
      lobby 碑铭「贴花→装配感铜牌」（厚底板+四角沉头螺钉螺槽各朝一向）/
      archive 卡片柜使用痕迹（磨亮拉手+指腹磨光晕）/ mul 票亭交易五金组
      （出票槽双板夹缝+防磨铜条+碟侧螺钉零新增）/ tp 樱桃派真格纹
      （酥皮壁+填馅盘+格纹条+捏花圈 16 粒顶点色单 mesh；sRGB→线性
      +反射率灰化双坑勘破三轮收口）/ bv 点唱机（选曲标签架无真文字
      +投币口铜板）/ era 铅笔解剖（铝箍双压紧滚棱+削木锥+石墨末粒）/
      studio 画布背面内框（撑条+横档+四角撑楔）。
- [x] 既有彩蛋可发现性**光/声引导**（E8）：bv 衣柜每 50–85s 木吱呀
      （方位可辨）+ 板缝红渗光呼吸（彩蛋期 hold 让位）；era 炉栅暖光
      呼吸（炉膛式抖）+ 远蒸汽轻声（blackout 让位）——零新增字幕、
      零新增 mesh，克制口径单测不动。
- [x] **E-9 新彩蛋「门后刚走过一个人」**（一次性，零字幕）：本次进
      馆第一次走近任意一扇门（<2.6m），门虚空里剪影横穿（遮住渗光
      与竖隙）+ 两声极轻空间化脚步，fired 锁存整馆一次；与「帷幕后
      的暗影」机制/位置/读感均不同；抽象无面目三团块剪影（非肖像）；
      v111-eggs +4 源审计（393→397）；横穿帧已捕（/tmp/shots-v112-ghost）。
- [x] 交互不回退：lobby 18 / archive 30 / era 25 / bv 20 / studio 26 /
      tp 22 / mul 21 = **162**；activateAll 全量无异常（每轮冒烟断言）；
      字幕 ≤22 字（captions 全绿）。

**验证方式**：`--smoke` 逐厅断言 + v111-eggs/captions 全绿 + 截屏。

### 62. Release 1.12.0（v1.12 新增）
- [x] `npm test` / `npm run smoke` / `xvfb-run npx electron . --smoke`
      三连全绿（1.12.0 构建产物上复跑：399 全绿 / 冒烟 5/5 / 运行时
      EXIT=0 两重惊吓保持；另加**发布候选浸泡三连跑 3/3**——连续三轮
      全冒烟 EXIT=0、每轮双惊吓自然触发）。
- [x] WORKLOG v1.12 段显示本轮 ≥8 小时（UTC 逐段与 git 提交时间互证：
      15:59 起 40 段无缝衔接至 23:59 合计 **8.00h**，另有打包轮在后；
      三次时间戳校准均按提交时钟收紧不放松）。
- [x] 版本 bump 1.12.0（package.json + `__SV__.version` 一致，dist 产物
      内 rg 常量核对在包）；CHANGELOG v1.12.0 全节。
- [x] `release/*-1.12.0.exe` 双目标 + `SHA256SUMS.txt` 重写；旧版 exe
      从本分支移除防混淆（Portable 95.44MB / Setup 95.61MB，PE32 校验
      ×2、双双低于 GitHub 100MiB 硬限可走 raw 直链；asar 内 package.json
      版本 1.12.0 提取到 /tmp 核对——不重蹈 v1.11 覆盖源文件之辙）。
- [x] `DOWNLOAD.md` raw 直链醒目标注「请下本轮 Portable-1.12.0」
      （直链两条指向本分支 + SHA256 实测值 + 本轮看点五条）。
- [x] 文档（README/BUILD/TESTING/本文件/WORKLOG/DOWNLOAD）同步 1.12.0。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**（拐角+转身两重惊吓
      自然触发保持，/tmp/smoke-release-v112.log）。

### 63. 七厅彩蛋齐加一遍 + 交互 ≥170（v1.13 新增）
- [x] 七厅各 +1 件可发现彩蛋，全部 ≥2 通道反馈（动画/光 + 声 + 短句）：
      lobby 碑背面一支放反了的白花（花头点动 + 光缝借亮 + 穗须声）/
      era 展柜脚下用剩的橡皮（立起转半圈躺回 + 木磕）/
      bv 吧台尽头小费罐（罐身晃 + 硬币脆响）/
      tp 柜台一只倒扣的杯（抬起欠身扣回 + 瓷釉两磕 1.5s 错拍）/
      mul 台口弱音小号（**声音比乐器晚收半拍**：音色 2.3s > 抬落 1.75s，
      与台口「没有乐队」同题——这次不用字幕说）/
      studio 搁板上没干的黏土小像（dt×1.1 极慢转头看你，可转回去）/
      archive 趴在地上的书（**合上是永久的**——世界第一次记得你按过 E）。
- [x] 短句全部 ≤22 字且**一次性锁存**（said/closed）——第二次按 E 只剩
      声与动作，世界不复述自己。
- [x] 新音色 `mutetrumpet`（引擎第 83 种 case）：鼻音分音配比（基频弱、
      2/3 次分音反重）+ 气声垫。
- [x] INTERACTIVE_MIN 重锁：运行时普查 19/33/26/21/23/22/27 = **171**
      （阈值 = 普查 −1，七厅全部上调）。
- [x] mesh 预算 **240→250**（twinpeaks/mulholland 此前贴顶 240/239；
      新峰值双厅 242、留 8 余量；tris/lights 不动；「彩蛋能合的都合、
      预算余量不许拿去加布景」纪律入 PLAN §6——杯身杯柄合并单 mesh 为例）。
- [x] `tests/v113-eggs.test.js` 30 用例：机制字段 / 双通道 / 锁存 /
      错拍 / 永久态 / 阈值全部源码级审计。

**验证方式**：`npx vitest run tests/v113-eggs.test.js` + `--smoke` 逐厅断言。

### 64. 访谈摘录层「更多他自己的话」（v1.13 新增）
- [x] `src/data/interviews.js` 12 条结构化访谈/讲座/著作短引语（中英对照
      + 出处类型 + 策展语境一句），en ≤220 / zh ≤120 字符、与 QUOTES 零重叠。
- [x] 「访谈摘录册」面板（overlay.showInterviews）**收纳纪律**：不进空间、
      主动翻开才读；三入口——原话墙底部按钮 / 年表尾行 / archive 阅览桌
      剪报盒（E → 台灯闪两下 + page 声 + 面板）。
- [x] archive 第 7 座立牌 doughnut（日常仪式主题）+ DOCENT 注解 +1
      （essays.js narration.test USED 同步）。
- [x] 合规口径：粉丝纪念、出处只标**类型**不编造刊物细节、非商业、
      零 verbatim 台词；叙事禁词/元叙述扫描沿用门禁 13/19/33。
- [x] `tests/interviews.test.js` 14 用例（配额/禁词/双语长度/接线审计）。

**验证方式**：`npx vitest run tests/interviews.test.js` + 三入口手工翻页。

### 65. Blender 4.1.1 headless 资产管线 + 首件英雄 loop（v1.13 新增）
- [x] Blender **4.1.1** headless 安装口径入 `scripts/blender/README.md`
      （NLUUG 镜像 tarball + `blender --version` 验证；官方站 Cloudflare
      拦截勘破入册）。
- [x] `scripts/blender/` bpy 管线五件：common.py（studio rig / Cycles CPU /
      GLB 导出）/ gen_corner_wraith.py（block/mid/fine 三阶段生成）/
      inspect_blend.py（INSPECT 网格统计）/ render_views.py（四机位样张）/
      export_glb.py。
- [x] 首件英雄资产**拐角魅影五拍精修 loop 实录**（README 开发日志：
      锥向反置 / fringe 穿地 / 管风琴绺束 / 面部虚空 specular 病灶逐拍
      勘破）；产物 `assets/blender/corner_wraith.{blend,glb}` +
      `assets/blender/renders/` 样张入库。
- [x] `npm run blender:check` 工具链冒烟（生成 block 阶段 + INSPECT；
      未装 Blender 的环境跳过不红——CI 可选项文档化）。
- [x] Three.js 接入规划入 README：GLB → GLTFLoader 后续轮次落厅
      （本轮 toolchain + 1 件示范收口）。

**验证方式**：`blender --version` + `npm run blender:check` + README 步骤复走。

### 66. 林奇风审计 + Release 1.13.0（v1.13 新增）
- [x] `STYLE_AUDIT.md`：现状清点（171 交互 / 约 133 字幕 / 7 立牌 /
      13 DOCENT）+ 六病灶判断（字幕惯性 / 声画同拍 / 世界无记忆 /
      光的礼貌 / 文字收纳 / 预算贴顶纪律）+ 本轮改动清单 + 下一轮观察点。
- [x] 版本 bump 1.13.0（package.json + `__SV__.version` 一致）。
- [x] CHANGELOG v1.13.0 全节 + WORKLOG 本轮逐段。
- [x] `npm test`（443 全绿）/ `npm run smoke`（5/5）/ electron `--smoke`
      （EXIT=0，双惊吓自然触发保持）三连全绿。
- [x] Windows exe：**本轮不打包**（Blender 管线为本轮主投入，代码面
      对打包链路零改动——v1.12 的 1.12.0 exe 仍可用）；打包交接给
      下一轮，步骤 BUILD.md 不变（GOAL_HANDOFF 注明）。

**验证方式**：三连命令 + STYLE_AUDIT/GOAL_HANDOFF 通读。

### 67. Blender loop 第 2/3 件 + GLB 经 GLTFLoader 落厅（v1.14 新增）
- [x] **资产②双峰松树四拍精修 loop**：`gen_pine_tree.py`（block/mid/fine
      三阶段，与 kit.js pineGeometryMaterial v3 同源位移场）；病灶实录
      入 README 开发日志——纸伞钣金锥面（表面噪声+轴向切环）/ 荷叶边
      （锯齿针梢裙边）/ 草叉亮棍（垂须簇宽头藏裙内）/ 顶部噪声鼓包
      （幅度按层半径衰减）。产物 `pine_tree.{blend,glb}` + 四机位样张 +
      block/mid 递进对照。
- [x] **资产③档案图书梯两拍精修 loop**：`gen_library_ladder.py`
      （archive.js 滚梯同源结构：双弦倒角 + 11 车削横档葫芦腰领圈 +
      贯穿榫楔 + 双挂钩 + 轮叉胶轮 + 踏面磨浅顶点色）；病灶实录——
      后倾符号反 / 挂钩藏进弦木剪影 / 胶轮读成五金粒。产物
      `library_ladder.{blend,glb}` + 四机位样张 + 递进对照。
- [x] 管线基础设施升级：`common.py` 通用件上移（seeded / attr_material /
      MeshBuilder / open_cone、make_material +metallic）；**studio rig /
      四机位随主体高度等比取景**（2.35m 人形逐位兼容——大件不再裁头）；
      `npm run blender:check` 扩到三件资产比例架。
- [x] **GLB 落厅（A/B 对照）**：双峰厅林中岔路北缘**孪生松**——西棵
      gen_pine_tree.py GLB（Vite `?inline` data URI + 手动 base64 解码
      绕 electron sandbox fetch 拦截，GLTFLoader.parse 直进）、东棵
      kit pineGeometryMaterial v3 固定实例（InstancedMesh +1 槽零新增
      mesh）。
- [x] STYLE_AUDIT 材质克制条目落实：GLB 落地钳制 roughness ≥0.92 /
      metalness 0 / envMapIntensity 0.25 / 零自发光。
- [x] **声先于形**：40–75s 稀发远处枝腰吱呀（sfxAt 钉在孪生松坐标）；
      树身零字幕交互（针叶簌簌即时 + 吱呀迟半拍错拍）。
- [x] 厅返回 `ready` promise——main.js 等 GLB 就位再宣布 hall-loaded
      （预算普查读完整场景，8s 兜底）；main.cjs 回显 `glb-*` 信号 +
      `player()` 带 yaw。预算实测 twinpeaks 245/250 mesh、144k/240k tris。

**验证方式**：`npm run blender:check` + electron 冒烟 glb-ok 信号 + A/B 机位截屏。

### 68. 访谈摘录 12→20 条 + 低语朗读（v1.14 新增）
- [x] `src/data/interviews.js` **20 条**（+8：鹰级童子军一行简介 / 每个
      观众 / 对立面 / 毒药 / 幽暗处 / 工业与烟 / 从不缺席 / 第一个
      点子）；同一合规纪律——en ≤220 / zh ≤120、context 两短句、
      出处只标类型、与 QUOTES 零重叠、叙事禁词扫描。
- [x] **每张卡片低语钮**：MurmurVoice 把摘录读成气声音节 + 静电碎语
      （永远听不清字——清晰人声不回来）；同钮收声 / 换条互斥 /
      关面板即收声；`onMurmurRead` 回调接线 main.js narration.murmur。
- [x] `tests/interviews.test.js` 阈值提到 ≥20 并新增朗读闭环审计。

**验证方式**：`npx vitest run tests/interviews.test.js` + 面板三入口手工翻页 + 低语钮切换。

### 69. 彩蛋第二批·七厅又各 +1（v1.14 新增，STYLE_AUDIT 纪律升级）
- [x] 七件全部**零字幕 + 错拍默认 + 每厅至多一件永久态**（观察点直接
      变门禁）：lobby 名册中缝钢笔（滚到唇边磕一声 / 1.7s 后没有人的
      翻页 / 永久不回中缝）/ archive 没关严的抽屉（推严 2.6s 自己滑开
      ——你永远改变不了；cardCatalog +skip 格位）/ eraserhead 检修口
      盖板（敲一下 3.5s 后楼那头回敲，一次比一次少，第四次起永久沉默）/
      bluevelvet 吧台火柴盒（strike + 磷头自发光零新光源 / 1.4s 被谁
      吹灭 / 三根用尽永久空盒）/ twinpeaks 手板硬币（弹旋 / 0.8s 迟到
      落定声 / 永久立在手板上）/ mulholland 巷墙海报角（papertear 撕落
      墙脚、底下露更早一层 …CHE / 1.8s 夜风再掀 / 永久躺平）/ studio
      场记板（clapslap 合拍 / 1.2s 顶灯自己暗一拍再回来）。
- [x] 新音色 `papertear` + `clapslap`（引擎第 84/85 种 case）。
- [x] 光的礼貌保持：七件零新增光源（火柴磷头走自发光材质）。
- [x] INTERACTIVE_MIN 重锁：运行时普查 20/34/27/22/25/23/28 = **179**
      （阈值 = 普查 −1：19/33/26/21/24/22/27 = 172）。
- [x] `tests/v114-eggs.test.js` 27 用例：onActivate 块级零字幕断言（精确
      提取块尾防邻域误伤）/ 错拍延迟 / 永久态字段 / 新音色接线 / 阈值。

**验证方式**：`npx vitest run tests/v114-eggs.test.js` + `--smoke` 逐厅激活。

### 70. Release 1.14.0（v1.14 新增）
- [x] 版本 bump 1.14.0（package.json + `__SV__.version` 一致）。
- [x] CHANGELOG v1.14.0 全节 + WORKLOG v1.14 逐段 + TESTING/README/
      STYLE_AUDIT/GOAL_HANDOFF 同步。
- [x] `npm test`（470 全绿）/ `npm run smoke`（5/5）/ electron `--smoke`
      （EXIT=0，双惊吓自然触发保持）/ `npm run blender:check`（三件
      资产比例架）四连全绿。
- [x] Windows exe 双目标打包（Portable + Setup）+ SHA256SUMS 重写 +
      DOWNLOAD.md 直链与实测哈希 + 旧版 exe 移出分支防混用。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 71. Blender loop 第 4 件·大厅纪念浮雕（v1.15 新增）
- [x] **资产④纪念浮雕三阶段五拍精修 loop**：`gen_memorial_relief.py`
      （block 比例架：基座/碑身/檐口 2.04m → mid：框缘 + 凹场位移
      幕褶/下摆/单缕烟 → fine：烟分缕 + 凿痕只留已刻坡面 + 褶脊
      pow 提脊压谷 + 顶点色深度分层 0.46–1.0 + 框缘错缝线脚 +
      鎏金内缘）。五拍病灶实录：磨砂柱（褶幅+波形双修）/ 鎏金线
      埋进框条盒体（移到开口内沿探出框脸 3mm）/ 凿痕成污渍斑
      （只留已刻坡面 + 方向性）/ 褶半波 5 顶点欠采样（84×64 重采样）。
- [x] **GLB 体积纪律确立**：导出器 COLOR_0 强转 float VEC3（BYTE_COLOR
      不省）——网格账目 96×72→84×64 收到 **275KB ≤300KB**；单件
      ≤300KB、每厅 ≤1 件 GLB 入 `tests/v115-eggs.test.js` 钉死。
- [x] `npm run blender:check` 扩到**四件**资产比例架（浮雕 3 mesh /
      高 1.9–2.2m）。

**验证方式**：`npm run blender:check` + 四机位样张 + GLB json 账目核对。

### 72. GLB 落厅第二批：corner_wraith + memorial_relief（v1.15 新增）
- [x] **corner_wraith.glb 落穆赫兰道**（214KB）：评估后走「仅换网格、
      保留程序化动画」——GLB 场景 Y 轴转 π 对齐 lookAt(+Z)，子件
      （发帘/发丝/面部虚空/眼环/眼洞/双臂）重挂运行时 headPivot 与
      arm 枢轴，setLurch/setRush 原逻辑直驱 + 眼环自发光脉冲保持；
      材质钳制豁免 wraithEye/wraithBody（黑影类 specular=0 教训沿用）。
- [x] **memorial_relief.glb 落大厅哀悼角**（东南柱间 atan2 朝室心）；
      交互零字幕：stonebrush 即时 + 2.1s 错拍独石光缝借亮一拍 +
      stonechime 低应（浮雕答的是碑；钳制豁免 reliefGilt）。
- [x] **贴顶厅先合并再新增**：mulholland 七座远山合一（-6 mesh，
      244→242 落 GLB 后仍低于基线）；twinpeaks 柴堆墩顶并入 cutGeos +
      滚木双端盖合一（245→243）。
- [x] wraithReady/reliefReady promise 挂厅返回值——main.js 等 GLB 就位
      再宣布 hall-loaded；冒烟三信号 `glb-landed {lobby relief,
      twinpeaks pine, mulholland wraith}` 齐全。

**验证方式**：electron 冒烟三 glb-landed 信号 + 双惊吓自然触发保持 + 预算普查。

### 73. 彩蛋第三批·远声应答音色谱系（v1.15 新增）
- [x] **谱系确立**：`REPLY_DYAD = [146.83, 174.61]`（D3-F3 小三度）——
      所有「另一边」的应答共享同一副音高（replyhum 有嗓子的那头 /
      replytap 调过音的指节）；「这头」的动作声（clank/thud/woodknock/
      latchsnap）不调音，对照关系单测钉死。
- [x] 六厅各 +1，全部**可重复 / 零字幕 / 游戏时钟错拍 / 零新增光源 /
      无永久态加码**（七厅各恰一件永久态是 v1.14 的账）：archive 通风
      格栅（2.4s 后风道深处应答且每次换一头——它在管道里走）/
      eraserhead 对讲管（3.0s 后极远那头 replyhum）/ bluevelvet 返听
      音箱死敲（v1.4 就在台上的箱子零新增网格，1.9s 后监听从后幕
      回来）/ twinpeaks 松果（磕柴垛 2.2s 后林海深处 replytap）/
      mulholland 售票亭 BACK IN 5 小牌（拨转半圈背面还是 BACK IN 5，
      1.6s 后亭子里面 replyhum）/ studio 墙角铸铁立管（1.4s 后楼下
      回敲——每个厅的「另一边」是同一个另一边）。
- [x] **回归修复**：门禁 69 插场记板时误删工作桌挂载三行——自
      v1.14.0 起整张桌子（抽屉/笔记本/铅笔刀/场记板）不在场景里；
      git 考古找回（studio 207→226 mesh），修复断言入 v115-eggs.test。
- [x] 新音色 replyhum/replytap/stonebrush（引擎第 86/87/88 种 case）。
- [x] INTERACTIVE_MIN 重锁：普查 21/35/28/23/26/24/29 = **186**
      （阈值 = 普查 −1）。
- [x] `tests/v115-eggs.test.js` 33 用例：谱系 / 块级零字幕 / 无锁存 /
      零光源 / GLB 体积纪律 / 回归修复 / 阈值全审计。

**验证方式**：`npx vitest run tests/v115-eggs.test.js` + `--smoke` 逐厅激活。

### 74. 访谈摘录 20→28 条 + 主题筛选（v1.15 新增）
- [x] `src/data/interviews.js` **28 条**（+8：忠于点子 / 谜 / 红帷幕 /
      胶片之后 / 最终剪辑权 / 高尔夫球 / 自私的权利 / 天气播报）；
      同一合规纪律 + 与 QUOTES 零重复（既有立牌语录全部避开）；
      出处类型收「播报」一类。
- [x] **主题筛选**：28 条全部归入四主题（点子 5 / 电影 9 / 心境 8 /
      此生 6，`INTERVIEW_THEMES` 常量）；面板顶部筛选片（全部 +
      四主题带计数）只重排面板内容——D-5 收纳纪律保持文字只进
      面板；换筛即收声（murmur 不留残响）。
- [x] `tests/interviews.test.js` 阈值提至 ≥28 + 主题口径与筛选片审计。

**验证方式**：`npx vitest run tests/interviews.test.js` + 面板筛选片手工切换。

### 75. Release 1.15.0（v1.15 新增）
- [x] 版本 bump 1.15.0（package.json + `__SV__.version` 一致）。
- [x] CHANGELOG v1.15.0 全节 + WORKLOG v1.15 逐段 + TESTING/README/
      STYLE_AUDIT/GOAL_HANDOFF 同步。
- [x] `npm test`（505 全绿）/ `npm run smoke`（5/5）/ electron `--smoke`
      （EXIT=0，双惊吓自然触发保持）/ `npm run blender:check`（四件
      资产比例架）四连全绿。
- [x] Windows exe 双目标打包（Portable + Setup）+ SHA256SUMS 重写 +
      DOWNLOAD.md 直链与实测哈希。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 76. Blender loop 第 5 件·橡皮头蒸汽调速器（v1.16 新增）
- [x] `gen_steam_governor.py` 三档五拍精修：block 比例架（基座/立柱/
      锭轴/双臂飞球/套筒 1.66m）→ mid（柱粗球小/带轮套筒撞件/节流
      杆贴柱三病灶）→ fine（连杆天线化——球顶挂点上移改陡 / S 臂
      折痕平滑 / 叉口啃轴——止于套筒环外缘）。
- [x] 定稿 6 mesh / 2480 顶点 / 4320 tris / GLB **112KB ≤300KB**；
      三视图 + 双阶段诊断图落 `assets/blender/renders/`。
- [x] `blender:check` 纳入第 5 件（block 5 mesh / 高 1.5–1.8m）。

**验证方式**：`npm run blender:check` 五件全过 + 渲染样张目检。

### 77. GLB 落厅第三批 + 冒烟盲区补漏（v1.16 新增）
- [x] **落厅红线**：第三批只落 era/bv/archive/studio（tp/mull 244/250
      贴顶禁入）——steam_governor.glb 落橡皮头厅西墙（机器与锅炉房
      门洞之间），单测钉死贴顶厅无 governor 导入。
- [x] **程序化动画直驱**：GLB 与程序化兜底共用 `rigGovernor` 装配
      （spinPivot 旋转 + armPivots 随 omega 张角 + 套筒升降 + 节流
      杆杠杆随动）；`machineState.run` 直驱转速——泄压阀停机时
      调速器同步塌臂垂停。
- [x] 同步代理热区（不等 GLB）：E → govwhirr 超速一拍、0.9s 错拍后
      大机器冲一拍回敬；`governorReady` 入 ready 承诺 +
      `[sv] glb-landed eraserhead governor` 信号。
- [x] **冒烟盲区补漏（v1.15 工作桌事故制度化）**：`__SV__.
      auditHotspots()`——每个已登记热点的网格 **parent 链上溯必须到
      engine.scene**，返回孤儿 hint 列表；electron 冒烟逐厅普查，
      见孤儿即 `app.exit(1)` 硬失败。交互审计从此**验场景归属**。

**验证方式**：`npx vitest run tests/v116-eggs.test.js` + electron 冒烟
逐厅「热点场景归属：全部在场景树上」行。

### 78. 彩蛋第四批·换轴（v1.16 新增）
- [x] **换轴理由入册**：远声应答（REPLY_DYAD）已铺六厅 + 石钟支系，
      密度到顶——第四批全部换三条新轴，且**九件新音色零件不入
      REPLY_DYAD**（单测对照钉死）。
- [x] **光的应答**（共用 kit `ANSWER_BREATH` 包络）：lobby 烛剪
      （brasstap → 1.5s 后火苗压低又缓缓立回）/ bv 空话筒（micthump
      → 1.5s 后台口整排脚灯呼吸一口，熄灯档也答）。
- [x] **温度**（冷高薄、暖低软）：era 结霜支管（coldhiss → 1.7s 后
      墙外极远 icecrack）/ tp 保温座（warmhum 掌温 → 1.3s 后蒸汽旺
      一大口 + 回滴）/ studio 白瓷小碟（chinatick 收缩三嗒，间隔
      0.9→1.2s 拉长——热量走得越来越慢）。
- [x] **时间错位**：archive 上弦钥匙（秒针挣三格 0.4/1.05/1.9s
      一格比一格迟、一声比一声轻，第三格滑回 6 点位，滑回不响）/
      mull 路灯铁杆（poletap 双鸣即刻，灯隔 **2.4s 用光原样迟放**
      双沉——声与光不同步；应答只走光通道，不加远场重放）。
- [x] **贴顶厅纪律**：tp/mull 零新增网格（热点落既有 potBase /
      streetLampV2 `userData.pole`）；全批零字幕/零光源/可重复
      无锁存；交互 186→**194**（INTERACTIVE_MIN 重锁普查 −1）。

**验证方式**：`npx vitest run tests/v116-eggs.test.js`（47 用例）+
七件手工触发耳测目测。

### 79. 访谈摘录 28→32 条·补「点子」主题（v1.16 新增）
- [x] +4 条全部取自《钓大鱼》可查证原文：钓点子要等 / 接住的一瞬 /
      隔壁的拼图 / 小鱼与大鱼（书名出处段落，与立牌大鱼句互为
      上下句）；「点子」主题 5→**9**（四主题 9/9/8/6 = 32）。
- [x] **防撞记录留档**：doughnut 与 beneath the surface 两候选均已是
      QUOTES 立牌语录——弃用（interviews.js 注释留名）。
- [x] 同一合规纪律；`interviews.test` 阈值 ≥32 + 点子主题 ≥9。

**验证方式**：`npx vitest run tests/interviews.test.js` + 面板「点子」
筛选片计数。

### 80. Release 1.16.0（v1.16 新增）
- [x] 版本 bump 1.16.0（package.json + `__SV__.version` 一致，口径
      入单测）。
- [x] CHANGELOG v1.16.0 全节 + WORKLOG v1.16 逐段 + TESTING/README/
      STYLE_AUDIT §7/GOAL_HANDOFF 第 5 轮交接同步。
- [x] `npm test`（**552** 全绿）/ `npm run smoke`（5/5）/ electron
      `--smoke`（EXIT=0，双惊吓保持 + 四 glb-landed + 七厅场景归属
      普查全过）/ `npm run blender:check`（五件）四连全绿。
- [x] Windows exe 双目标打包 + SHA256SUMS 重写 + DOWNLOAD.md 直链。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 81. Blender loop 第 6 件·酒瓶墙精修件 + GLB 落厅第四批（v1.17 新增）
- [x] `gen_bottle_wall.py` 三档五拍精修 loop（block 比例架两排 12 位
      +躺瓶槽位立规 → mid 奶瓷玻璃/K3 车削棱/药房货架阵形三修 →
      fine 捻口环+堆唇+跟珠+逐瓶明暗顶点色）；首件**宽>高横幅资产**
      （`render_views.py` +wide 机位 5.6H/30mm）。
- [x] 体积纪律两步收编 **469→371→297KB ≤300KB**（16→14→12 段 +
      捻口环两点法）；定稿 4 mesh（三色玻璃+软木塞）/ 11676 tris；
      四剖面 × 三色玻璃 × 31 支 + 2 空缺位 + 7 伴瓶位 + 顶排横躺
      空瓶；`blender:check` 扩到**六件**。
- [x] **落厅红线**：第四批只许 bv/archive/studio（era 每厅 ≤1 件已有
      调速器、tp/mull 244 贴顶禁入——三条全入单测）；首件落 **bv
      吧台背柜**替换程序化酒瓶墙 v2。
- [x] 「仅换网格保留程序化动画」第三次执行：程序化三件即兜底、GLB
      就位原位退场（净账 +1 mesh：bv 231→**232** 冒烟实证）；
      「电压不稳」闪烁改驱 `bottleGlassMats` **活登记表**（换网格
      不换电压线）；材质运行时整套重设（GLB 只带几何/命名/COLOR_0）；
      `bottleWallReady` 承诺 + glb-landed 第五处。

**验证方式**：`npm run blender:check`（六件）+ `npx vitest run
tests/v117-eggs.test.js` + bv 单厅冒烟截屏。

### 82. 彩蛋第五批·问第二遍（v1.17 新增）
- [x] **即时轴**：全馆 wait 错拍彩蛋已 13 件——本批零新增等待，给
      v1.16 七件各加第二层：应答落定的**回声窗**（echo，游戏时钟
      自复位）内再问，**同拍即答**、答在意想不到的通道（烛剪→三对
      流苏齐晃 / 上弦钥匙→停摆钟分针 / 结霜支管→大机器沉半口+
      调速器塌臂 / 空话筒→酒瓶墙泛光 / 保温座→派柜转格 / 路灯
      铁杆→**光先声无**因果换位 / 白瓷小碟→节拍器无声点头）。
- [x] **克制账**：零新增交互（INTERACTIVE_MIN 逐厅**相等**入单测——
      五轮连涨惯性刹车）/ 零新增网格/光源/字幕 / **零新增音色**
      （97 持平，答全走既有通道）；mull 即时重放不续窗（replay
      标记七件同口径）。

**验证方式**：`npx vitest run tests/v117-eggs.test.js` 表驱审计七件 +
手工回声窗内二触耳测目测。

### 83. 时间错位轴共用件盯防（v1.17 新增）
- [x] **单写者纪律入单测**：archive 分针只有 clockState 一个写者、
      秒针只有 windState 一个写者；问第二遍只点火 clockState 不直驱
      任何针；windState `k≥3.4` 全量归位后才开回声窗。
- [x] **并发抽查实录**（冒烟探针，钟针命名取证句柄 clockMinHand/
      clockSecHand）：同帧双触发（停摆钟+上弦钥匙）→ 两针各自挣扎
      各自归位；回声窗第二拧 → 分针同拍挣、**秒针纹丝不动**、全部
      归位（六项断言全 true）。

**验证方式**：SV_SHOT_PRE 探针日志 + `tests/v117-eggs.test.js`
单写者审计用例。

### 84. 访谈摘录 32→34 条·补「此生」主题（v1.17 新增）
- [x] +2 条：过去上的色（纪录片访谈 2016）/ 不退休（公开播报
      2024——「我心里满是幸福，而且我永远不会退休」）；「此生」
      6→**8**（四主题 9/9/8/8 = 34）。
- [x] 防撞纪律第三轮实战：'retire' / 'the past' 关键词与 QUOTES
      逐句比对零重复；出处类型收敛勘破（「公开声明」不在白名单，
      改「播报」）。

**验证方式**：`npx vitest run tests/interviews.test.js` + 面板
「此生」筛选片计数。

### 85. Release 1.17.0（v1.17 新增）
- [x] 版本 bump 1.17.0（package.json + `__SV__.version` 一致，版本钉
      移交 v117-eggs.test）。
- [x] CHANGELOG v1.17.0 全节 + WORKLOG v1.17 逐段 + TESTING/README/
      STYLE_AUDIT §8/GOAL_HANDOFF 第 6 轮交接同步。
- [x] `npm test`（**589** 全绿）/ `npm run smoke`（5/5）/ electron
      `--smoke`（EXIT=0，双惊吓保持 + **五 glb-landed** + 七厅场景
      归属普查全过 + 交互普查 194 持平）/ `npm run blender:check`
      （六件）四连全绿。
- [x] Windows exe 双目标 `dist:win` **完整一次成型** EXIT=0 +
      SHA256SUMS 重写 + DOWNLOAD.md 直链换轨。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 86. Blender loop 第 7 件·卡片柜抽屉阵 + GLB 落厅第五批收官件（v1.18 新增）
- [x] `gen_card_catalog.py` 三档五拍精修 loop（block 19+1 格阵立规
      ——4 列 5 排与运行时 cardCatalog 逐位对齐、HOLE (3,1) 留给
      程序化可拉抽屉 → mid 修木色/镂空标签框/断茬座盘 → fine 起线
      门芯+滑出卡+逐屉顶点色+黑衬板）；第 4 拍 **box(w,h,d) 参数序
      病灶**勘破实录入注（黑衬板 h/d 写反读成外探搁板）。
- [x] 细节账：WORN 三只磨亮、顶排**卡死抽屉 14mm 斜探**带断茬座盘
      与空标签框、两格滑出卡；生成侧 **x 预镜像**对齐落厅 pivot
      rotation.y=π；定稿 **7 mesh / 5136 tris / GLB 146KB ≤300KB**；
      `blender:check` 扩到**七件**。
- [x] **落厅红线**：收官件落 **archive 第一座目录柜**（212 余量
      最大；第二座连同「没关严的抽屉」彩蛋原件不动；tp/mull 贴顶
      禁入、era 每厅 ≤1 照旧）。
- [x] 「仅换网格保留程序化动画」第四次执行：可拉抽屉程序化原件
      动画不动（GLB 洞位预留）、三张静态阵网格原位退场（几何释放/
      共享材质留任/磨亮光晕贴花留任）、材质运行时整套重设；
      **换接缝抹平**（洞位脸板/拉手换 GLB 阵同参材质）；
      `cardArrayReady` 承诺 + glb-landed **第六处**（catalog）。
- [x] **陈年病灶修正**：v1.4 卡沓停在 drawer 局部 z=0 探出柜面
      9cm（场景包围盒探针勘破），退进屉体 -0.15——拉开才看得见
      「卡片都空着」。

**验证方式**：`npm run blender:check`（七件）+ `npx vitest run
tests/v118-eggs.test.js` + archive 单厅冒烟截屏（修复前后对照）。

### 87. 新交互「卡死的抽屉」+ 音色 98（v1.18 新增）
- [x] 顶排斜探的那只：按 E **挣一下又咬死**（14mm 探出量上微弱
      往复动画），`drawerstuck` 即时（**第 98 种音色**——木滑轨
      憋住的一声，**不入 REPLY_DYAD**）；2.2s 后**西墙那头**
      （v1.10「墙后没有房间」的那面墙）同一只抽屉滑轨到底关上
      （drawerfar 错拍应答——这只在这里永远关不上，在那头关得上）。
- [x] 克制口径：**游戏时钟**（零 setTimeout）、**零字幕**、可重复、
      应答期再按无效（re-entry 守卫，防连按刷答）。
- [x] INTERACTIVE_MIN archive 35→**36**（普查 **195**——五轮连涨
      惯性刹车后只为收官件开一格，其余六厅相等语义保持）。

**验证方式**：`npx vitest run tests/v118-eggs.test.js` + 冒烟探针
（挣扎动画位移 + re-entry 守卫断言）实录。

### 88. 回声窗一句暗示 + 访谈 34→38（v1.18 新增）
- [x] **一句不点破的暗示**：留言簿第四条种子「回头客」——访客
      口吻的传闻（不指名哪件、不讲窗口机制、不提秒数）；**禁 UI
      提示**红线不破（单测钉死：无「回声窗/6 秒/echo」字面、
      种子在 store 层不在 UI 层）。
- [x] 访谈 +4 条**四主题齐涨**（10/10/9/9 = **38**）：点子说了算 /
      在电话上看电影 / 生活的抽象（与立牌「直觉」句互为上下文）/
      五十年代的小城（《Lynch on Lynch》访谈录）。
- [x] 防撞纪律第四轮实战：三句候选与 essays.js QUOTES 撞句弃用；
      新四条关键词逐句比对零重复；出处类型白名单口径沿用。

**验证方式**：`npx vitest run tests/interviews.test.js
tests/v118-eggs.test.js` + 面板四主题筛选片计数。

### 89. GLB 轴收官转维护（v1.18 新增）
- [x] 六件 GLB 落六厅收官：tp 松 / mull 魅影 / lobby 浮雕 / era
      调速器 / bv 酒瓶墙 / **archive 卡片柜**；**studio 刻意留空**
      （不为「每轮一件」松红线）。
- [x] 收官红线入单测：七件资产全 ≤300KB 实测、六处落厅名单钉死、
      studio 零 GLB 导入、tp/mull 贴顶禁入语义不回退、每厅 ≤1 件。
- [x] `blender:check` 七件资产比例架全绿；GLB 轴自此**转维护**
      ——后续轮次只修不添，除非红线内出现新余量。

**验证方式**：`npm run blender:check` + `npx vitest run
tests/v118-eggs.test.js` 门禁 89 段。

### 90. Release 1.18.0（v1.18 新增）
- [x] 版本 bump 1.18.0（package.json + `__SV__.version` 一致，版本钉
      移交 v118-eggs.test；v117 计数钉改「不回退」语义）。
- [x] CHANGELOG v1.18.0 全节 + WORKLOG v1.18 逐段 + TESTING/README/
      STYLE_AUDIT §9/GOAL_HANDOFF 第 7 轮交接同步。
- [x] `npm test`（**607** 全绿）/ `npm run smoke`（5/5）/ electron
      `--smoke`（EXIT=0，双惊吓保持 + **六 glb-landed** + 七厅场景
      归属普查全过 + 交互普查 **195**）/ `npm run blender:check`
      （七件）四连全绿。
- [x] Windows exe 双目标 `dist:win` **完整一次成型** EXIT=0 +
      SHA256SUMS 重写 + DOWNLOAD.md 直链换轨。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 91. 回声窗第七批·窗长差异化——余温总账 9s（v1.19 新增）
- [x] **模板拆除**：七件回声窗全 ≈6s（archive 8s 唯一例外且方向
      反了——最慢落定配了最长窗）→ **窗长 = 9 − 落定时长**：
      tp 7.7 / era 7.3 / mull·studio 6.0（账落老值）/ bv 5.7 /
      archive 5.6 / lobby 5.3——落定慢的窗短、落定快的窗长
- [x] **机制零改动**：echo 倒数/问一次即消耗/游戏时钟自复位/
      即答通道照旧，只改常数 + 逐厅留账注释；零新增交互/网格/
      光源/音色/字幕（v117 口径全部不回退）
- [x] **单测钉死**（v119-eggs 25 用例）：七件窗长精确钉 + 落定
      账目行溯源（算术要有源码根据）/ 总账守恒 9.0 / 至少 5 个
      不同窗长值 / 逆序单调一对不破 / archive 8s 不回流（v117
      精确钉移交）
- [x] **运行时双端取证**（SV_SHOT_PRE 探针 + 注入游戏钟 updater
      对时——软渲染 0.8fps 真实钟不可用，SV_WIN_SIZE=360x560
      提步速 0.08→0.42）：archive 探针七项全真（窗内二问分针同
      拍挣/秒针纹丝不动；落定后 7.0 游戏秒——旧账 8s 仍会答的
      位置——窗已关、开的是新序列）；tp 探针 ok（askedAtG 7.91
      落在旧账 6s 已关、新账 7.7 还开的区间，答走派柜 5.8× 基线）

**验证方式**：`npx vitest run tests/v119-eggs.test.js` + 两厅
SV_SHOT_PRE 探针日志 + 手工按新窗长复测（TESTING v1.19 抽查段）。

### 92. GLB 轴维护巡检 + 第 7 轮纪律三钉（v1.19 新增）
- [x] **维护巡检零病灶**：六处落厅定向机位目检（lobby 浮雕贴近
      ——凹场烟缕走近显影 / archive 卡片柜——抽屉脸/拉手/标签
      框全读出、卡沓修正不回潮 / era 调速器 / bv 酒瓶墙 / tp 松
      林 / mull 魅影走拐角惊吓自然触发功能取证）——**判定不动**
      （anti-overwork：达标者不回炉、不为「每轮一件」立新项）
- [x] 维护轴入测：gen_*.py 恰七件 / 在库 GLB 恰六件全 ≤300KB /
      studio 零导入
- [x] **「墙那头」封口**：archive drawerfar 调用点钉恰三处、
      其余六厅禁借用——西墙叙事呼应成对到此为止
- [x] **暗示预算用尽**：留言簿种子恰四条、含暗示的只有「回头客」
      一条；UI 层零提示复钉
- [x] **访谈封顶**：38 持平（≥38 且 ≤40，四主题各 ≥9）；音色 98
      持平（v118 精确钉守恒）；交互普查 195 持平

**验证方式**：`npx vitest run tests/v119-eggs.test.js` + 六厅
定向机位截屏留档（/tmp/shots-v119-glb*）。

### 93. Release 1.19.0（v1.19 新增）
- [x] 版本 bump 1.19.0（package.json + `__SV__.version` 一致，版本钉
      移交 v119-eggs.test；v118 版本钉改通值语义）。
- [x] CHANGELOG v1.19.0 全节 + WORKLOG v1.19 逐段 + TESTING/README/
      STYLE_AUDIT §10/GOAL_HANDOFF 第 8 轮交接同步。
- [x] `npm test`（**632** 全绿）/ `npm run smoke`（5/5）/ electron
      `--smoke`（EXIT=0，双惊吓保持 + 六 glb-landed + 七厅场景
      归属普查全过 + 交互普查 **195** 持平）/ `npm run blender:check`
      （七件）四连全绿。
- [x] Windows exe 双目标 `dist:win` **完整一次成型** EXIT=0 +
      SHA256SUMS 重写 + DOWNLOAD.md 直链换轨。
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**。

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

### 94. studio 台灯 v3·手作精修（v1.20 新增）
- [x] **活轴盘点先行**（STYLE_AUDIT §11）：七轴过账两投两不投——
      studio 程序化精修与访谈质量维护两条活轴投入；低密度厅
      「第三层反应」**判死**（问第三遍同构，红线不许换厅名回流）
- [x] **手作四件账**：车削木重底座（座肩车刀痕 + 手刻歪旋钮）/
      手弯铁杆（CatmullRom 单管四弯 ±6mm 出平面 + 蝶形螺母）/
      手锤黄铜罩（整数角频率定植锤痕，接缝安全；手刷绿漆横道
      毛边）/ 布包余线盘桌面——v2 工厂 Anglepoise（珐琅座/镀铬
      双节臂/拉簧/clearcoat 清漆）全部退役且单测禁回流
- [x] **精修只动做工不动账**：热点（shade）/光路（3.4/5.5/1.8
      D-11 三值）/hint/两句字幕/音色一字不动；精修函数区零
      setTimeout/零 sfx/零 caption（单测钉死）；mesh 账 7→5
      （studio 227→**225** 冒烟实证）；studio 零 GLB 导入复钉
- [x] **定向机位双样张目检**：开灯全景 + 关灯特写（SV_SHOT_PRE
      activateByHint 关灯、S.hall() 守卫）——手作语言成立

**验证方式**：`npx vitest run tests/v120-eggs.test.js` +
/tmp/shots-v120{,-off}/studio.png 目检。

### 95. 访谈质量维护首轮——替换不追加（v1.20 新增）
- [x] **弱条目退役有账**：absurdity（「满眼荒诞」）——与立牌
      sense（「生活讲不通」）领地重叠、语义最薄；退役理由留账
      进数据源注释，en 原句全库不回流
- [x] **换血同格**：detectives（「我们在生活里都像侦探」）心境
      主题原格换入——**38 条恰等 + 四主题 10/10/9/9 逐格相等**
      双持平（涨一条都算破「替换不追加」口径）
- [x] **防撞五轮口径照旧**：detective 语料在 QUOTES/DOCENT/既有
      条目零出现；en ≤200 / zh ≤120 / 出处只标类型

**验证方式**：`npx vitest run tests/v120-eggs.test.js tests/interviews.test.js`。

### 96. GLB 维护巡检第 2 轮 + Release 1.20.0（v1.20 新增）
- [x] **巡检更轻口径**（§10「零改动焦虑」承诺兑现）：落厅代码
      零改动 → 免全量定向截屏；blender:check 七件全过（Blender
      4.1.1 NLUUG 重装）+ 六 glb-landed 冒烟信号全在 + mull 魅影
      拐角惊吓自然触发功能验证——**零病灶、零改动、零回炉**
- [x] 版本 bump 1.20.0（package.json + `__SV__.version` 一致，
      版本钉移交 v120-eggs.test；v119 版本钉改同值语义）
- [x] CHANGELOG v1.20.0 全节 + WORKLOG v1.20 逐段 + TESTING/README/
      STYLE_AUDIT §11/GOAL_HANDOFF 第 9 轮交接同步
- [x] `npm test`（**649** 全绿）/ `npm run smoke`（5/5）/ electron
      `--smoke`（EXIT=0，双惊吓保持 + 六 glb-landed + 普查 **195**
      持平 + studio mesh 225）/ `npm run blender:check`（七件）
      四连全绿
- [x] Windows exe 双目标 `dist:win` **完整一次成型** EXIT=0 +
      SHA256SUMS 重写 + DOWNLOAD.md 直链换轨
- [x] 打包后发布产物验证：全冒烟复跑 **EXIT=0**

**验证方式**：四连命令 + DOWNLOAD.md 直链下载校验。

---

## 补充说明：关于「PS5 级别」
v1.2 曾以门禁 17 落地第一版清单；v1.3 以门禁 21 升级为 PS4-tier（三通道 PBR/
Sobel 法线/各向异性）；**v1.4 以门禁 26 的 P1–P10 定义 PS5-tier**（五通道 PBR/
新高级材质组/电影分级/halation/双层体积光/远近景三层/微观质感），即高完成度
风格化实时渲染的第二级。以 WebGL 合理复杂度实现电影氛围，不虚假承诺主机
独占级光追画质；交互探索性为第一优先。

---

## 最终自检结果（v1.3.0）

| # | 门禁 | 状态 | 备注 |
|---|------|------|------|
| 19 | 讲解克制 | ✅ | 8×≤16 字风格化旁白（84 字）/ 展签每厅 ≤1 / 字幕 ≤22 字 / 叙事禁词扫描 |
| 20 | 交互密度 | ✅ | 90 件非导航交互（10/19/12/11/14/13/11），阈值锁普查 -1 入冒烟 |
| 21 | PS4-tier 清单 | ✅ | Sobel 法线三通道纹理组 ×8 + 35 预制体 + 三遍艺术重做 + 20+ 机位截屏复核 |
| 22 | 性能预算 | ✅ | 220/200k/40 硬断言；实测峰值 203 mesh / 12.4 万 tris / 26 灯；自动降档实证 |
| 23 | 音景抛光 | ✅ | 18 种交互音（含 7 脚步 + rain）/ sfxAt 位置化 / 程序化混响四预设 / 爵士填充+LFO |
| 24 | Release 1.3.0 | ✅ | 双 exe（各约 100MB）+ SHA256SUMS + PE 校验 + 文档同步 |

以下为 v1.2.0 存档：

| # | 门禁 | 状态 | 备注 |
|---|------|------|------|
| 1 | 美术 | ✅ | 7 场景全程序化，统一色板/字体，截屏复核 |
| 2 | 画面 | ✅ | 雾/动态光/粒子/后处理链（含冲击通道）全配 |
| 3 | 性能 | ✅ | 每厅 ≤155 网格 / ≤11.2 万三角，预算断言入冒烟 |
| 4 | 音效 | ✅ | 7 配方底噪 + 27 种合成音效 + duck，零采样 |
| 5 | 语音旁白 | ✅ | 8 段原创旁白（每段 ≤24 字），四档模式 |
| 6 | 社交 | ✅ | 发帖/点赞/回复 + localStorage + 3D 留言板 |
| 7 | 版权合规 | ✅ | 合规页 + README 免责 + 短引语审计 + 自动扫描 |
| 8 | 测试 | ✅ | 42 单测 + 构建冒烟 + 运行时七厅巡检全绿 |
| 9 | Release | ✅ | 两个 Windows exe（1.2.0）+ 校验和 + BUILD.md |
| 10 | 旁白重做 | ✅ | 字母显现默认 / 爵士层 / 戏剧化 TTS / 四档循环 |
| 11 | 每厅彩蛋 | ✅ | 7 厅 7 彩蛋，穆赫兰道惊吓可复现并入冒烟 |
| 12 | 林奇的房间 | ✅ | 9 项交互 + 叙事链 + 冥想深水序列 + 读书角 |
| 13 | 文案审计 | ✅ | 标签清退 + 12 条原话引语 + 理论名词清零 |
| 14 | 重新打包 | ✅ | 1.2.0 portable + setup + SHA256SUMS |
| 15 | 留白文案 | ✅ | 文章体系移除 / 旁白 ≤24 字 / 每厅展签 1 块 / 字幕 ≤26 字（源码级单测） |
| 16 | 多分区地图 | ✅ | 双峰 5 分区小径互连；六厅均加可逛深度 |
| 17 | 资产精度 | ✅ | 圆角/车削/合并多部件 + 程序化 PBR 纹理 + 体积光 + 截屏复核 |
| 18 | 预算入冒烟 | ✅ | meshes≤260 / tris≤40 万 硬断言，实测最高 155 / 11.2 万 |
