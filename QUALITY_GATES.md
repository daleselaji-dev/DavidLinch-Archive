# QUALITY GATES — 大卫·林奇数字纪念展览馆

> 本文件为交付前必须全部通过的质量门禁。每一项在交付前对照自检，
> 通过后勾选并注明验证方式。未全绿不得宣称交付。

## 门禁清单

### 1. 美术 — 统一视觉语言
- [ ] 统一色板：深黑 `#0a0608` / 天鹅绒红 `#8f0e1e` / 霓虹蓝 `#3ec5ff` / 荧光粉 `#ff2e88` / 象牙白 `#f2e9dc`（定义于 `src/style.css` CSS 变量与场景材质常量）
- [ ] 统一字体语言：衬线展签体（标题）+ 等宽档案体（正文/UI），全站一致
- [ ] 禁止 AI 默认紫白渐变、无个性模板界面 —— 全部界面为定制暗色档案馆风格
- [ ] 至少 3 个可进入的作品展厅 **+** 1 个总览大厅（实际数量：____）
- [ ] 所有场景为原创程序化生成（几何/材质/纹理均由代码生成，无外部版权素材）

**验证方式**：运行 `npm run dev`，逐一进入大厅与各展厅目视检查。

### 2. 画面 — 氛围与响应式
- [ ] 每个展厅具备体积感雾效（`FogExp2` + 动态烟雾粒子层）
- [ ] 动态光源（闪烁灯、霓虹、频闪）至少每厅 2 处
- [ ] 粒子系统（尘埃/烟雾/雪点）至少每厅 1 套
- [ ] 全屏后处理链：Bloom + 胶片颗粒 + 暗角 + 扫描线 + 色差 + 亮度颤动
- [ ] 桌面（≥1280px）与窄屏（≤768px，含触屏虚拟摇杆/点击移动）布局均可用

**验证方式**：桌面全屏 + 浏览器 DevTools 模拟 375px 宽度各操作一遍。

### 3. 性能
- [ ] 中档机合理场景复杂度：单厅 draw call < 120，三角面 < 40 万
- [ ] 交互目标 30fps+（像素比钳制 ≤ 1.5，自适应降级开关）
- [ ] 无严重卡死：展厅切换异步加载 + 加载过场，主线程无 >200ms 长任务循环
- [ ] 资源懒加载：各展厅模块 `import()` 动态分包，按需加载

**验证方式**：内置 FPS 计数（`F` 键切换）观察 ≥30；`npm run build` 查看分包产物。

### 4. 音效 — 全程序化合成
- [ ] 环境底噪：WebAudio 合成低频嗡鸣（每厅不同配方：振荡器+噪声+滤波）
- [ ] 至少 2 类交互音（实际：悬停嗡音 / 点击电脉冲 / 传送门骤响 / UI 翻页，均合成）
- [ ] 无任何采样自受版权保护原声带的音频
- [ ] 全局静音开关（`M` 键 + UI 按钮）

**验证方式**：戴耳机逐厅巡检；源码检索确认无音频资源文件。

### 5. 语音 / 旁白
- [ ] 至少一段原创短旁白文案（馆长导览词，见 `src/data/essays.js`）
- [ ] 通过 Web Speech API（`speechSynthesis`）朗读，不可用时显示字幕降级
- [ ] 可开关（UI 按钮 + `V` 键），默认字幕常显

**验证方式**：进入大厅触发欢迎旁白，切换开关验证停止/恢复。

### 6. 社交 — 讨论区
- [ ] 访客留言墙：可发帖（昵称+内容）、列表展示、点赞、回复，基础闭环完整
- [ ] 数据持久化：`localStorage`（刷新/重启应用后保留）
- [ ] 输入做基本转义，防 XSS

**验证方式**：发帖→点赞→回复→刷新页面确认数据保留；`npm test` 含 store 单测。

### 7. 版权合规
- [ ] 应用内「版权与合规」页：声明非官方、非授权、独立粉丝艺术纪念展
- [ ] README 顶部免责声明
- [ ] 内容自查：无电影截图/官方海报/原声采样/受保护对白 verbatim/角色精确肖像/商标 Logo
- [ ] 事实性内容（年份、片名、奖项）仅使用公开事实；评述均为原创文案

**验证方式**：人工全文审读 `src/data/*.js` 与全部 UI 文案。

### 8. 测试
- [ ] `npm test` 通过（vitest：数据完整性 / 留言 store / 音频参数 / 文案合规扫描）
- [ ] `npm run smoke` 通过（构建产物存在性 + Electron 主进程语法 + 页面模块解析）
- [ ] `TESTING.md` 手动测试清单齐备

**验证方式**：CI 式本地执行，两命令 exit 0。

### 9. Release — Windows 产物
- [ ] `release/` 目录含可分发 Windows x64 产物，优先 `.exe`（portable 与/或 NSIS 安装包）
- [ ] `BUILD.md` 说明一键构建命令与环境要求
- [ ] 产物经解包/启动冒烟验证（Linux 下以 wine 或结构校验方式）

**验证方式**：`npm run dist:win` 产出；检查 `release/*.exe` 存在且体积合理。

## 补充说明：关于「PS5 级别」
本项目将其定义为 **高完成度风格化实时渲染目标**：PBR 材质、全屏后处理链（Bloom/颗粒/色差/暗角）、体积氛围、流畅第一人称探索。以 WebGL 合理复杂度实现电影氛围，不虚假承诺主机独占级光追画质；交互探索性为第一优先。

---

## 最终自检结果

（交付前填写，全部 ✅ 后方可宣称交付）

| # | 门禁 | 状态 | 备注 |
|---|------|------|------|
| 1 | 美术 | ⬜ | |
| 2 | 画面 | ⬜ | |
| 3 | 性能 | ⬜ | |
| 4 | 音效 | ⬜ | |
| 5 | 语音旁白 | ⬜ | |
| 6 | 社交 | ⬜ | |
| 7 | 版权合规 | ⬜ | |
| 8 | 测试 | ⬜ | |
| 9 | Release | ⬜ | |
